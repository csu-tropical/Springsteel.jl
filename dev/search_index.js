var documenterSearchIndex = {"docs":
[{"category":"section","location":"fourier/#Fourier","page":"Fourier","text":"The Fourier submodule implements the 1D Fourier ring basis used for the azimuthal direction in cylindrical-coordinate grids (RL_Grid, RLZ_Grid) and for purely periodic grids (L, LL, LLZ). It uses FFTW.jl real-to- halfcomplex transforms (FFTW.R2HC / FFTW.HC2R) with a pre-measured plan for maximum throughput.","title":"Fourier"},{"category":"section","location":"fourier/#Mathematical-Overview","page":"Fourier","text":"For a real-valued function u on the circle 0 2pi) sampled at N equally spaced points phi_n = 2pi n  N, the discrete Fourier series writes u as a sum of sines and cosines up to the Nyquist wavenumber lfloor N2 rfloor:\n\nu(phi) = fraca_02\n          + sum_k=1^lfloor N2 rfloor - 1\n              bigl(a_k cos(kphi) + b_k sin(kphi)bigr)\n          + fraca_N22cosleft(tfracN2phiright)\n\nwhere the coefficient pair (a_k b_k) captures the k-th azimuthal wavenumber. The last term is the Nyquist mode and is real-valued only when N is even. Because u is real, the full complex spectrum is conjugate-symmetric and only lfloor N2rfloor + 1 independent coefficients need to be stored — this is the half-complex (R2HC) layout FFTW uses.\n\nDerivatives in the spectral domain are exact and local: differentiating cos(kphi) gives -ksin(kphi) and differentiating sin(kphi) gives kcos(kphi), so the n-th derivative operator is a diagonal multiplication by (ik)^n on the complex spectrum (modulo the real-valued storage reshuffle). This is why Fourier bases are the natural choice for periodic directions: exactness plus FFT-speed evaluation.\n\nSpringsteel stores the half-complex coefficients in the order [a_0, a_1, a_2, ..., a_{N/2}, b_{N/2-1}, ..., b_2, b_1] — the standard FFTW R2HC layout. Internally the cylindrical and spherical grids further rearrange these per-ring coefficients into the larger grid.spectral array using the wavenumber-interleaved layout documented in Contributing.","title":"Mathematical Overview"},{"category":"section","location":"fourier/#References","page":"Fourier","text":"Wikipedia: Discrete Fourier transform\nWikipedia: Fourier series\nPress et al., Numerical Recipes, 3rd ed., Chapter 12\nFFTW manual: One-dimensional DFTs of real data","title":"References"},{"category":"section","location":"fourier/#Boundary-Condition-Constants","page":"Fourier","text":"","title":"Boundary Condition Constants"},{"category":"section","location":"fourier/#Parameter-and-Data-Structures","page":"Fourier","text":"","title":"Parameter and Data Structures"},{"category":"section","location":"fourier/#Internal-Setup-Functions","page":"Fourier","text":"","title":"Internal Setup Functions"},{"category":"section","location":"fourier/#Transform-Functions","page":"Fourier","text":"","title":"Transform Functions"},{"category":"section","location":"fourier/#Matrix-Representations","page":"Fourier","text":"","title":"Matrix Representations"},{"category":"section","location":"fourier/#Generic-Wrappers","page":"Fourier","text":"No-prefix wrappers that delegate to the F-prefixed functions above, enabling basis-type-agnostic code.","title":"Generic Wrappers"},{"category":"constant","location":"fourier/#Springsteel.Fourier.PERIODIC","page":"Fourier","text":"Periodic boundary condition (Ooyama 2002, section 3e): the only valid BC for the Fourier basis. Couples the left and right ends of the azimuthal domain to simulate a cyclically continuous ring. All Fourier rings use this BC automatically; it need not be specified explicitly in most APIs.\n\n\n\n\n\n","title":"Springsteel.Fourier.PERIODIC"},{"category":"type","location":"fourier/#Springsteel.Fourier.FourierParameters","page":"Fourier","text":"FourierParameters\n\nImmutable parameter struct (using @kwdef) for a 1D Fourier ring basis.\n\nFields\n\nymin::Float64: Azimuthal offset (radians) of the first grid point. Values other than 0.0 activate the phase-shift filter so that coefficients are referenced to a common zero-phase origin.\nkmax::Int64: Maximum retained wavenumber. All Fourier modes with wavenumber k > kmax are zeroed by the phase-filter matrix.\nyDim::Int64: Total number of physical grid points around the ring. Must be even for the real-to-halfcomplex FFT (FFTW.R2HC).\nbDim::Int64: Number of filtered Fourier B-coefficients. Formula: bDim = 2*kmax + 1 (one constant term plus one cosine and one sine coefficient per wavenumber).\n\nNotes\n\nUnlike CubicBSpline.SplineParameters, there are no auto-computed fields; all four fields must be set explicitly at construction time.\n\nExample\n\nfp = Fourier.FourierParameters(\n    ymin = 0.0,\n    kmax = 10,\n    yDim = 64,\n    bDim = 21   # 2*kmax + 1\n)\n\nSee also: Fourier1D\n\n\n\n\n\n","title":"Springsteel.Fourier.FourierParameters"},{"category":"type","location":"fourier/#Springsteel.Fourier.Fourier1D","page":"Fourier","text":"Fourier1D\n\nOne-dimensional Fourier ring object. Construct via Fourier1D(fp::FourierParameters).\n\nFields\n\nparams::FourierParameters: Ring configuration (domain offset, kmax, grid size)\nmishPoints::Vector{Float64}: Evenly-spaced angles in [ymin, ymin + 2π) (half-open)\nfftPlan::FFTW.r2rFFTWPlan: Pre-measured real-to-halfcomplex forward FFT plan. Do not serialise; FFTW plans are not portable across processes.\nifftPlan::FFTW.r2rFFTWPlan: Pre-measured halfcomplex-to-real inverse FFT plan.\nphasefilter::PhaseFilter: Combined azimuthal phase-shift + spectral cutoff. Stored as O(kmax) precomputed trig values (not a dense matrix) and shareable across rings with identical (yDim, kmax, bDim, ymin). Applied forward via apply_phasefilter_forward! and inverted via apply_phasefilter_inverse!.\nuMish::Vector{Float64}: Physical field values at the yDim ring points (mutable buffer).\nb::Vector{Float64}: Filtered B-coefficients of length bDim (result of FBtransform).\na::Vector{Float64}: Zero-padded A-coefficients of length yDim, ready for inverse FFT (result of FAtransform).\nax::Vector{Float64}: Working buffer of length yDim for derivative and integral coefficient computations.\n\nNotes\n\nConstructing Fourier1D calls FFTW.plan_r2r with FFTW.PATIENT, which performs benchmark measurements to find the fastest algorithm. First construction is slow; subsequent transforms using the same plan are fast.\nThe struct is not thread-safe if uMish, b, a, or ax are mutated concurrently. Create one Fourier1D per thread for parallel workloads.\n\nSee also: FourierParameters, FBtransform, FAtransform, FItransform\n\n\n\n\n\n","title":"Springsteel.Fourier.Fourier1D"},{"category":"type","location":"fourier/#Springsteel.Fourier.PhaseFilter","page":"Fourier","text":"PhaseFilter\n\nApply-once azimuthal phase shift and spectral cutoff for a single Fourier ring or Chebyshev column. Stored as O(kmax) precomputed trig values rather than a dense (yDim × bDim) matrix.\n\nFields\n\nyDim::Int — physical azimuthal point count (R2HC scratch length).\nkmax::Int — effective max wavenumber kept. The apply loop walks 1..kmax.\nbDim::Int — length of the filtered output vector b. Must satisfy bDim ≥ 1 + 2*kmax (equality for single-patch; strict > allowed when a caller reserves extra zero-padding slots).\ncos_k::Vector{Float64} — cos(k·ymin) for k = 1..kmax. Empty ⇒ no shift (Chebyshev and other non-periodic uses).\nsin_k::Vector{Float64} — sin(k·ymin) for k = 1..kmax. Must match cos_k length.\n\nLayout convention\n\nThe b vector mirrors FFTW's halfcomplex (R2HC) ordering, but with bDim:\n\nb[1] — DC term (k = 0)\nb[k+1] — real part of wavenumber k, for k = 1..kmax\nb[bDim - k + 1] — imaginary part of wavenumber k, for k = 1..kmax\nall other slots — zero (cubic-grid anti-aliasing / max_wavenumber cutoff)\n\nForward apply computes b[k] from the R2HC fft output via a rotation by -k·ymin; inverse apply reverses it (rotation by +k·ymin).\n\n\n\n\n\n","title":"Springsteel.Fourier.PhaseFilter"},{"category":"function","location":"fourier/#Springsteel.Fourier.calcMishPoints","page":"Fourier","text":"calcMishPoints(fp::FourierParameters) -> Vector{Float64}\n\nCompute the evenly-spaced grid point locations around the Fourier ring.\n\nPoints are placed at angles ymin + 2π*(n-1)/yDim for n = 1:yDim. The grid is half-open: the last point is one step before ymin + 2π, so the starting angle is not repeated when the ring closes.\n\nReturns\n\nVector{Float64}: Ring angles of length yDim, beginning at ymin\n\nSee also: Fourier1D, FBtransform\n\n\n\n\n\n","title":"Springsteel.Fourier.calcMishPoints"},{"category":"function","location":"fourier/#Springsteel.Fourier.calcPhaseFilter","page":"Fourier","text":"calcPhaseFilter(fp::FourierParameters) -> Matrix{Float64}\n\nBuild the phase-shift and anti-aliasing filter matrix of size (yDim, bDim).\n\nCombines two operations into a single matrix multiplication applied after the forward FFT:\n\nPhase shift: rotates each wavenumber k by angle −k·ymin so that spectral coefficients are referenced to a common ymin = 0 origin, enabling coherent aggregation along an orthogonal dimension (e.g. with cubic B-splines).\nAnti-aliasing truncation: keeps only wavenumbers 0:kmax; higher modes are not present in the bDim-column output.\n\nBlock structure of the matrix:\n\n(1, 1) → wavenumber 0 (constant term pass-through).\nFor each k = 1:kmax: a 2×2 rotation block placed at rows (k+1, yDim-k+1) and columns (k+1, bDim-k+1).\n\nReturns\n\nMatrix{Float64}: Phase-filter matrix of size (yDim, bDim)\n\nSee also: calcInvPhaseFilter, FBtransform\n\n\n\n\n\n","title":"Springsteel.Fourier.calcPhaseFilter"},{"category":"function","location":"fourier/#Springsteel.Fourier.calcInvPhaseFilter","page":"Fourier","text":"calcInvPhaseFilter(fp::FourierParameters) -> Matrix{Float64}\n\nBuild the inverse phase-shift and zero-padding matrix of size (bDim, yDim).\n\nThe inverse of calcPhaseFilter. Applies angle +k·ymin to each wavenumber to undo the phase shift, then zero-pads the bDim filtered coefficients back to a yDim-length array for the subsequent inverse FFT.\n\nBlock structure mirrors calcPhaseFilter with opposite sign rotation.\n\nReturns\n\nMatrix{Float64}: Inverse phase-filter matrix of size (bDim, yDim)\n\nSee also: calcPhaseFilter, FAtransform\n\n\n\n\n\n","title":"Springsteel.Fourier.calcInvPhaseFilter"},{"category":"function","location":"fourier/#Springsteel.Fourier.apply_phasefilter_forward!","page":"Fourier","text":"apply_phasefilter_forward!(b, fft_scratch, pf) -> b\n\nIn-place forward apply: read fft_scratch (length pf.yDim, FFTW R2HC layout), write b (length pf.bDim). Combines the azimuthal phase-shift rotation (if populated) and the spectral cutoff.\n\nMathematically equivalent to mul!(b, old_phasefilter', fft_scratch) but with O(kmax) work instead of O(yDim · bDim).\n\n\n\n\n\n","title":"Springsteel.Fourier.apply_phasefilter_forward!"},{"category":"function","location":"fourier/#Springsteel.Fourier.apply_phasefilter_inverse!","page":"Fourier","text":"apply_phasefilter_inverse!(fft_scratch, b, pf) -> fft_scratch\n\nIn-place inverse apply: read b (length pf.bDim), write fft_scratch (length pf.yDim, FFTW R2HC layout ready for the inverse FFT). Zero-fills the tail beyond kmax.\n\nMathematically equivalent to mul!(a, old_invphasefilter', b).\n\n\n\n\n\n","title":"Springsteel.Fourier.apply_phasefilter_inverse!"},{"category":"function","location":"fourier/#Springsteel.Fourier.FBtransform","page":"Fourier","text":"FBtransform(fp::FourierParameters, fftPlan, phasefilter::PhaseFilter, uMish::Vector{Float64}) -> Vector{Float64}\nFBtransform(ring::Fourier1D, uMish::Vector{Float64}) -> Vector{Float64}\nFBtransform!(ring::Fourier1D)\n\nCompute the forward Fourier transform (physical → filtered B-coefficients).\n\nb = textPhaseFilterbigl(textFFT(u)  Nbigr)\n\nThe FFTW.R2HC (real-to-halfcomplex) output is divided by yDim so that an amplitude-1 sinusoid in physical space maps to amplitude-1 in b. The PhaseFilter then applies the azimuthal phase-shift rotation and spectral cutoff in O(kmax) work, producing a compact bDim-length coefficient vector.\n\nVariants\n\nFBtransform(fp, fftPlan, phasefilter, uMish) — allocates; requires an explicit plan and PhaseFilter (useful when constructing per-radius rings with varying parameters)\nFBtransform(ring, uMish) — allocates using the ring's cached plan and PhaseFilter\nFBtransform!(ring) — in-place; reads ring.uMish, writes ring.b\n\nSee also: PhaseFilter, FAtransform, FItransform\n\n\n\n\n\n","title":"Springsteel.Fourier.FBtransform"},{"category":"function","location":"fourier/#Springsteel.Fourier.FAtransform","page":"Fourier","text":"FAtransform(fp::FourierParameters, phasefilter::PhaseFilter, b::Vector{Float64}) -> Vector{Float64}\nFAtransform(ring::Fourier1D, b::AbstractVector) -> Vector{Float64}\nFAtransform!(ring::Fourier1D)\n\nCompute the FA transform: convert filtered B-coefficients to zero-padded A-coefficients ready for the inverse FFT, via apply_phasefilter_inverse!.\n\nUndoes the azimuthal phase shift introduced in FBtransform and pads the bDim-length b back to a yDim-length a (zeros for wavenumbers above kmax).\n\nVariants\n\nFAtransform(fp, phasefilter, b) — allocates; takes an explicit PhaseFilter\nFAtransform(ring, b) — allocates using the ring's cached PhaseFilter\nFAtransform!(ring) — in-place; reads ring.b, writes ring.a\n\nSee also: PhaseFilter, FBtransform, FItransform\n\n\n\n\n\n","title":"Springsteel.Fourier.FAtransform"},{"category":"function","location":"fourier/#Springsteel.Fourier.FItransform","page":"Fourier","text":"FItransform(fp::FourierParameters, ifftPlan, a::Vector{Float64}) -> Vector{Float64}\nFItransform!(ring::Fourier1D)\n\nCompute the inverse Fourier transform (A-coefficients → physical values).\n\nu = textIFFT(a)\n\nPerforms a FFTW.HC2R (halfcomplex-to-real) inverse FFT on the yDim-length padded A-coefficient vector. No additional scaling is required; the forward normalisation applied in FBtransform ensures that the round-trip FB → FA → FI recovers the original physical values.\n\nVariants\n\nFItransform(fp, ifftPlan, a) — allocates and returns uMish\nFItransform!(ring) — in-place; reads ring.a, writes ring.uMish\n\nReturns\n\nVector{Float64}: Physical field values of length yDim (allocating variant)\n\nSee also: FBtransform, FAtransform\n\n\n\n\n\n","title":"Springsteel.Fourier.FItransform"},{"category":"function","location":"fourier/#Springsteel.Fourier.FIxcoefficients","page":"Fourier","text":"FIxcoefficients(fp::FourierParameters, a::Vector{Float64}, ax::Vector{real}) -> Vector{Float64}\n\nConvert A-coefficients to first-derivative A-coefficients in-place.\n\nExploits the analytic Fourier differentiation rule: multiplying the cosine coefficient of wavenumber k by +k and the sine coefficient by −k gives the derivative. In the FFTW.HC2R halfcomplex layout:\n\nax[k+1]      = −k · a[yDim-k+1]  (cosine component of ∂/∂θ)\nax[yDim-k+1] = +k · a[k+1]       (sine component of ∂/∂θ)\n\nThe constant term (k = 0) is not modified (derivative of a constant is zero). Mutates ax in-place and also returns it.\n\nArguments\n\nfp::FourierParameters: Ring parameters (provides kmax and yDim)\na::Vector{Float64}: Source A-coefficient vector of length yDim\nax::Vector{real}: Pre-allocated output buffer of length yDim; overwritten on return\n\nReturns\n\nax: Derivative A-coefficient vector\n\nSee also: FIxtransform, FIxxtransform\n\n\n\n\n\n","title":"Springsteel.Fourier.FIxcoefficients"},{"category":"function","location":"fourier/#Springsteel.Fourier.FIxtransform","page":"Fourier","text":"FIxtransform(fp::FourierParameters, ifftPlan, a::Vector{Float64}, ax::Vector{Float64}) -> Vector{Float64}\nFIxtransform(ring::Fourier1D) -> Vector{Float64}\nFIxtransform(ring::Fourier1D, ux::AbstractVector) -> AbstractVector\n\nEvaluate the first azimuthal derivative partial u  partial theta in physical space.\n\nApplies FIxcoefficients to convert A-coefficients to derivative coefficients, then performs an inverse FFT.\n\nVariants\n\nFIxtransform(fp, ifftPlan, a, ax) — allocates; ax is overwritten as a side effect\nFIxtransform(ring) — allocates a new output vector; uses ring.a and ring.ax\nFIxtransform(ring, ux) — writes into pre-allocated ux; uses ring.a and ring.ax\n\nReturns\n\nVector{Float64}: First-derivative values of length yDim\n\nSee also: FIxcoefficients, FIxxtransform, FItransform\n\n\n\n\n\n","title":"Springsteel.Fourier.FIxtransform"},{"category":"function","location":"fourier/#Springsteel.Fourier.FIxxtransform","page":"Fourier","text":"FIxxtransform(ring::Fourier1D) -> Vector{Float64}\n\nEvaluate the second azimuthal derivative partial^2 u  partial theta^2 in physical space.\n\nApplies FIxcoefficients twice: first to obtain first-derivative coefficients a', then again on a' to obtain a''. A copy of the intermediate buffer is used to avoid overwriting the ax working buffer mid-computation. Performs one inverse FFT on the final a'' coefficients.\n\nReturns\n\nVector{Float64}: Second-derivative values of length yDim\n\nSee also: FIxtransform, FItransform\n\n\n\n\n\n","title":"Springsteel.Fourier.FIxxtransform"},{"category":"function","location":"fourier/#Springsteel.Fourier.FIIntcoefficients","page":"Fourier","text":"FIIntcoefficients(fp::FourierParameters, a::Vector{Float64}, aInt::Vector{Float64}, C0::Float64 = 0.0) -> Vector{Float64}\n\nConvert A-coefficients to indefinite-integral A-coefficients in-place.\n\nExploits the analytic Fourier integration rule:\n\naInt[k+1]      =  a[yDim-k+1] / k   (integral of k-th cosine → sine/k)\naInt[yDim-k+1] = -a[k+1] / k        (integral of k-th sine → -cosine/k)\naInt[1]         = C0                 (constant of integration)\n\nMutates aInt in-place and also returns it.\n\nArguments\n\nfp::FourierParameters: Ring parameters (provides kmax and yDim)\na::Vector{Float64}: Source A-coefficient vector of length yDim\naInt::Vector{Float64}: Pre-allocated output buffer of length yDim; overwritten on return\nC0::Float64: Constant of integration (default 0.0); sets the mean of the integral\n\nReturns\n\naInt: Integral A-coefficient vector\n\nSee also: FIInttransform\n\n\n\n\n\n","title":"Springsteel.Fourier.FIIntcoefficients"},{"category":"function","location":"fourier/#Springsteel.Fourier.FIInttransform","page":"Fourier","text":"FIInttransform(fp::FourierParameters, ifftPlan, a::Vector{Float64}, aInt::Vector{Float64}, C0::Float64 = 0.0) -> Vector{Float64}\nFIInttransform(ring::Fourier1D, C0::Float64 = 0.0) -> Vector{Float64}\n\nCompute the indefinite integral of the Fourier expansion in physical space.\n\nApplies FIIntcoefficients to convert A-coefficients to integral coefficients, then performs an inverse FFT. The constant of integration C0 sets the mean value of the resulting field.\n\nVariants\n\nFIInttransform(fp, ifftPlan, a, aInt, C0) — allocates; aInt is overwritten\nFIInttransform(ring, C0) — convenience form using the cached ring internals\n\nArguments\n\nC0::Float64: Constant of integration (default 0.0)\n\nReturns\n\nVector{Float64}: Integral field values of length yDim\n\nSee also: FIIntcoefficients, FIxtransform\n\n\n\n\n\n","title":"Springsteel.Fourier.FIInttransform"},{"category":"function","location":"fourier/#Springsteel.Fourier.dft_matrix","page":"Fourier","text":"dft_matrix(ring::Fourier1D) -> Matrix{Float64}\n\nBuild the (N times M) DFT evaluation matrix for the Fourier basis so that dft_matrix(ring) * ring.b recovers the physical field values at the mish points.\n\nColumns follow the B-coefficient layout produced by FBtransform: column 1 is the constant (wavenumber 0), columns k+1 correspond to cos(ktheta) and columns bDim-k+1 to sin(ktheta) for k = 1:kmax. The FFTW normalisation convention means cosine columns carry a factor of 2 and sine columns a factor of -2.\n\nSee also: dft_1st_derivative, dft_2nd_derivative\n\n\n\n\n\n","title":"Springsteel.Fourier.dft_matrix"},{"category":"function","location":"fourier/#Springsteel.Fourier.dft_1st_derivative","page":"Fourier","text":"dft_1st_derivative(ring::Fourier1D) -> Matrix{Float64}\n\nBuild the (N times M) first-derivative DFT matrix for the Fourier basis so that dft_1st_derivative(ring) * ring.b gives partial u  partial theta at the mish points.\n\nUses the analytic rules ddthetacos(ktheta) = -ksin(ktheta) and ddthetasin(ktheta) = kcos(ktheta) combined with the FFTW normalisation factors.\n\nSee also: dft_matrix, dft_2nd_derivative\n\n\n\n\n\n","title":"Springsteel.Fourier.dft_1st_derivative"},{"category":"function","location":"fourier/#Springsteel.Fourier.dft_2nd_derivative","page":"Fourier","text":"dft_2nd_derivative(ring::Fourier1D) -> Matrix{Float64}\n\nBuild the (N times M) second-derivative DFT matrix for the Fourier basis so that dft_2nd_derivative(ring) * ring.b gives partial^2 u  partial theta^2 at the mish points.\n\nUses the analytic rules d^2dtheta^2cos(ktheta) = -k^2cos(ktheta) and d^2dtheta^2sin(ktheta) = -k^2sin(ktheta) combined with the FFTW normalisation factors.\n\nSee also: dft_matrix, dft_1st_derivative\n\n\n\n\n\n","title":"Springsteel.Fourier.dft_2nd_derivative"},{"category":"function","location":"fourier/#Springsteel.Fourier.Btransform","page":"Fourier","text":"Generic B-transform wrapper for Fourier1D (allocating). Delegates to FBtransform.\n\n\n\n\n\n","title":"Springsteel.Fourier.Btransform"},{"category":"function","location":"fourier/#Springsteel.Fourier.Btransform!","page":"Fourier","text":"Generic in-place B-transform wrapper for Fourier1D. Delegates to FBtransform!.\n\n\n\n\n\n","title":"Springsteel.Fourier.Btransform!"},{"category":"function","location":"fourier/#Springsteel.Fourier.Atransform","page":"Fourier","text":"Generic A-transform wrapper for Fourier1D (allocating). Delegates to FAtransform.\n\n\n\n\n\n","title":"Springsteel.Fourier.Atransform"},{"category":"function","location":"fourier/#Springsteel.Fourier.Atransform!","page":"Fourier","text":"Generic in-place A-transform wrapper for Fourier1D. Delegates to FAtransform!.\n\n\n\n\n\n","title":"Springsteel.Fourier.Atransform!"},{"category":"function","location":"fourier/#Springsteel.Fourier.Itransform","page":"Fourier","text":"Generic I-transform wrapper for Fourier1D (in-place output). Performs inverse FFT from ring.a and writes into u.\n\n\n\n\n\n","title":"Springsteel.Fourier.Itransform"},{"category":"function","location":"fourier/#Springsteel.Fourier.Itransform!","page":"Fourier","text":"Generic in-place I-transform wrapper for Fourier1D. Delegates to FItransform!.\n\n\n\n\n\n","title":"Springsteel.Fourier.Itransform!"},{"category":"function","location":"fourier/#Springsteel.Fourier.Ixtransform","page":"Fourier","text":"Generic Ix-transform wrapper for Fourier1D (allocating). Delegates to FIxtransform.\n\n\n\n\n\nGeneric Ix-transform wrapper for Fourier1D (in-place output). Delegates to FIxtransform.\n\n\n\n\n\n","title":"Springsteel.Fourier.Ixtransform"},{"category":"function","location":"fourier/#Springsteel.Fourier.Ixxtransform","page":"Fourier","text":"Generic Ixx-transform wrapper for Fourier1D (allocating). Delegates to FIxxtransform.\n\n\n\n\n\n","title":"Springsteel.Fourier.Ixxtransform"},{"category":"function","location":"fourier/#Springsteel.Fourier.IInttransform","page":"Fourier","text":"Generic indefinite-integral transform wrapper for Fourier1D. Delegates to FIInttransform.\n\n\n\n\n\n","title":"Springsteel.Fourier.IInttransform"},{"category":"section","location":"contributing/#Contributing","page":"Contributing","text":"This page is for people working on the Springsteel source — whether that's adding a feature, fixing a bug, writing a test, or extending the solver framework. It covers the development workflow, test conventions, architecture invariants you should know before touching the transform pipeline, and the v1.1+ roadmap.\n\nUser-facing documentation (how to use the package) lives in the other pages; this one is about the inside.","title":"Contributing"},{"category":"section","location":"contributing/#Getting-started","page":"Contributing","text":"Clone the repo and install in development mode:\n\ngit clone https://github.com/csu-tropical/Springsteel.jl\ncd Springsteel.jl\njulia --project -e 'using Pkg; Pkg.instantiate()'\n\nRun the full test suite once to verify your environment:\n\njulia --project -e 'using Pkg; Pkg.test()'\n\nThe suite takes ~90 seconds cold, ~60 seconds warm, and passes 12,000+ tests. If anything fails on a clean clone, that's a bug worth filing.","title":"Getting started"},{"category":"section","location":"contributing/#Running-tests","page":"Contributing","text":"","title":"Running tests"},{"category":"section","location":"contributing/#Full-suite","page":"Contributing","text":"julia --project -e 'using Pkg; Pkg.test()'","title":"Full suite"},{"category":"section","location":"contributing/#A-single-test-group","page":"Contributing","text":"During development, run only the group you're touching for fast iteration. Use the TEST_GROUP environment variable:\n\njulia --project -e 'ENV[\"TEST_GROUP\"]=\"solver\"; using Pkg; Pkg.test()'\n\nAvailable groups (from test/runtests.jl):\n\nbasis            banded_cholesky  grids            transforms\ntiling           io               solver           mubar\ninterpolation    filtering        r3x              bc\nmultipatch       tile_multipatch  operator_algebra solver_problem\nbasis_cache      relocation\n\nEach group corresponds to one file under test/. The top-level runtests.jl is just a dispatcher.","title":"A single test group"},{"category":"section","location":"contributing/#Bench-scripts","page":"Contributing","text":"Performance regressions are checked manually (not in CI — runner variance is too flaky). See bench/README.md for the workflow:\n\n# Snapshot baseline from main before your change\ngit checkout main\njulia --project bench/bench_grids.jl bench/baseline/grids_pre.csv\njulia --project bench/bench_interpolation.jl bench/baseline/interp_pre.csv\n\n# Make your changes, re-run, diff\ngit checkout my-feature\njulia --project bench/bench_grids.jl bench/baseline/grids_post.csv\ndiff bench/baseline/grids_pre.csv bench/baseline/grids_post.csv\n\nA >20% slowdown on any row or any new allocation is a red flag — investigate before merging.","title":"Bench scripts"},{"category":"section","location":"contributing/#Test-conventions","page":"Contributing","text":"Test files don't include using Springsteel or using Test — those are loaded once in runtests.jl so that individual files can be included directly without re-importing. Each test file has one top-level @testset wrapping the sub-testsets:\n\n@testset \"Solver Tests\" begin\n    @testset \"Operator Matrix Assembly\" begin\n        # ...\n    end\n    @testset \"SpringsteelProblem\" begin\n        # ...\n    end\nend\n\nWhen adding a new test file:\n\nCreate test/<name>.jl with the above shape.\nAdd one dispatch line to test/runtests.jl: TEST_GROUP in (\"all\", \"<name>\") && include(\"<name>.jl\").\nPrefer deterministic inputs over randomness in assertions. If you do use RNG, print the seed.\nUse analytic solutions as ground truth. For BVPs, tests that check sol.physical ≈ u_analytic to 1e-3..1e-8 (depending on grid size) are the gold standard.\nDon't mock the basis modules or the solver — run them end-to-end. Mocks of numerical code drift from reality; the suite's value comes from its fidelity.","title":"Test conventions"},{"category":"section","location":"contributing/#Docstring-conventions","page":"Contributing","text":"Every exported symbol gets a triple-quoted docstring directly above its definition. Keep them substantive — describe the why, the inputs and outputs, the edge cases, and any non-obvious invariants.\n\n\"\"\"\n    assemble_from_equation(grid, var; d0=nothing, d_ii=nothing, ...)\n\nAssemble a multi-dimensional operator matrix via Kronecker products.\n...\n\"\"\"\nfunction assemble_from_equation(grid, var; kwargs...)\n    # ...\nend\n\nRules:\n\nLead with the signature line; Documenter.jl uses it for cross-refs.\nUse # Fields, # Arguments, # Returns, # Example sections as needed.\nCross-link with [otherfunction](@ref) — the Documenter build checks these and warns on breakage.\nDo not reference deprecated or removed APIs. The audit from commit 1871ef9 onwards caught sol.info, prob.operator, and field.grid in stale docstrings; don't let them creep back.\nInternal helpers (_foo_bar) don't need docstrings unless the logic is non-obvious.","title":"Docstring conventions"},{"category":"section","location":"contributing/#Architecture-invariants","page":"Contributing","text":"These are the things you should know before touching the transform pipeline, the tiling code, or the multipatch machinery. They're intentionally non-uniform across grid types — resist the urge to \"clean them up\" until you understand why they differ.","title":"Architecture invariants"},{"category":"section","location":"contributing/#Spectral-array-layouts","page":"Contributing","text":"Every grid stores spectral data in grid.spectral[row, var]. The row ordering scheme differs by grid type and must not be unified. RL and RLZ arrays look similar but use different wavenumber offset formulas; see TRAP-1 below.\n\n1D grids\n\nGrid Layout Total rows\nR [1:b_iDim, v] b_iDim\nZ [1:b_iDim, v] b_iDim\nL [1:b_iDim, v] b_iDim\n\n2D grids\n\nGrid Layout Total rows\nRR / LL row-major: r1 = (l-1)*b_iDim + 1 b_jDim * b_iDim\nRZ z-major: r1 = (z-1)*b_iDim + 1 b_kDim * b_iDim\nRL wavenumber-interleaved flat: `[k=0 k=1 real\nSL same as RL b_iDim * (1 + 2*kDim)\n\n3D grids\n\nGrid Layout Total rows\nRRR / ZZZ / LLZ z-major, then row-major L: idx = (z-1)*b_jDim*b_iDim + (l-1)*b_iDim + 1 b_kDim * b_jDim * b_iDim\nRLZ z-major, then wavenumber-interleaved: r1 = (z-1)*b_iDim*(1+kDim_wn*2) + 1 b_kDim * b_iDim * (1 + kDim_wn*2)\nSLZ same as RLZ b_kDim * b_iDim * (1 + kDim_wn*2)","title":"Spectral array layouts"},{"category":"section","location":"contributing/#TRAP-1:-RL-vs-RLZ-wavenumber-offset-formulas-differ","page":"Contributing","text":"Files: transforms_cylindrical.jl, transforms_spherical.jl.\n\nRL  spectralTransform:  p = k*2     for k ≥ 1\nRLZ spectralTransform:  p = (k-1)*2 for k ≥ 1\n\nBoth are correct — they refer to different quantities.\n\nRL / SL (2D): The spectral array is a single flat interleaved vector. The block index of the real part of wavenumber k is 2k (1-based), hence p = k*2.\nRLZ / SLZ (3D): The array is z-major. Within each z-level the k=0 block comes first; the offset to wavenumber k ≥ 1 measured from just after the k=0 block is (k-1)*2 blocks, hence p = (k-1)*2.\n\nThe two implementations were written independently. Do not harmonise these formulas without rewriting one transform end-to-end.","title":"TRAP-1: RL vs RLZ wavenumber offset formulas differ"},{"category":"section","location":"contributing/#TRAP-2:-Halo-boundary-arithmetic-uses-4,-not-3","page":"Contributing","text":"File: tiling.jl (calcHaloMap).\n\nThe tile overlap width is exactly 3 B-spline coefficients. With b_iDim = num_cells + 3, the last owned coefficient of a tile is at row b_iDim - 4 (1-based), and the 3 shared halo coefficients are at b_iDim-3 : b_iDim-1.\n\ntileShare = b_iDim - 4 is correct. Changing to -3 gives 2-wide overlap (misses one shared coefficient); -5 gives 4-wide (double- counts one). Either breaks the assembled spline.","title":"TRAP-2: Halo boundary arithmetic uses -4, not -3"},{"category":"section","location":"contributing/#TRAP-3:-Tiled-cylindrical-transform-reuses-patchSplines[1,-v]","page":"Contributing","text":"File: tiling.jl.\n\nIn the tiled cylindrical transform, patchSplines[1, v] is reused for wavenumber 0 and overwritten for each subsequent wavenumber. Only one wavenumber is live in the buffer at any moment. Don't allocate separate Spline1D objects per wavenumber — the reuse pattern is necessary for memory efficiency on large tiled cylindrical grids.","title":"TRAP-3: Tiled cylindrical transform reuses patchSplines[1, v]"},{"category":"section","location":"contributing/#TRAP-4:-Physical-index-formulas-are-grid-specific","page":"Contributing","text":"Files: transforms_cylindrical.jl, transforms_cartesian.jl.\n\nRL and RLZ grids have variable-length Fourier rings (ring size 4 + 4*(r + patchOffsetL) for RL, sin(θ)-based for SL) and cannot use a simple product formula.\n\nGrid Physical index formula Notes\nR physical[r, v, d] direct\nRL running: l1 = l2+1; l2 = l1+3+4*ri variable rings\nRR flat = (r-1)*jDim + l regular\nRZ flat = (r-1)*kDim + z regular\nRLZ running: accumulate lpoints * b_kDim per ring variable rings\nRRR flat = (r-1)*jDim*kDim + (l-1)*kDim + z regular\n\nDon't generalise these.","title":"TRAP-4: Physical index formulas are grid-specific"},{"category":"section","location":"contributing/#RACE-1:-SharedArray-halo-summation-is-non-atomic","page":"Contributing","text":"File: tiling.jl (sumSharedSpectral).\n\nshared_spectral[idx] += tile_spectral[local_idx] is a read-modify-write on a SharedArray. Two workers operating on adjacent tiles whose halo zones overlap will race.\n\nCurrent callers serialise halo-zone writes. If you extend distributed execution, either preserve serialisation or add explicit barriers / atomic operations.","title":"RACE-1: SharedArray halo summation is non-atomic"},{"category":"section","location":"contributing/#RACE-2:-tileTransform!-per-variable-threading-invariant","page":"Contributing","text":"File: tiling.jl (tileTransform!).\n\nThe Threads.@threads for v in 1:length(pp.vars) loop is safe only because:\n\nibasis.data[1, v] objects are fully independent per variable.\nphysical[:, v, :] slices are non-overlapping.\nNo shared mutable state is read or written across v.\n\nAny refactor that shares a basis across variables or changes the physical array layout introduces a race.","title":"RACE-2: tileTransform! per-variable threading invariant"},{"category":"section","location":"contributing/#RACE-3:-splineTransform!-needs-fully-populated-SharedArray","page":"Contributing","text":"File: tiling.jl (splineTransform!).\n\nRead-only access to a SharedArray is safe only after all workers have finished writing. There's no internal barrier — the caller must guarantee completeness before parallel reads.","title":"RACE-3: splineTransform! needs fully-populated SharedArray"},{"category":"section","location":"contributing/#Critical-constants","page":"Contributing","text":"Derived from the Ooyama (2002) B-spline formulation. Any deviation produces wrong results.\n\nName Value Meaning\nmubar 3 Gauss-Legendre quadrature points per B-spline cell\niDim num_cells * mubar Total physical gridpoints in the i-direction\nb_iDim num_cells + 3 Number of spectral B-spline coefficients\nlpoints 4 + 4*ri Fourier ring size at radius index ri (RL / RLZ)\nhalo_width 3 Shared B-spline coefficients in tile overlap\nmin_tile_cells 3 Minimum B-spline cells per tile (≥ 9 gridpoints)","title":"Critical constants"},{"category":"section","location":"contributing/#Transform-ordering-reference","page":"Contributing","text":"Physical → spectral (forward)\n\nGrid Stage order\nR SBtransform\nRL FBtransform per ring → SBtransform per wavenumber\nRR SBtransform in j (per i) → SBtransform in i (per j)\nRZ CBtransform per column → SBtransform per z-coefficient\nRLZ CBtransform per column → FBtransform per ring×z → SBtransform per wavenumber×z\nRRR SBtransform k → SBtransform j → SBtransform i\nSL FBtransform per sin(θ) ring → SBtransform per wavenumber\nSLZ CBtransform per column → FBtransform per ring×z → SBtransform per wavenumber×z\n\nInverse gridTransform! runs the stages in reverse.","title":"Transform ordering reference"},{"category":"section","location":"contributing/#Fourier-indexing-invariants","page":"Contributing","text":"Half-complex convention: imaginary part of wavenumber k is stored at bDim - k + 1 within the Fourier coefficient array.\nRing azimuthal offset: offset = 0.5 * dl * (ri - 1) (phase staggering between rings; handled by calcPhaseFilter).\nPer-ring kmax: for cylindrical grids kmax_ring = r + patchOffsetL; for spherical grids kmax_ring = ring.params.kmax.","title":"Fourier indexing invariants"},{"category":"section","location":"contributing/#Tiling","page":"Contributing","text":"Tiling is restricted to the i-dimension only. B-splines are the only basis with compact local support; Fourier and Chebyshev bases are global and cannot be partitioned with finite halos.\n\nGeometry Tiling\nCartesian + Spline i Full 1-D tiling\nCylindrical (RL, RLZ) 1-D in i; all wavenumber blocks present per tile\nSpherical (SL, SLZ) 1-D in i (same as cylindrical)\nNo Spline i No tiling (single-tile metadata only)\n\nMulti-D tiling for pure-spline Cartesian grids is implemented in tiling.jl but not yet exercised in production.","title":"Tiling"},{"category":"section","location":"contributing/#Performance-notes","page":"Contributing","text":"Transform and interpolation allocation expectations after the v1.0 refactor (bench/bench_grids.jl, bench/bench_interpolation.jl):\n\nEvery multi-D grid (R, RR, RZ, RL, SL, RRR, RLZ, SLZ) has zero allocations per call for gridTransform! and spectralTransform! at steady state.\nSAtransform! pipelines scale O(Mdim) via the structured GammaBC operator.\ncreateGrid is sub-millisecond warm via the basis template cache (cold construction of a CONUS-10km RR grid was 1.9 s; warm is 1.8 ms — a 1000× speedup).\nupdate_interface! is zero alloc after the PatchInterface{P,S} parameterisation.\nRepeated solve! on a prebuilt SpringsteelProblem is 1.6 μs per call at N=25 Chebyshev Poisson, allocating only the inevitable ~400 B for the backsolve return.\nInterpolation hot paths use cached γ-folded ahat stripes (_AHAT_CACHE) and per-grid scratch registries (_ScratchInterpRL / _ScratchInterpRLZ). RL unstructured evaluation dropped 24.8 MB → 494 kB; RLZ dropped 27 MB → 75 kB.\n\nDon't regress these. bench/bench_grids.jl runs in ~30 s and bench/bench_interpolation.jl in ~2 min. Any non-zero allocation on a transform or >20 % slowdown is a bug.","title":"Performance notes"},{"category":"section","location":"contributing/#v1.1-roadmap","page":"Contributing","text":"Items deferred from the v1.0 perf and feature arc, parked as candidates for future releases. All of these are scoped and have no API-breaking implications in v1.0.\n\nMulti-threading outer transform loops — requires per-thread scratch restructure in _solve_* paths and the grid transforms. Tier 1 in the old perf doc. Biggest potential win for large grids.\nFully unify MultiPatchGrid dispatch — parameterise MultiPatchGrid on its patch / interface types to eliminate the residual ~2 kB multiGridTransform! allocation from heterogeneous vector dispatch. Small but annoying.\nNon-cylindrical grid relocation — relocate_grid! currently supports RL and RLZ only. Cartesian and spherical relocations are feasible via the same evaluate_unstructured machinery but would need a coordinate mapping layer and new boundary strategies.\nPrecompute SAtransform application matrix — a per-wavenumber cache; only a win at Mdim < 20.\nCombined SBtransform + SAtransform matrix — O(nc²) memory growth, so only usable for small-to-moderate grids; big win when applicable.\nCache-friendly physical / spectral layout — high risk refactor that reorders the physical array for better cache locality on wide multi-var grids. Would need a clear downstream workload driving it.\nAdditional grid types are \"available but untested\": Z, ZZ, ZZZ, L, LL, LLZ. They build and transform, but coverage is thin compared to the RR / RL / RLZ production paths. Filling in test suites for these is a good v1.1 contribution that won't break any existing API. Part of this work: cross-validate the unified solver on Chebyshev (Z) grids against the legacy Chebyshev.bvp reference implementation — once that parity test is solid, the Chebyshev.bvp / bvp_modified_basis / bvp_basis reference functions become candidates for deprecation in v1.2+.\nImplement the missing Chebyshev BC types: the constants Chebyshev.R1T2, Chebyshev.R2T10, Chebyshev.R2T20, and Chebyshev.R3 are exported for symmetry with CubicBSpline but are not wired into _bc_type / calcGammaBC; using them throws DomainError at grid construction. The transform-level path needs either a coefficient-correction formulation (following the Wang et al. 1993 / Ooyama 2002 style) or a row-replacement approach in the solver framework. Inhomogeneous Dirichlet / Neumann values (non-zero DirichletBC(v) / NeumannBC(v)) on Chebyshev dimensions are a related missing piece — the current Wang et al. path assumes homogeneous conditions.","title":"v1.1+ roadmap"},{"category":"section","location":"contributing/#See-also","page":"Contributing","text":"bench/README.md — manual perf workflow\nCLAUDE.md in the repo root — project-specific agent instructions\nagent_files/ (gitignored) — planning docs and scratch notes","title":"See also"},{"category":"section","location":"chebyshev/#Chebyshev","page":"Chebyshev","text":"The Chebyshev submodule implements the 1D Chebyshev spectral transform used for the vertical direction in cylindrical- and spherical-coordinate grids (RZ_Grid, RLZ_Grid, SLZ_Grid) and for purely Chebyshev-based grids (Z, ZZ, ZZZ). It uses FFTW.jl DCT-I transforms (FFTW.REDFT00) with a pre-measured plan for maximum throughput. Grid points are placed at Chebyshev–Gauss–Lobatto (CGL) nodes, giving spectral accuracy for smooth functions on a bounded interval.","title":"Chebyshev"},{"category":"section","location":"chebyshev/#Mathematical-Overview","page":"Chebyshev","text":"Chebyshev polynomials of the first kind, T_k(x), are defined on x in -1 1 by the identity T_k(costheta) = cos(ktheta). They form an orthogonal basis under the weight (1-x^2)^-12, and any sufficiently smooth function u(x) can be expanded as\n\nu(x) = sum_k=0^infty c_k T_k(x)\n\ntruncated to a finite sum of N+1 terms in practice. For smooth u the coefficients c_k decay exponentially in k — this is where the \"spectral accuracy\" phrase comes from, and why Chebyshev is the standard choice for non-periodic bounded-interval problems.","title":"Mathematical Overview"},{"category":"section","location":"chebyshev/#Chebyshev–Gauss–Lobatto-collocation","page":"Chebyshev","text":"Springsteel evaluates u at the Chebyshev–Gauss–Lobatto (CGL) nodes x_j = cos(pi j  N) for j = 0 ldots N. At these nodes the polynomial interpolation problem is diagonalised by the discrete cosine transform: if u_j = u(x_j) then the coefficients are\n\nc_k = fracp_kN\n          sum_j=0^N p_j u_j cosleft(fracpi j kNright)\nqquad p_j = begincases tfrac12  j = 0 text or  j = N  1  textotherwiseendcases\n\nThis is exactly a DCT-I (FFTW.REDFT00), so the forward and inverse transforms run at FFT speed. The inverse is the same transform with a factor-of-2 rescaling baked in.","title":"Chebyshev–Gauss–Lobatto collocation"},{"category":"section","location":"chebyshev/#Derivatives","page":"Chebyshev","text":"Differentiating a Chebyshev expansion is exact and local in coefficient space: the recurrence T_k+1(x) = 2(k+1) T_k(x) + tfrack+1k-1T_k-1(x) gives a triangular linear map from c_k to c_k. Springsteel applies this in-place for first and second derivatives and also exposes matrix representations via Chebyshev.dct_matrix, Chebyshev.dct_1st_derivative, and Chebyshev.dct_2nd_derivative for operator assembly in the Solver Framework.","title":"Derivatives"},{"category":"section","location":"chebyshev/#Boundary-conditions","page":"Chebyshev","text":"Springsteel currently implements three Chebyshev BC types at the transform level, all homogeneous:\n\nConstant Condition Method\nChebyshev.R0 natural (no constraint) zero gammaBC vector — no-op\nChebyshev.R1T0 Dirichlet u(z_0) = 0 rank-1 coefficient correction vector\nChebyshev.R1T1 Neumann u(z_0) = 0 full N times N correction matrix via the Wang et al. (1993) global coefficient method\n\nEach BC combination (BCB/BCT pair) produces a gammaBC correction stored on the Chebyshev.Chebyshev1D object at construction time. The correction is then applied during the forward transform by CAtransform as\n\na = b_textfill + textttgammaBC^top b_textfill\n\nwhere b_textfill is the raw DCT output padded to the full mode count. For R0/R0 the correction is the zero vector and no work is done; for single-ended Dirichlet or Neumann the correction is rank-1; for Neumann at one or both ends Springsteel falls back to the full matrix form. This is a coefficient-modification scheme — the boundary rows of the spectral-to-physical evaluation are not rewritten, unlike the solver-framework row-replacement path described below.\n\nThe Wang et al. (1993) reference:\n\nWang, H., Lacroix, S., & Labrosse, G. (1993). A Chebyshev collocation method for the Stokes problem with application to the driven cavity. Journal of Computational Physics, 106(1), 7–24. doi:10.1006/jcph.1993.1133\n\nnote: Supported BCs only\nOnly R0, R1T0, and R1T1 currently work at the transform level. The Ooyama higher-rank constants Chebyshev.R1T2, Chebyshev.R2T10, Chebyshev.R2T20, and Chebyshev.R3 are exported for naming symmetry with CubicBSpline but throw DomainError at grid construction. Inhomogeneous (non-zero) Dirichlet / Neumann values are also not yet implemented. See the Contributing roadmap.","title":"Boundary conditions"},{"category":"section","location":"chebyshev/#Solver-level-BC-enforcement-(separate-path)","page":"Chebyshev","text":"When the Solver Framework assembles an explicit operator matrix mathbfL for a SpringsteelProblem, it layers a second BC mechanism on top of the transform-level gammaBC: the boundary rows of mathbfL are replaced with evaluation rows (for Dirichlet) or first-derivative rows (for Neumann) at the two boundary CGL nodes x_0 and x_N, and the RHS at those rows is set to the BC value. This row-replacement path handles the same R1T0 / R1T1 conditions as the transform path — it does not add support for R1T2 or the higher ranks. Both paths run for any problem built through the solver framework; for base transforms (spectralTransform! / gridTransform!) only the transform-level path applies.","title":"Solver-level BC enforcement (separate path)"},{"category":"section","location":"chebyshev/#References","page":"Chebyshev","text":"Wikipedia: Chebyshev polynomials\nWikipedia: Discrete cosine transform\nTrefethen, Spectral Methods in MATLAB, SIAM, 2000 — Chapters 6–8 for the collocation viewpoint and derivative matrices\nBoyd, Chebyshev and Fourier Spectral Methods, 2nd ed., Dover, 2001 — comprehensive reference including boundary condition handling","title":"References"},{"category":"section","location":"chebyshev/#Boundary-Condition-Constants","page":"Chebyshev","text":"","title":"Boundary Condition Constants"},{"category":"section","location":"chebyshev/#Parameter-and-Data-Structures","page":"Chebyshev","text":"","title":"Parameter and Data Structures"},{"category":"section","location":"chebyshev/#Internal-Setup-Functions","page":"Chebyshev","text":"","title":"Internal Setup Functions"},{"category":"section","location":"chebyshev/#Transform-Functions","page":"Chebyshev","text":"","title":"Transform Functions"},{"category":"section","location":"chebyshev/#Generic-Wrappers","page":"Chebyshev","text":"No-prefix wrappers that delegate to the C-prefixed functions above, enabling basis-type-agnostic code.","title":"Generic Wrappers"},{"category":"section","location":"chebyshev/#DCT-Matrix-Utilities","page":"Chebyshev","text":"","title":"DCT Matrix Utilities"},{"category":"section","location":"chebyshev/#BVP-Solver-Utilities","page":"Chebyshev","text":"","title":"BVP Solver Utilities"},{"category":"constant","location":"chebyshev/#Springsteel.Chebyshev.R0","page":"Chebyshev","text":"Rank-0 boundary condition (Ooyama 2002, Eq. 3.2a): no constraint at the boundary. All Chebyshev coefficients remain free. Use when no physical condition needs to be enforced at the domain edge.\n\n\n\n\n\n","title":"Springsteel.Chebyshev.R0"},{"category":"constant","location":"chebyshev/#Springsteel.Chebyshev.R1T0","page":"Chebyshev","text":"Rank-1, type-0 boundary condition (Ooyama 2002, Eq. 3.2b): zero field value at the boundary, u(z_0) = 0 (homogeneous Dirichlet). Implemented via the global affine (bottom-anchor) method.\n\n\n\n\n\n","title":"Springsteel.Chebyshev.R1T0"},{"category":"constant","location":"chebyshev/#Springsteel.Chebyshev.R1T1","page":"Chebyshev","text":"Rank-1, type-1 boundary condition (Ooyama 2002, Eq. 3.2c): zero first derivative at the boundary, u(z_0) = 0 (homogeneous Neumann). Implemented via the Wang et al. (1993) global-coefficient method.\n\n\n\n\n\n","title":"Springsteel.Chebyshev.R1T1"},{"category":"constant","location":"chebyshev/#Springsteel.Chebyshev.R1T2","page":"Chebyshev","text":"Rank-1, type-2 boundary condition (Ooyama 2002, Eq. 3.2d): zero second derivative at the boundary, u(z_0) = 0.\n\nwarning: Not yet implemented\nExported for symmetry with the CubicBSpline BC naming, but the Chebyshev transform does not currently handle this case — using it as BCB or BCT throws a DomainError at grid construction. Tracked in the v1.1+ roadmap. Use R0, R1T0, or R1T1 instead.\n\n\n\n\n\n","title":"Springsteel.Chebyshev.R1T2"},{"category":"constant","location":"chebyshev/#Springsteel.Chebyshev.R2T10","page":"Chebyshev","text":"Rank-2, type-1-0 boundary condition (Ooyama 2002, Eq. 3.2f): zero value and zero first derivative at the boundary, u(z_0) = u(z_0) = 0. Appropriate for a symmetrically reflecting boundary.\n\nwarning: Not yet implemented\nExported for naming symmetry only — throws DomainError at grid construction. See R1T2 for the full story.\n\n\n\n\n\n","title":"Springsteel.Chebyshev.R2T10"},{"category":"constant","location":"chebyshev/#Springsteel.Chebyshev.R2T20","page":"Chebyshev","text":"Rank-2, type-2-0 boundary condition (Ooyama 2002, Eq. 3.2g): zero value and zero second derivative at the boundary, u(z_0) = u(z_0) = 0. Forces the field to be antisymmetric with respect to the boundary.\n\nwarning: Not yet implemented\nExported for naming symmetry only — throws DomainError at grid construction. See R1T2 for the full story.\n\n\n\n\n\n","title":"Springsteel.Chebyshev.R2T20"},{"category":"constant","location":"chebyshev/#Springsteel.Chebyshev.R3","page":"Chebyshev","text":"Rank-3 boundary condition (Ooyama 2002, Eq. 3.2h): zero value, zero first derivative, and zero second derivative at the boundary, u(z_0) = u(z_0) = u(z_0) = 0. Eliminates all three border coefficients. Relevant for domain-nesting inhomogeneous BCs (future work).\n\nwarning: Not yet implemented\nExported for naming symmetry only — throws DomainError at grid construction. See R1T2 for the full story.\n\n\n\n\n\n","title":"Springsteel.Chebyshev.R3"},{"category":"type","location":"chebyshev/#Springsteel.Chebyshev.ChebyshevParameters","page":"Chebyshev","text":"ChebyshevParameters\n\nImmutable parameter struct (using @kwdef) for a 1D Chebyshev column basis.\n\nThe physical domain [zmin, zmax] is sampled at zDim Chebyshev–Gauss–Lobatto (CGL) points clustered near both endpoints: z_j = cosleft(frac(j-1)piN-1right) mapped to the physical range. This gives spectral accuracy for smooth functions.\n\nFields\n\nzmin::Float64: Bottom of the vertical domain (e.g. 0 m or a pressure level).\nzmax::Float64: Top of the vertical domain.\nzDim::Int64: Number of CGL nodes (physical grid points). Both endpoints are included.\nbDim::Int64: Number of retained Chebyshev modes. When bDim < zDim a sharp truncation matrix is applied; when bDim == zDim an exponential Eresman damping filter exp(−36*(k/N)^36) is used instead.\nBCB::Dict: Bottom boundary condition. Currently supported values are R0 (natural), R1T0 (homogeneous Dirichlet), and R1T1 (homogeneous Neumann via the Wang et al. 1993 global coefficient method). The higher-rank constants R1T2, R2T10, R2T20, and R3 are exported for naming symmetry with CubicBSpline but are not yet implemented and throw DomainError at grid construction.\nBCT::Dict: Top boundary condition dict (same options as BCB).\n\nNotes\n\nUnlike CubicBSpline.SplineParameters, there are no auto-computed fields; all six must be set explicitly.\nzDim must be ≥ 2 (a DCT-I requires at least two endpoints).\n\nExample\n\ncp = Chebyshev.ChebyshevParameters(\n    zmin = 0.0, zmax = 10000.0,\n    zDim = 25, bDim = 25,\n    BCB = Chebyshev.R0, BCT = Chebyshev.R0\n)\n\nSee also: Chebyshev1D\n\n\n\n\n\n","title":"Springsteel.Chebyshev.ChebyshevParameters"},{"category":"type","location":"chebyshev/#Springsteel.Chebyshev.Chebyshev1D","page":"Chebyshev","text":"Chebyshev1D\n\nOne-dimensional Chebyshev column object. Construct via Chebyshev1D(cp::ChebyshevParameters).\n\nFields\n\nparams::ChebyshevParameters: Column configuration (domain bounds, grid size, BCs).\nmishPoints::Vector{Float64}: CGL points ordered from zmin up to zmax (increasing z), because scale = -0.5*(zmax-zmin) flips the cosine so that cos(0) maps to zmin (bottom) and cos(π) maps to zmax (top). Points are clustered near both endpoints.\ngammaBC::Array{Float64}: Spectral BC correction factor. A zero Vector for R0/R0 (no correction); a rank-1 Vector or full N×N Matrix for Neumann/Robin combinations. The polymorphic Array type annotation covers both cases.\nfftPlan::FFTW.r2rFFTWPlan: Pre-measured FFTW.REDFT00 (DCT-I) plan. DCT-I is symmetric around both endpoints, consistent with the CGL grid that includes both zmin and zmax. Do not serialise; FFTW plans are not portable.\nfilter::Matrix{Float64}: Size (bDim, zDim). Either a sharp truncation matrix (when bDim < zDim) or an exponential spectral damping matrix (when bDim == zDim).\nuMish::Vector{Float64}: Physical field values at the zDim CGL points (mutable buffer).\nb::Vector{Float64}: Filtered Chebyshev B-coefficients of length bDim.\na::Vector{Float64}: BC-corrected A-coefficients of length zDim, ready for inverse DCT.\nax::Vector{Float64}: Working buffer of length zDim for derivative/integral coefficients.\n\nNotes\n\nConstructing Chebyshev1D calls FFTW.plan_r2r with FFTW.PATIENT; first construction is slow but subsequent transforms are fast.\nThe struct is not thread-safe if uMish, b, a, or ax are mutated concurrently.\n\nSee also: ChebyshevParameters, CBtransform, CAtransform, CItransform\n\n\n\n\n\n","title":"Springsteel.Chebyshev.Chebyshev1D"},{"category":"function","location":"chebyshev/#Springsteel.Chebyshev.calcMishPoints","page":"Chebyshev","text":"calcMishPoints(cp::ChebyshevParameters) -> Vector{Float64}\n\nCompute the Chebyshev–Gauss–Lobatto (CGL) physical grid points for the column.\n\nPoints are placed at z_j = cosleft(frac(j-1)piN-1right) cdot s + o where s = -0.5*(zmax−zmin) (negative scale) and o = 0.5*(zmin+zmax). The negative scale maps cos(0) = 1 → zmin and cos(π) = −1 → zmax, so the returned vector runs from zmin up to zmax (increasing z, bottom to top). Points are clustered near both endpoints according to the CGL distribution.\n\nReturns\n\nVector{Float64}: CGL points of length zDim, from zmin to zmax\n\nSee also: Chebyshev1D\n\n\n\n\n\n","title":"Springsteel.Chebyshev.calcMishPoints"},{"category":"function","location":"chebyshev/#Springsteel.Chebyshev.calcFilterMatrix","page":"Chebyshev","text":"calcFilterMatrix(cp::ChebyshevParameters) -> Matrix{Float64}\n\nBuild the spectral filter/truncation matrix of size (bDim, zDim) (when bDim < zDim) or (zDim, zDim) with exponential damping (when bDim == zDim).\n\nTwo branches:\n\nSharp truncation (bDim < zDim): returns the leading bDim × zDim rows of the identity, zeroing all Chebyshev modes above index bDim.\nSpectral damping (bDim == zDim): returns a diagonal matrix with entries textfilter_ii = expleft(-36 left(fraciNright)^36right), which leaves low modes essentially unchanged while strongly attenuating modes near the Nyquist limit (the Eresman/Boyd exponential filter).\n\nReturns\n\nMatrix{Float64}: Filter matrix applied after the forward DCT in CBtransform!\n\nSee also: CBtransform, Chebyshev1D\n\n\n\n\n\n","title":"Springsteel.Chebyshev.calcFilterMatrix"},{"category":"function","location":"chebyshev/#Springsteel.Chebyshev.calcGammaBC","page":"Chebyshev","text":"calcGammaBC(cp::ChebyshevParameters) -> Union{Vector{Float64}, Matrix{Float64}}\n\nCompute the spectral BC correction term for CAtransform.\n\nFollowing the Ooyama (2002) nomenclature, returns:\n\nA zero Vector{Float64} for R0/R0 (Dirichlet at both ends — no correction needed)\nA rank-1 Vector{Float64} or rank-1 Matrix{Float64} for simple Neumann (R1T0/R0, R0/R1T0)\nA full N×N Matrix{Float64} for Neumann BCs using the Wang et al. (1993) global coefficient method (R1T1, R0/R1T1, R1T1/R0, R1T1/R1T1)\nCombined matrices for mixed-BC combinations\n\nNeumann BCs via the global method reference:\n\nWang, H., Lacroix, S., & Labrosse, G. (1993). A Chebyshev collocation method for the Navier–Stokes equations with application to double-diffusive convection. JCP 109, 133. https://doi.org/10.1006/jcph.1993.1133\n\nReturns\n\nArray{Float64}: Vector or matrix depending on BC combination\n\nNotes\n\nEach supported BC combination is handled by a separate branch. Unsupported combinations throw a DomainError.\n\nSee also: CAtransform, Chebyshev1D\n\n\n\n\n\n","title":"Springsteel.Chebyshev.calcGammaBC"},{"category":"function","location":"chebyshev/#Springsteel.Chebyshev.calcGammaBCalt","page":"Chebyshev","text":"calcGammaBCalt(cp::ChebyshevParameters)\n\nAlternative BC matrix construction via DCT matrix inversion.\n\nwarning: Deprecated\nThis function works only for Dirichlet BCs and is retained for reference. It does not handle Neumann BCs. Use calcGammaBC instead.\n\n\n\n\n\n","title":"Springsteel.Chebyshev.calcGammaBCalt"},{"category":"function","location":"chebyshev/#Springsteel.Chebyshev.CBtransform","page":"Chebyshev","text":"CBtransform(cp::ChebyshevParameters, fftPlan, uMish::Vector{Float64}) -> Vector{Float64}\nCBtransform(column::Chebyshev1D, uMish::Vector{Float64}) -> Vector{Float64}\nCBtransform!(column::Chebyshev1D)\n\nCompute the forward Chebyshev transform (physical → filtered B-coefficients).\n\nb = textfilter cdot fractextDCT(u)2(N-1)\n\nThe FFTW.REDFT00 (DCT-I) output is divided by 2*(zDim−1) so that the discrete orthogonality relation is satisfied and amplitude-1 polynomials map to amplitude-1 coefficients. The filter matrix then truncates to bDim modes or applies exponential damping per calcFilterMatrix.\n\nVariants\n\nCBtransform(cp, fftPlan, uMish) — allocates; requires explicit plan (useful for constructing columns with varying parameters without a full Chebyshev1D object)\nCBtransform(column, uMish) — allocates using column's cached filter and plan\nCBtransform!(column) — in-place; reads column.uMish, writes column.b\n\nArguments\n\ncp::ChebyshevParameters: Column parameters\nfftPlan: Pre-measured FFTW.r2rFFTWPlan for the DCT-I forward transform\nuMish::Vector{Float64}: Physical field values at the zDim CGL points\n\nReturns\n\nVector{Float64}: Filtered B-coefficient vector of length bDim (allocating variants)\n\nSee also: CAtransform, CItransform\n\n\n\n\n\n","title":"Springsteel.Chebyshev.CBtransform"},{"category":"function","location":"chebyshev/#Springsteel.Chebyshev.CAtransform","page":"Chebyshev","text":"CAtransform(cp::ChebyshevParameters, gammaBC, b::Vector{Float64}) -> Vector{Float64}\nCAtransform(column::Chebyshev1D, b::AbstractVector) -> Vector{Float64}\nCAtransform!(column::Chebyshev1D)\n\nApply boundary conditions and zero-pad B-coefficients to produce A-coefficients.\n\na = b_textfill + textttgammaBC^top cdot b_textfill\n\nwhere b_textfill is b zero-padded from bDim to zDim. The additive structure means that gammaBC = 0 (the R0/R0 Dirichlet case) applies no correction. Julia's * operator handles both vector and matrix gammaBC transparently.\n\nThe resulting a has length zDim and is ready for the inverse DCT in CItransform.\n\nVariants\n\nCAtransform(cp, gammaBC, b) — allocates; requires explicit gammaBC (low-level form)\nCAtransform(column, b) — allocates using the column's cached gammaBC\nCAtransform!(column) — in-place; reads column.b, writes column.a\n\nReturns\n\nVector{Float64}: BC-corrected A-coefficients of length zDim (allocating variants)\n\nSee also: CBtransform, CItransform\n\n\n\n\n\n","title":"Springsteel.Chebyshev.CAtransform"},{"category":"function","location":"chebyshev/#Springsteel.Chebyshev.CItransform","page":"Chebyshev","text":"CItransform(cp::ChebyshevParameters, fftPlan, a::Vector{Float64}) -> Vector{Float64}\nCItransform!(column::Chebyshev1D)\n\nCompute the inverse Chebyshev transform (A-coefficients → physical values).\n\nu = textDCT-I(a)\n\nPerforms the FFTW.REDFT00 (DCT-I) inverse transform on the zDim-length A-coefficient vector. No additional scaling is needed; the forward normalisation applied in CBtransform ensures that the round-trip CB → CA → CI recovers the original physical values.\n\nVariants\n\nCItransform(cp, fftPlan, a) — allocates and returns uMish\nCItransform!(column) — in-place; reads column.a, writes column.uMish\n\nReturns\n\nVector{Float64}: Physical field values of length zDim (allocating variant)\n\nSee also: CBtransform, CAtransform\n\n\n\n\n\n","title":"Springsteel.Chebyshev.CItransform"},{"category":"function","location":"chebyshev/#Springsteel.Chebyshev.CIxcoefficients","page":"Chebyshev","text":"CIxcoefficients(cp::ChebyshevParameters, a::Vector{Float64}, ax::Vector{Float64}) -> Vector{Float64}\n\nConvert A-coefficients to first-derivative A-coefficients via the Chebyshev recurrence.\n\nThe recurrence proceeds backwards from k = zDim to k = 2: a_k-1 = 2(k-1) a_k + a_k+1 then scaled by -1  (05 cdot (z_max - z_min)) to map from the reference domain -11 to the physical domain.\n\nMutates ax in-place and also returns it.\n\nArguments\n\ncp::ChebyshevParameters: Column parameters\na::Vector{Float64}: Source A-coefficient vector of length zDim\nax::Vector{Float64}: Pre-allocated output buffer of length zDim; overwritten on return\n\nReturns\n\nax: Derivative A-coefficient vector\n\nSee also: CIxtransform, CIxxtransform\n\n\n\n\n\n","title":"Springsteel.Chebyshev.CIxcoefficients"},{"category":"function","location":"chebyshev/#Springsteel.Chebyshev.CIxtransform","page":"Chebyshev","text":"CIxtransform(cp::ChebyshevParameters, fftPlan, a::Vector{Float64}, ax::Vector{Float64}) -> Vector{Float64}\nCIxtransform(column::Chebyshev1D) -> Vector{Float64}\nCIxtransform(column::Chebyshev1D, ux::AbstractVector) -> AbstractVector\n\nEvaluate the first vertical derivative partial u  partial z in physical space.\n\nApplies CIxcoefficients to convert A-coefficients to derivative coefficients, then performs an inverse DCT-I.\n\nVariants\n\nCIxtransform(cp, fftPlan, a, ax) — allocates a new output vector; ax is overwritten\nCIxtransform(column) — allocates; convenience form using the cached column internals\nCIxtransform(column, ux) — writes into pre-allocated ux buffer and returns it\n\nReturns\n\nVector{Float64}: First-derivative values of length zDim\n\nSee also: CIxcoefficients, CIxxtransform, CItransform\n\n\n\n\n\n","title":"Springsteel.Chebyshev.CIxtransform"},{"category":"function","location":"chebyshev/#Springsteel.Chebyshev.CIxxtransform","page":"Chebyshev","text":"CIxxtransform(column::Chebyshev1D) -> Vector{Float64}\n\nEvaluate the second vertical derivative partial^2 u  partial z^2 in physical space.\n\nApplies CIxcoefficients twice: first on column.a to obtain first-derivative coefficients a', then again on a' to obtain a''. A copy of the intermediate a' buffer is used to avoid overwriting the ax working buffer mid-computation. Performs one inverse DCT-I on the final a'' coefficients.\n\nReturns\n\nVector{Float64}: Second-derivative values of length zDim\n\nSee also: CIxtransform, CItransform\n\n\n\n\n\n","title":"Springsteel.Chebyshev.CIxxtransform"},{"category":"function","location":"chebyshev/#Springsteel.Chebyshev.CIIntcoefficients","page":"Chebyshev","text":"CIIntcoefficients(cp::ChebyshevParameters, a::Vector{Float64}, C0::Float64 = 0.0) -> Vector{Float64}\n\nConvert A-coefficients to indefinite-integral A-coefficients via the Chebyshev recurrence.\n\na^textint_k = fracDelta z4 cdot fraca_k-1 - a_k+1k-1 quad k = 2 ldots N-1 a^textint_N = fraca_N-1N-1 a^textint_1 = C_0 - 2sum_k=2^N a^textint_k\n\nwhere Delta z = z_max - z_min and C0 is the optional constant of integration (value of the integral at the reference point).\n\nArguments\n\ncp::ChebyshevParameters: Column parameters\na::Vector{Float64}: Source A-coefficient vector of length zDim\nC0::Float64: Constant of integration (default 0.0)\n\nReturns\n\nVector{Float64}: Integral A-coefficient vector (allocates)\n\nSee also: CIInttransform\n\n\n\n\n\n","title":"Springsteel.Chebyshev.CIIntcoefficients"},{"category":"function","location":"chebyshev/#Springsteel.Chebyshev.CIInttransform","page":"Chebyshev","text":"CIInttransform(cp::ChebyshevParameters, fftPlan, a::Vector{Float64}, C0::Float64 = 0.0) -> Vector{Float64}\nCIInttransform(column::Chebyshev1D, C0::Float64 = 0.0) -> Vector{Float64}\n\nCompute the indefinite integral of the Chebyshev expansion in physical space.\n\nApplies CIIntcoefficients to convert A-coefficients to integral coefficients, then performs an inverse DCT-I. The constant of integration C0 sets the reference value.\n\nVariants\n\nCIInttransform(cp, fftPlan, a, C0) — allocates; requires explicit plan\nCIInttransform(column, C0) — convenience form using the cached column internals\n\nArguments\n\nC0::Float64: Constant of integration (default 0.0)\n\nReturns\n\nVector{Float64}: Integral field values of length zDim\n\nSee also: CIIntcoefficients, CIxtransform\n\n\n\n\n\n","title":"Springsteel.Chebyshev.CIInttransform"},{"category":"function","location":"chebyshev/#Springsteel.Chebyshev.Btransform","page":"Chebyshev","text":"Generic B-transform wrapper for Chebyshev1D (allocating). Delegates to CBtransform.\n\n\n\n\n\n","title":"Springsteel.Chebyshev.Btransform"},{"category":"function","location":"chebyshev/#Springsteel.Chebyshev.Btransform!","page":"Chebyshev","text":"Generic in-place B-transform wrapper for Chebyshev1D. Delegates to CBtransform!.\n\n\n\n\n\n","title":"Springsteel.Chebyshev.Btransform!"},{"category":"function","location":"chebyshev/#Springsteel.Chebyshev.Atransform","page":"Chebyshev","text":"Generic A-transform wrapper for Chebyshev1D (allocating). Delegates to CAtransform.\n\n\n\n\n\n","title":"Springsteel.Chebyshev.Atransform"},{"category":"function","location":"chebyshev/#Springsteel.Chebyshev.Atransform!","page":"Chebyshev","text":"Generic in-place A-transform wrapper for Chebyshev1D. Delegates to CAtransform!.\n\n\n\n\n\n","title":"Springsteel.Chebyshev.Atransform!"},{"category":"function","location":"chebyshev/#Springsteel.Chebyshev.Itransform","page":"Chebyshev","text":"Generic I-transform wrapper for Chebyshev1D (in-place output). Performs inverse DCT from column.a and writes into u.\n\n\n\n\n\n","title":"Springsteel.Chebyshev.Itransform"},{"category":"function","location":"chebyshev/#Springsteel.Chebyshev.Itransform!","page":"Chebyshev","text":"Generic in-place I-transform wrapper for Chebyshev1D. Delegates to CItransform!.\n\n\n\n\n\n","title":"Springsteel.Chebyshev.Itransform!"},{"category":"function","location":"chebyshev/#Springsteel.Chebyshev.Ixtransform","page":"Chebyshev","text":"Generic Ix-transform wrapper for Chebyshev1D (allocating). Delegates to CIxtransform.\n\n\n\n\n\nGeneric Ix-transform wrapper for Chebyshev1D (in-place output). Delegates to CIxtransform.\n\n\n\n\n\n","title":"Springsteel.Chebyshev.Ixtransform"},{"category":"function","location":"chebyshev/#Springsteel.Chebyshev.Ixxtransform","page":"Chebyshev","text":"Generic Ixx-transform wrapper for Chebyshev1D (allocating). Delegates to CIxxtransform.\n\n\n\n\n\n","title":"Springsteel.Chebyshev.Ixxtransform"},{"category":"function","location":"chebyshev/#Springsteel.Chebyshev.IInttransform","page":"Chebyshev","text":"Generic indefinite-integral transform wrapper for Chebyshev1D. Delegates to CIInttransform.\n\n\n\n\n\n","title":"Springsteel.Chebyshev.IInttransform"},{"category":"function","location":"chebyshev/#Springsteel.Chebyshev.dct_matrix","page":"Chebyshev","text":"dct_matrix(Nbasis::Int64) -> Matrix{Float64}\n\nBuild the full DCT-I evaluation matrix of size (Nbasis, Nbasis).\n\nEntry [i,j] = 2 cos((j-1) * (i-1)*π/(N-1)) with endpoint columns scaled by 0.5. Useful for debugging transforms and constructing linear solvers directly in spectral space.\n\nSee also: dct_1st_derivative, dct_2nd_derivative\n\n\n\n\n\n","title":"Springsteel.Chebyshev.dct_matrix"},{"category":"function","location":"chebyshev/#Springsteel.Chebyshev.dct_1st_derivative","page":"Chebyshev","text":"dct_1st_derivative(Nbasis::Int64, physical_length::Float64) -> Matrix{Float64}\n\nBuild the first-derivative matrix in DCT-I basis of size (Nbasis, Nbasis).\n\nApplies spectral differentiation analytically: entry [i,j] is the value of dT_ndx at the i-th CGL node, scaled by 1 / (physical_length/4) to convert from the reference domain -11 to the physical domain. Endpoint rows use the limiting formula to avoid a sin(t) = 0 singularity.\n\nSee also: dct_matrix, dct_2nd_derivative\n\n\n\n\n\n","title":"Springsteel.Chebyshev.dct_1st_derivative"},{"category":"function","location":"chebyshev/#Springsteel.Chebyshev.dct_2nd_derivative","page":"Chebyshev","text":"dct_2nd_derivative(Nbasis::Int64, physical_length::Float64) -> Matrix{Float64}\n\nBuild the second-derivative matrix in DCT-I basis of size (Nbasis, Nbasis).\n\nAnalytic second spectral derivative, scaled by 1 / (physical_length^2/8). Endpoint rows use the limiting formula. Useful for BVP solvers and debugging.\n\nSee also: dct_matrix, dct_1st_derivative\n\n\n\n\n\n","title":"Springsteel.Chebyshev.dct_2nd_derivative"},{"category":"function","location":"chebyshev/#Springsteel.Chebyshev.bvp","page":"Chebyshev","text":"bvp(N, u, ux, uxx, f, d0, d1, d2, scale, alpha, beta) -> Vector{Float64}\n\nModified Chebyshev boundary value problem solver (Boyd 2000).\n\nSolves a general second-order BVP d_2 u + d_1 u + d_0 u = f on -11 with Dirichlet boundary conditions u(+1) = alpha, u(-1) = beta. Uses the modified basis functions from bvp_modified_basis to enforce homogeneous BCs automatically.\n\nnote: Reference implementation\nThis is a self-contained 1D Chebyshev BVP solver kept as a reference for cross-validating the v1.0 solver framework on Z grids. New code should use SpringsteelProblem with the operator algebra DSL — see the Solver Framework page. Chebyshev.bvp pre-dates that framework and is restricted to 1D Dirichlet BVPs with the Boyd (2000) modified-basis trick; it does not participate in workspace caching, multi-variable systems, or any of the v1.0 solver features.\n\nSee also: bvp_modified_basis, bvp_basis\n\n\n\n\n\n","title":"Springsteel.Chebyshev.bvp"},{"category":"function","location":"chebyshev/#Springsteel.Chebyshev.bvp_modified_basis","page":"Chebyshev","text":"bvp_modified_basis(x, nbasis, phi, phix, phixx, scale) -> (phi, phix, phixx)\n\nEvaluate the modified Chebyshev basis functions and their derivatives at point x.\n\nModified basis functions satisfy homogeneous Dirichlet BCs at ±1. Handles the singularity at endpoints by using the limiting formulae. Returns in-place the arrays phi (values), phix (first derivatives), phixx (second derivatives).\n\nnote: Reference implementation\nHelper for bvp; same caveats apply — reference code only, not part of the v1.0 solver framework.\n\nSee also: bvp\n\n\n\n\n\n","title":"Springsteel.Chebyshev.bvp_modified_basis"},{"category":"function","location":"chebyshev/#Springsteel.Chebyshev.bvp_basis","page":"Chebyshev","text":"bvp_basis(x, nbasis, phi, phix, phixx) -> (phi, phix, phixx)\n\nEvaluate the standard (unmodified) Chebyshev basis functions and their derivatives at x.\n\nUsed for debugging and verification; does not enforce BCs. See bvp_modified_basis for the BC-enforcing variant.\n\nnote: Reference implementation\nDebugging variant of bvp_modified_basis; reference code only, not part of the v1.0 solver framework.\n\n\n\n\n\n","title":"Springsteel.Chebyshev.bvp_basis"},{"category":"section","location":"filtering/#Spectral-Filtering","page":"Spectral Filtering","text":"Springsteel supports post-transform spectral coefficient filtering for Fourier and Chebyshev bases. Filters are specified per-variable on the grid parameters and applied automatically at the end of every spectralTransform! — your physical arrays and derivative slots see the filtered field, not the raw projection.\n\nFiltering is useful for\n\nremoving specific azimuthal or vertical modes you know aren't physical (e.g. wave 0 for a mean-subtracted analysis, wave 1 for a translation-removed vortex);\nsmoothing fields before differentiating, to avoid aliasing or small-scale noise contaminating the derivatives;\nband-passing in wavenumber space to isolate scales of interest.\n\nFiltering is a no-op for pure B-spline grids (R, RR, RRR) — B-spline smoothness is controlled at the basis level via l_q rather than at the spectral-coefficient level. Filter specs on pure-spline grids are accepted but silently ignored.","title":"Spectral Filtering"},{"category":"section","location":"filtering/#Filter-types","page":"Spectral Filtering","text":"Two filter types cover most needs. Both are subtypes of AbstractFilter.","title":"Filter types"},{"category":"section","location":"filtering/#SpectralFilter-—-wavenumber-domain","page":"Spectral Filtering","text":"SpectralFilter is a parameterised combination of low-pass, high-pass, and notch cuts with an optional tapered transition at the passband edges. Fields:\n\nField Default Meaning\nlow_pass -1 Zero modes k > low_pass; -1 disables\nhigh_pass 0 Zero modes k < high_pass; 0 disables\nnotch Int[] Hard-cutoff list of specific modes to zero\nwindow :boxcar Taper window: :boxcar, :hann, :lanczos, :exponential\ntaper_width 0 Mode count over which window transitions 1 → 0\n\nWith taper_width = 0 every window reduces to boxcar (sharp cutoff). For smooth transitions, increase taper_width — typical values are 3–10 modes. Hann is the most forgiving taper; Lanczos has a slightly sharper transition; exponential is near-Gaussian.\n\n# Hard low-pass at k=50, plus remove wave 1\nSpectralFilter(low_pass=50, notch=[1])\n\n# Band-pass 5 ≤ k ≤ 20 with Lanczos taper over 3 modes on each edge\nSpectralFilter(high_pass=5, low_pass=20, window=:lanczos, taper_width=3)\n\n# Remove just the mean (k=0)\nSpectralFilter(notch=[0])","title":"SpectralFilter — wavenumber-domain"},{"category":"section","location":"filtering/#GaussianFilter-—-smooth-envelope","page":"Spectral Filtering","text":"GaussianFilter multiplies each spectral coefficient by exp(-(k/σ)^(2p)), where σ is a width parameter (modes at k = σ are attenuated to e⁻¹ for order = 1) and p is the order. Higher orders approach a boxcar with a smooth transition — useful when you want aggressive smoothing without ringing:\n\n# Standard Gaussian with width σ = 20\nGaussianFilter(sigma=20.0)\n\n# Super-Gaussian order 3 — sharper cutoff, still smooth\nGaussianFilter(sigma=20.0, order=3)","title":"GaussianFilter — smooth envelope"},{"category":"section","location":"filtering/#Attaching-filters-to-a-grid","page":"Spectral Filtering","text":"Filters live in two per-variable dicts on SpringsteelGridParameters: fourier_filter for Fourier dimensions and chebyshev_filter for Chebyshev dimensions. The keys match the grid's variable names, with an optional \"default\" fallback:\n\ngp = SpringsteelGridParameters(\n    geometry = \"RLZ\",\n    iMin = 0.0, iMax = 100.0, num_cells = 30,\n    kMin = 0.0, kMax = 10.0, kDim = 32,\n    vars = Dict(\"u\" => 1, \"v\" => 2),\n    BCL  = Dict(\"default\" => NaturalBC()),\n    BCR  = Dict(\"default\" => NaturalBC()),\n    BCB  = Dict(\"default\" => DirichletBC()),\n    BCT  = Dict(\"default\" => DirichletBC()),\n\n    # Remove wave 0 from u (zonal mean), band-pass v (azimuthal waves 1–5)\n    fourier_filter = Dict(\n        \"u\" => SpectralFilter(notch=[0]),\n        \"v\" => SpectralFilter(high_pass=1, low_pass=5),\n    ),\n    # Smooth the vertical structure of both\n    chebyshev_filter = Dict(\n        \"default\" => GaussianFilter(sigma=20.0, order=2),\n    ),\n)\ngrid = createGrid(gp)\n\nVariables without an entry fall back to \"default\", and variables with no filter at all are left unfiltered.","title":"Attaching filters to a grid"},{"category":"section","location":"filtering/#Application","page":"Spectral Filtering","text":"applyFilter! runs automatically at the end of every spectralTransform! — you don't normally call it directly. If you modify grid.spectral by hand and want to re-apply the configured filters, call applyFilter!(grid) manually. The dispatch is geometry-aware; it walks the per-variable filter dicts and applies the right window / envelope to each spectral block.","title":"Application"},{"category":"section","location":"filtering/#Anti-aliasing-vs-filtering","page":"Spectral Filtering","text":"Filtering via fourier_filter / chebyshev_filter is orthogonal to max_wavenumber. The max_wavenumber setting controls the physical grid resolution's Nyquist cap and acts as a hard anti-aliasing cutoff at the transform level — that's how many modes the grid can resolve at all. Filters act on the modes that survive anti-aliasing, selecting which of those to keep, weight, or suppress.\n\nUse max_wavenumber to cap the resolved spectrum for performance or accuracy reasons; use a filter to shape the spectrum within that cap for physical reasons.","title":"Anti-aliasing vs filtering"},{"category":"section","location":"filtering/#See-also","page":"Spectral Filtering","text":"SpringsteelGrid — where filter dicts live in grid parameters\nFourier / Chebyshev — underlying basis modules\nSolver Framework — filtering interacts with BC handling; the solver sees the filtered operator","title":"See also"},{"category":"type","location":"filtering/#Springsteel.AbstractFilter","page":"Spectral Filtering","text":"AbstractFilter\n\nAbstract supertype for all spectral filter types. Subtypes define how spectral coefficients are weighted (kept, zeroed, or tapered) after the forward transform.\n\nSee also: SpectralFilter, GaussianFilter, applyFilter!\n\n\n\n\n\n","title":"Springsteel.AbstractFilter"},{"category":"type","location":"filtering/#Springsteel.SpectralFilter","page":"Spectral Filtering","text":"SpectralFilter <: AbstractFilter\n\nWavenumber-domain filter with optional windowed transitions. Applies to Fourier wavenumbers or Chebyshev mode numbers.\n\nFields\n\nlow_pass::Int = -1: Zero modes k > low_pass. Set to -1 to disable.\nhigh_pass::Int = 0: Zero modes k < high_pass. Set to 0 to disable.\nnotch::Vector{Int} = Int[]: Specific modes to zero (hard cutoff, ignores window).\nwindow::Symbol = :boxcar: Window function for taper at cutoff edges. Options: :boxcar, :hann, :lanczos, :exponential.\ntaper_width::Int = 0: Number of modes over which the taper transitions from 1 to 0 at each cutoff edge. With taper_width=0, all windows reduce to boxcar.\n\nExamples\n\n# Remove wave 1 and everything above wave 50\nSpectralFilter(low_pass=50, notch=[1])\n\n# Band pass: keep only wavenumbers 5–20 with Lanczos taper over 3 modes\nSpectralFilter(high_pass=5, low_pass=20, window=:lanczos, taper_width=3)\n\n# Remove just the mean (wave 0)\nSpectralFilter(notch=[0])\n\nSee also: GaussianFilter, AbstractFilter, applyFilter!\n\n\n\n\n\n","title":"Springsteel.SpectralFilter"},{"category":"type","location":"filtering/#Springsteel.GaussianFilter","page":"Spectral Filtering","text":"GaussianFilter <: AbstractFilter\n\nGaussian envelope filter that multiplies spectral coefficients by exp(-(k/σ)^(2p)) where k is the wavenumber/mode number, σ is the width parameter, and p is the order.\n\nFields\n\nsigma::Float64: Width parameter in wavenumber/mode space. Modes at k = sigma are attenuated to exp(-1) for order=1.\norder::Int = 1: Filter order. Higher orders give a sharper transition. order=1 is a standard Gaussian. The exponent is 2*order.\n\nExamples\n\n# Standard Gaussian with σ = 20\nGaussianFilter(sigma=20.0)\n\n# Sharper Gaussian (super-Gaussian) with order 3\nGaussianFilter(sigma=20.0, order=3)\n\nSee also: SpectralFilter, AbstractFilter, applyFilter!\n\n\n\n\n\n","title":"Springsteel.GaussianFilter"},{"category":"function","location":"filtering/#Springsteel.applyFilter!","page":"Spectral Filtering","text":"applyFilter!(grid::SpringsteelGrid) -> Nothing\n\nApply spectral filters to the grid's spectral coefficient array in-place.\n\nReads filter specifications from grid.params.fourier_filter and grid.params.chebyshev_filter (both Dict{String, AbstractFilter} keyed by variable name, with optional \"default\" fallback key).\n\nFor Fourier dimensions, the filter weight is applied to each wavenumber's real and imaginary coefficient blocks. For Chebyshev dimensions, the filter weight is applied to each polynomial mode's coefficient block.\n\nThis function is called automatically at the end of spectralTransform! when filter Dicts are non-empty. It can also be called manually.\n\nDoes nothing for grids with only CubicBSpline dimensions (R, RR, RRR).\n\nExamples\n\ngp = SpringsteelGridParameters(\n    geometry = \"RL\", num_cells = 10,\n    iMin = 0.0, iMax = 100.0,\n    vars = Dict(\"u\" => 1),\n    BCL = Dict(\"u\" => CubicBSpline.R0),\n    BCR = Dict(\"u\" => CubicBSpline.R0),\n    fourier_filter = Dict(\"u\" => SpectralFilter(low_pass=5, notch=[1])))\ngrid = createGrid(gp)\n# ... populate physical, then:\nspectralTransform!(grid)  # automatically calls applyFilter!\n\nSee also: SpectralFilter, GaussianFilter, spectralTransform!\n\n\n\n\n\n","title":"Springsteel.applyFilter!"},{"category":"section","location":"cubicbspline/#CubicBSpline","page":"CubicBSpline","text":"The CubicBSpline submodule implements the Ooyama (2002) cubic B-spline spectral transform method, which provides compact-support basis functions with a built-in sixth-order low-pass filter. Cubic B-splines are a good general choice for an underlying basis function due to their computational efficiency and flexible boundary conditions. They are semi-spectral since the basis functions are not orthogonal, and are a class of finite element bases. Another advantage of this basis is the ability to decompose the domain into tiles for shared memory parallelization and/or multiple patches for domain nesting or distributed memory parallelization.\n\nReference: Ooyama, K. V., 2002: The cubic-spline transform method: Basic definitions and tests in a 1D single domain. Mon. Wea. Rev., 130, 2392–2415.\n\n","title":"CubicBSpline"},{"category":"section","location":"cubicbspline/#Mathematical-Overview","page":"CubicBSpline","text":"","title":"Mathematical Overview"},{"category":"section","location":"cubicbspline/#Basis-Functions","page":"CubicBSpline","text":"A domain x_0 x_0 is divided into m equal cells of width Delta x. The B-spline basis function centred on node x_n is\n\nvarphi_n(x) = Fleft(fracx - x_nDelta xright) qquad\nF(xi) = begincases\n  tfrac16(1-xi)^2bigl4 - (2-3xi)(1+3xi)bigr  0 le xi le 1 \n  tfrac16(2-xi)^3  1 le xi le 2 \n  0  xi ge 2\nendcases\n\nEach basis function spans four cells and has second-order continuity (C^2). The expansion of a field u is\n\nu(x) = sum_n in M a_n varphi_n(x) qquad M = -1 0 1 ldots m+1\n\ngiving m + 3 spectral coefficients a_n for m cells.","title":"Basis Functions"},{"category":"section","location":"cubicbspline/#Physical-Sampling:-The-Mish","page":"CubicBSpline","text":"Field values are sampled at mu = 3 Gauss–Legendre quadrature points per cell (the \"mish\"), using the sqrt35 abscissae with weights w = 518 818 518. The total number of physical points is iDim = num_cells × 3.","title":"Physical Sampling: The Mish"},{"category":"section","location":"cubicbspline/#The-Three-Step-Transform-Pipeline","page":"CubicBSpline","text":"Forward transform (physical → spectral):\n\nThe forward transform is split into two steps following Ooyama (2002), sections 4a–b:\n\nSB transform — projects mish values onto the basis via weighted summation:\nb_n = sum_textcells Delta x sum_mu=1^3 w_mu varphi_n(x_mu) u(x_mu)\nSA transform — solves the variational system with boundary conditions to obtain spectral coefficients:\nhata = biglGamma(P + Q)Gamma^Tbigr^-1Gamma b qquad\na = Gamma^T hata\nwhere P = int varphi_n varphi_ndx is the Gram matrix, Q incorporates the optional derivative-smoothing constraint, and Gamma is the base-folding operator that encodes the current set of boundary conditions.  The system is solved via a pre-factored Cholesky decomposition, so the SA transform is O(m).\n\nInverse transform (spectral → physical):\n\nSI transform — evaluates the B-spline expansion at any set of physical points:\nu(x) = sum_n a_n varphi_n(x) qquad\nu(x) = sum_n a_n varphi_n(x) qquad\nu(x) = sum_n a_n varphi_n(x)\nValue, first derivative, and second derivative are all computed simultaneously and stored in the three derivative slots of the physical array.","title":"The Three-Step Transform Pipeline"},{"category":"section","location":"cubicbspline/#Boundary-Condition-Rank-and-Type-(Ooyama-2002,-Eq.-3.2)","page":"CubicBSpline","text":"A homogeneous BC at boundary x_0 is designated RrTt, where r in 0123 is the rank (number of constraints) and t identifies which derivatives are constrained.  The rank removes r border coefficients from the spectral solve by modifying Gamma.\n\nConstant Rank Condition Physical use\nR0 0 No constraint (free boundary) Open/interior boundaries, domain nesting\nR1T0 1 u(x_0) = 0 Dirichlet; zero-value wall\nR1T1 1 u(x_0) = 0 Neumann; symmetry / reflecting wall\nR1T2 1 u(x_0) = 0 Zero curvature at boundary\nR2T10 2 u = u = 0 Symmetric reflection (value + slope)\nR2T20 2 u = u = 0 Antisymmetric reflection (value + curvature)\nR3 3 u = u = u = 0 Full constraint; precursor to R3X nesting\nPERIODIC — Cyclic domain Azimuthal / periodic domains\n\nThe base-folding coefficients used inside calcGammaBC (Ooyama 2002, Table 1):\n\nBC alpha_1 beta_1\nR1T0 −4 −1\nR1T1 0 1\nR1T2 2 −1\n\nBC alpha_2 beta_2\nR2T10 1 −0.5\nR2T20 −1 0\n\n","title":"Boundary Condition Rank and Type (Ooyama 2002, Eq. 3.2)"},{"category":"section","location":"cubicbspline/#Boundary-Condition-Constants","page":"CubicBSpline","text":"","title":"Boundary Condition Constants"},{"category":"section","location":"cubicbspline/#Parameter-and-Data-Structures","page":"CubicBSpline","text":"","title":"Parameter and Data Structures"},{"category":"section","location":"cubicbspline/#Internal-Basis-and-Setup-Functions","page":"CubicBSpline","text":"","title":"Internal Basis and Setup Functions"},{"category":"section","location":"cubicbspline/#Transform-Functions","page":"CubicBSpline","text":"","title":"Transform Functions"},{"category":"section","location":"cubicbspline/#Matrix-Representations","page":"CubicBSpline","text":"","title":"Matrix Representations"},{"category":"section","location":"cubicbspline/#Generic-Wrappers","page":"CubicBSpline","text":"No-prefix wrappers that delegate to the S-prefixed functions above, enabling basis-type-agnostic code.","title":"Generic Wrappers"},{"category":"constant","location":"cubicbspline/#Springsteel.CubicBSpline.R0","page":"CubicBSpline","text":"Rank-0 boundary condition (Ooyama 2002, Eq. 3.2a): no constraint at the boundary. All border spline coefficients remain free variational parameters. Use when no physical condition needs to be enforced at the domain edge (e.g. open or internal boundaries in nested-domain applications).\n\n\n\n\n\n","title":"Springsteel.CubicBSpline.R0"},{"category":"constant","location":"cubicbspline/#Springsteel.CubicBSpline.R1T0","page":"CubicBSpline","text":"Rank-1, type-0 boundary condition (Ooyama 2002, Eq. 3.2b): zero field value at the boundary, u(x_0) = 0 (homogeneous Dirichlet). Removes one border coefficient via the base-folding operator. Base-folding coefficients (Ooyama 2002, Table 1): alpha_1 = -4, beta_1 = -1.\n\n\n\n\n\n","title":"Springsteel.CubicBSpline.R1T0"},{"category":"constant","location":"cubicbspline/#Springsteel.CubicBSpline.R1T1","page":"CubicBSpline","text":"Rank-1, type-1 boundary condition (Ooyama 2002, Eq. 3.2c): zero first derivative at the boundary, u(x_0) = 0 (homogeneous Neumann). Removes one border coefficient. Sufficient to enforce symmetry at a reflecting boundary. Base-folding coefficients (Table 1): alpha_1 = 0, beta_1 = 1.\n\n\n\n\n\n","title":"Springsteel.CubicBSpline.R1T1"},{"category":"constant","location":"cubicbspline/#Springsteel.CubicBSpline.R1T2","page":"CubicBSpline","text":"Rank-1, type-2 boundary condition (Ooyama 2002, Eq. 3.2d): zero second derivative at the boundary, u(x_0) = 0. Removes one border coefficient. Base-folding coefficients (Table 1): alpha_1 = 2, beta_1 = -1.\n\n\n\n\n\n","title":"Springsteel.CubicBSpline.R1T2"},{"category":"constant","location":"cubicbspline/#Springsteel.CubicBSpline.R2T10","page":"CubicBSpline","text":"Rank-2, type-1-0 boundary condition (Ooyama 2002, Eq. 3.2f): zero value and zero first derivative at the boundary, u(x_0) = u(x_0) = 0. Removes two border coefficients. Appropriate for a symmetrically reflecting boundary. Base-folding coefficients (Table 1): alpha_2 = 1, beta_2 = -05.\n\n\n\n\n\n","title":"Springsteel.CubicBSpline.R2T10"},{"category":"constant","location":"cubicbspline/#Springsteel.CubicBSpline.R2T20","page":"CubicBSpline","text":"Rank-2, type-2-0 boundary condition (Ooyama 2002, Eq. 3.2g): zero value and zero second derivative at the boundary, u(x_0) = u(x_0) = 0. Removes two border coefficients. Forces the field to be antisymmetric with respect to the boundary (the implicit field on the other side has opposite sign). Base-folding coefficients (Table 1): alpha_2 = -1, beta_2 = 0.\n\n\n\n\n\n","title":"Springsteel.CubicBSpline.R2T20"},{"category":"constant","location":"cubicbspline/#Springsteel.CubicBSpline.R3","page":"CubicBSpline","text":"Rank-3 boundary condition (Ooyama 2002, Eq. 3.2h): zero value, zero first derivative, and zero second derivative at the boundary, u(x_0) = u(x_0) = u(x_0) = 0. Eliminates all three border coefficients. The inhomogeneous variant R3X (where the three conditions carry external data from an adjacent domain) is the primary tool for domain nesting and will be implemented in a future release.\n\n\n\n\n\n","title":"Springsteel.CubicBSpline.R3"},{"category":"constant","location":"cubicbspline/#Springsteel.CubicBSpline.PERIODIC","page":"CubicBSpline","text":"Periodic boundary condition (Ooyama 2002, section 3e): couples the left and right domain boundaries to simulate a cyclically continuous space. The border basis functions are folded onto the interior so that the domain has exactly num_cells independent coefficients. This is the only BC option for the Fourier module and is also available for B-splines.\n\n\n\n\n\n","title":"Springsteel.CubicBSpline.PERIODIC"},{"category":"type","location":"cubicbspline/#Springsteel.CubicBSpline.SplineParameters","page":"CubicBSpline","text":"SplineParameters\n\nImmutable parameter struct (using @kwdef) for a 1D cubic B-spline basis.\n\nFields\n\nxmin::Float64: Left boundary of the domain\nxmax::Float64: Right boundary of the domain\nnum_cells::Int64: Number of spline cells\nmubar::Int64: Number of quadrature (mish) points per cell (default 3). This controls the physical grid density within each cell. The spectral resolution is always bDim = num_cells + 3 regardless of mubar.\nquadrature::Symbol: Quadrature type (default :gauss).\n:gauss — Gauss-Legendre quadrature (mubar 1–5). Optimal integration accuracy.\n:regular — equispaced midpoint rule (any mubar ≥ 1). Produces a globally uniform physical grid with spacing DX/mubar.\nl_q::Float64: Filter length scale (default 2.0). Larger values produce smoother spectral fits\nBCL::Dict: Left boundary condition (one of R0, R1T0, R1T1, R1T2, R2T10, R2T20, R3, PERIODIC)\nBCR::Dict: Right boundary condition (same options as BCL)\nDX::Float64: Cell width, computed as (xmax - xmin) / num_cells\nDXrecip::Float64: Reciprocal of DX, precomputed for efficiency\nbDim::Int64: Number of spectral coefficients, computed as num_cells + 3\nmishDim::Int64: Total physical (mish) gridpoints, computed as num_cells * mubar\n\nExample\n\nsp = CubicBSpline.SplineParameters(\n    xmin = 0.0,\n    xmax = 100.0,\n    num_cells = 20,\n    l_q = 2.0,\n    BCL = CubicBSpline.R0,\n    BCR = CubicBSpline.R0\n)\n\n# Regular grid with 2 points per cell\nsp_reg = CubicBSpline.SplineParameters(\n    xmin = 0.0, xmax = 100.0, num_cells = 20,\n    mubar = 2, quadrature = :regular\n)\n\nSee also: Spline1D, _quadrature_rule (internal)\n\n\n\n\n\n","title":"Springsteel.CubicBSpline.SplineParameters"},{"category":"type","location":"cubicbspline/#Springsteel.CubicBSpline.Spline1D","page":"CubicBSpline","text":"Spline1D\n\nOne-dimensional cubic B-spline object.  Construct via Spline1D(sp::SplineParameters).\n\nFields\n\nparams::SplineParameters: Configuration (domain, cells, BCs, filter length, mubar, quadrature)\nquadpoints::Vector{Float64}: Cell-local quadrature positions in [0, 1) (length mubar)\nquadweights::Vector{Float64}: Quadrature weights normalised to sum to 1 (length mubar)\ngammaBC::Matrix{Float64}: Boundary-condition projection matrix (maps interior to full coefficient space)\npq: Full (P + Q) matrix used in the least-squares / variational solve\npqFactor::AbstractSplineFactor: Banded LLᵀ Cholesky factorisation of the open-form (P + Q) matrix for fast solves. A BandedCholesky3 for non-periodic BCs (cubic B-splines have fixed half-bandwidth 3); a DenseSplineFactor for the periodic-BC fallback where Γ wraparound breaks the band structure.\np1: Full (P⁽¹⁾ + Q) matrix for the integral variational solve (same Q as pq, but P uses first-derivative basis varphi_m; see calcP1factor)\np1Factor::AbstractSplineFactor: Banded (or dense, for periodic BCs) LLᵀ Cholesky factorisation of the open-form (P⁽¹⁾ + Q) matrix, used by SIIntcoefficients for fast integration solves\nmishPoints::Vector{Float64}: Physical locations of the mish points (length num_cells * mubar)\nuMish::Vector{Float64}: Physical field values at mish points (mutable working buffer)\nb::Vector{Float64}: B-vector (result of SB transform, inner products ⟨φₘ, u⟩)\na::Vector{Float64}: Spectral coefficient vector (result of SA transform)\nahat::Vector{Float64}: Background coefficient vector for inhomogeneous R3X boundary conditions (length bDim). Initialised to zeros; set via set_ahat_r3x! for grid nesting.\n\nNotes\n\nConstructing Spline1D builds gammaBC and factorises the (P + Q) matrix, which is the computationally expensive step.  Reuse spline objects when possible.\nThe number of quadrature points per cell (mubar) and the quadrature type (:gauss or :regular) are set in SplineParameters.\n\nSee also: SplineParameters, SBtransform, SAtransform, SItransform\n\n\n\n\n\n","title":"Springsteel.CubicBSpline.Spline1D"},{"category":"function","location":"cubicbspline/#Springsteel.CubicBSpline.basis","page":"CubicBSpline","text":"basis(sp::SplineParameters, m::Int64, x::Float64, derivative::Int64) -> Float64\n\nEvaluate the cubic B-spline basis function φₘ(x) or one of its derivatives.\n\nFollows the Ooyama (2002) compact-support cubic B-spline formulation. Each basis function is nonzero only within two cell widths of node m.\n\nArguments\n\nsp::SplineParameters: Spline configuration (domain and cell width)\nm::Int64: Node index.  Node positions are xmin + m*DX for m = -1, 0, ..., num_cells+1\nx::Float64: Physical evaluation point; must lie within [sp.xmin, sp.xmax]\nderivative::Int64: Order of derivative to evaluate:\n0 — function value\n1 — first derivative ∂φ/∂x\n2 — second derivative ∂²φ/∂x²\n3 — third derivative (piecewise constant, used for the Q-matrix smoothing term)\n\nReturns\n\nFloat64: Value of φₘ(x) or its requested derivative at x\n\nThrows\n\nDomainError if x lies outside [sp.xmin, sp.xmax]\n\n\n\n\n\n","title":"Springsteel.CubicBSpline.basis"},{"category":"function","location":"cubicbspline/#Springsteel.CubicBSpline.calcGammaBC","page":"CubicBSpline","text":"calcGammaBC(sp::SplineParameters) -> Matrix{Float64}\n\nBuild the boundary-condition projection matrix Γ.\n\nThe matrix maps the interior (free) spline coefficients to the full coefficient vector of length num_cells + 3, encoding the homogeneous boundary conditions specified by sp.BCL and sp.BCR.  It is used inside SAtransform to fold and unfold boundary conditions during the spectral solve.\n\nArguments\n\nsp::SplineParameters: Spline parameters including BCL, BCR, and num_cells\n\nReturns\n\nMatrix{Float64}: Projection matrix of size (Minterior, num_cells+3) where Minterior = num_cells + 3 - rankL - rankR and rankL/rankR are the number of constraints imposed by the left/right BCs.\n\n\n\n\n\n","title":"Springsteel.CubicBSpline.calcGammaBC"},{"category":"function","location":"cubicbspline/#Springsteel.CubicBSpline.calcPQfactor","page":"CubicBSpline","text":"calcPQfactor(sp::SplineParameters, gammaBC::Matrix{Float64}) -> (Matrix{Float64}, AbstractSplineFactor)\n\nBuild the variational (P + Q) matrix and return its banded LLᵀ Cholesky factorisation (or a dense Cholesky fallback for the periodic-BC edge case).\n\nFollowing Ooyama (2002), the spectral fit minimises a penalised least-squares functional:\n\nJa = langle u - hatu u - hatu rangle_P + varepsilon_q langle a a rangle_Q\n\nwhere P contains mass-matrix inner products of basis functions and Q contains inner products of their third derivatives.  The smoothing weight is:\n\nvarepsilon_q = left(fracl_q cdot Delta x2piright)^6\n\nThe open-form (P + Q) (with BCs folded in via gammaBC) is factorised with a sparse Cholesky decomposition for repeated fast linear solves during SAtransform.\n\nArguments\n\nsp::SplineParameters: Spline parameters (domain, cells, filter length l_q)\ngammaBC::Matrix{Float64}: Boundary-condition projection matrix from calcGammaBC\n\nReturns\n\n(pq::Matrix{Float64}, pqFactor::AbstractSplineFactor): Full (P + Q) matrix and its Cholesky factor\n\n\n\n\n\n","title":"Springsteel.CubicBSpline.calcPQfactor"},{"category":"function","location":"cubicbspline/#Springsteel.CubicBSpline.calcP1factor","page":"CubicBSpline","text":"calcP1factor(sp::SplineParameters, gammaBC::Matrix{Float64}) -> (Matrix{Float64}, AbstractSplineFactor)\n\nBuild the integral variational (P⁽¹⁾ + Q) matrix and return its banded LLᵀ Cholesky factorisation (or a dense Cholesky fallback for the periodic-BC edge case).\n\nFollows Ooyama (2002) Eq. 4.24. Like calcPQfactor, but the P matrix inner products use the first derivative of the basis function rather than the function itself:\n\nP^(1)_m_1 m_2 = sum_textcells Delta x sum_mu w_mu varphi_m_1(x_mu)varphi_m_2(x_mu)\n\nThe Q (smoothing) matrix is identical to the one in calcPQfactor.  The factorised system is used by SIIntcoefficients to find spectral coefficients of the indefinite integral.\n\nArguments\n\nsp::SplineParameters: Spline parameters (domain, cells, filter length l_q)\ngammaBC::Matrix{Float64}: Boundary-condition projection matrix from calcGammaBC\n\nReturns\n\n(p1q::Matrix{Float64}, p1qFactor::AbstractSplineFactor): Full (P⁽¹⁾ + Q) matrix and its Cholesky factor\n\nSee also: calcPQfactor, SIIntcoefficients\n\n\n\n\n\n","title":"Springsteel.CubicBSpline.calcP1factor"},{"category":"function","location":"cubicbspline/#Springsteel.CubicBSpline.calcMishPoints","page":"CubicBSpline","text":"calcMishPoints(sp::SplineParameters) -> Vector{Float64}\n\nCompute the Gaussian quadrature (\"mish\") point locations.\n\nFor each of the num_cells cells, three Gauss–Legendre quadrature points are placed at positions determined by the sqrt(3/5) abscissae (5-point rule mapped to each cell). These are the physical-space locations where field values must be provided for the spectral transform.\n\nArguments\n\nsp::SplineParameters: Spline parameters (domain, cell width)\n\nReturns\n\nVector{Float64}: Sorted vector of num_cells * mubar (= num_cells * 3) mish point coordinates in [xmin, xmax]\n\nSee also: Spline1D, SBtransform\n\n\n\n\n\n","title":"Springsteel.CubicBSpline.calcMishPoints"},{"category":"function","location":"cubicbspline/#Springsteel.CubicBSpline.setMishValues","page":"CubicBSpline","text":"setMishValues(spline::Spline1D, uMish::Vector{Float64})\n\nCopy physical field values into the spline's internal mish buffer.\n\nConvenience wrapper that performs spline.uMish .= uMish.  Use before calling SBtransform!(spline) when you prefer the in-place workflow.\n\nArguments\n\nspline::Spline1D: Target spline object\nuMish::Vector{Float64}: Field values at the mish points (length num_cells * 3)\n\nSee also: SBtransform\n\n\n\n\n\n","title":"Springsteel.CubicBSpline.setMishValues"},{"category":"function","location":"cubicbspline/#Springsteel.CubicBSpline.SBtransform","page":"CubicBSpline","text":"SBtransform(sp::SplineParameters, uMish::Vector{Float64}) -> Vector{Float64}\nSBtransform(spline::Spline1D,      uMish::Vector{Float64}) -> Vector{Float64}\nSBtransform!(spline::Spline1D)\n\nCompute the SB transform: project physical field values at the mish points onto the cubic B-spline basis to obtain the B-vector b_m = langle varphi_m u rangle.\n\nb_m = sum_textcells Delta x sum_mu w_mu varphi_m(x_mu) u(x_mu)\n\nThis is the first step of the two-step physical→spectral transform.  The full transform continues with SAtransform.\n\nArguments\n\nsp / spline: Spline parameters or a constructed Spline1D\nuMish::Vector{Float64}: Field values at the num_cells * 3 mish points\n\nReturns\n\nVector{Float64}: B-vector of length num_cells + 3 (allocating versions)\nThe in-place SBtransform! stores the result in spline.b\n\nNotes\n\nThe boundary folds (gammaBC) are not applied here; they are applied inside SAtransform.\n\nSee also: SAtransform, SBxtransform\n\n\n\n\n\n","title":"Springsteel.CubicBSpline.SBtransform"},{"category":"function","location":"cubicbspline/#Springsteel.CubicBSpline.SBxtransform","page":"CubicBSpline","text":"SBxtransform(sp::SplineParameters, uMish::Vector{Float64}, BCL::Float64, BCR::Float64) -> Vector{Float64}\nSBxtransform(spline::Spline1D,      uMish::Vector{Float64}, BCL::Float64, BCR::Float64) -> Vector{Float64}\n\nCompute the SBx transform: B-vector of the derivative f via integration by parts.\n\nb_m = biglvarphi_m cdot fbigr_x_0^x_0 - int varphi_m(x) f(x) dx\n\nArguments\n\nsp / spline: Spline parameters or a constructed Spline1D\nuMish::Vector{Float64}: Field values at the num_cells * 3 mish points\nBCL::Float64: Value of f at the left boundary xmin (not a boundary condition constant)\nBCR::Float64: Value of f at the right boundary xmax\n\nReturns\n\nVector{Float64}: B-vector for f', of length num_cells + 3\n\nSee also: SBtransform, SAtransform\n\n\n\n\n\n","title":"Springsteel.CubicBSpline.SBxtransform"},{"category":"function","location":"cubicbspline/#Springsteel.CubicBSpline.SBxtransform!","page":"CubicBSpline","text":"SBxtransform!(spline::Spline1D, uMish_input::AbstractVector, BCL::Real, BCR::Real)\n    -> Vector{Float64}\n\nIn-place forward Bx transform. Writes the B¹ vector (length bDim) into the spline's _scratch_bx buffer using the cached quadpoints/quadweights and returns it. Allocation-free; intended for the SIIntcoefficients hot path.\n\nThe boundary terms BCL, BCR are typically zero for the indefinite-integral solve; non-zero values implement inhomogeneous integration boundary data.\n\nSee also: SBxtransform, SIIntcoefficients\n\n\n\n\n\n","title":"Springsteel.CubicBSpline.SBxtransform!"},{"category":"function","location":"cubicbspline/#Springsteel.CubicBSpline.SAtransform","page":"CubicBSpline","text":"SAtransform(sp::SplineParameters, gammaBC::Matrix{Float64}, pqFactor, b::Vector{Float64}) -> Vector{Float64}\nSAtransform(spline::Spline1D, b::AbstractVector) -> Vector{Float64}\nSAtransform!(spline::Spline1D)\nSAtransform(spline::Spline1D, b::Vector{Float64}, ahat::Vector{Float64}) -> Vector{Float64}\n\nCompute the SA transform: convert a B-vector to spectral A-coefficients by solving the variational system with boundary conditions.\n\na = Gamma^T bigl(Gamma (P+Q) Gamma^T)^-1 Gamma bbigr\n\nwhere Gamma is the boundary-condition projection matrix and (P+Q) is the pre-computed (and pre-factored) variational matrix.\n\nVariants\n\nSAtransform(sp, gammaBC, pqFactor, b) — lowest-level call with all components explicit\nSAtransform(spline, b) — allocates and returns a using the cached spline internals\nSAtransform!(spline) — in-place; reads spline.b, writes spline.a\nSAtransform(spline, b, ahat) — inhomogeneous/incremental form; ahat is a background coefficient vector that carries inhomogeneous boundary values\n\nReturns\n\nVector{Float64}: Spectral coefficient vector a of length num_cells + 3 (allocating variants); the in-place SAtransform! stores in spline.a\n\nSee also: SBtransform, SItransform\n\n\n\n\n\n","title":"Springsteel.CubicBSpline.SAtransform"},{"category":"function","location":"cubicbspline/#Springsteel.CubicBSpline.SItransform","page":"CubicBSpline","text":"SItransform(sp::SplineParameters, a::Vector{Float64}, x::Float64, derivative::Int64 = 0) -> Float64\nSItransform(sp::SplineParameters, a::Vector{Float64}, derivative::Int64 = 0) -> Vector{Float64}\nSItransform(sp::SplineParameters, a::Vector{Float64}, points::Vector{Float64}, derivative::Int64 = 0) -> Vector{Float64}\nSItransform(sp::SplineParameters, a::Vector{Float64}, points::Vector{Float64}, u::AbstractVector, derivative::Int64 = 0) -> Vector{Float64}\nSItransform!(spline::Spline1D)\nSItransform(spline::Spline1D, u::AbstractVector) -> Vector{Float64}\nSItransform(spline::Spline1D, points::Vector{Float64}, u::AbstractVector) -> Vector{Float64}\n\nCompute the SI transform: evaluate the B-spline expansion at physical locations.\n\nu(x) = sum_m a_m varphi_m(x)\n\nThis is the spectral→physical step and is the inverse of the SB+SA pipeline.\n\nVariants\n\nSingle point: SItransform(sp, a, x, derivative) — returns one Float64\nAll mish points (allocating): SItransform(sp, a, derivative)\nCustom points (allocating): SItransform(sp, a, points, derivative)\nCustom points (in-place): SItransform(sp, a, points, u, derivative) — writes into u\nIn-place at mish points: SItransform!(spline) — writes into spline.uMish\nConvenience at mish points: SItransform(spline, u)\nConvenience at custom points: SItransform(spline, points, u)\n\nArguments\n\nsp / spline: Spline parameters or a Spline1D object\na::Vector{Float64}: Spectral coefficient vector of length num_cells + 3\nx / points: Evaluation location(s); must lie within [xmin, xmax]\nderivative::Int64: Derivative order (default 0; supports 0, 1, 2)\nu: Pre-allocated output vector (written in-place where applicable)\n\nReturns\n\nEvaluated field values (and/or derivatives) at the requested points\n\nSee also: SAtransform, SIxtransform, SIxxtransform\n\n\n\n\n\n","title":"Springsteel.CubicBSpline.SItransform"},{"category":"function","location":"cubicbspline/#Springsteel.CubicBSpline.SItransform_matrix","page":"CubicBSpline","text":"SItransform_matrix(spline::Spline1D, points::Vector{Float64}, derivative::Int64 = 0) -> Matrix{Float64}\n\nBuild the SI transform as an explicit evaluation matrix (mainly for debugging and linear-system solving).\n\nReturns a matrix M of size (num_cells*mubar, bDim) such that M * a ≈ u at the given points.  Each row corresponds to one point and each column to one spline basis function.\n\nArguments\n\nspline::Spline1D: Spline object (provides parameters and bDim)\npoints::Vector{Float64}: Physical evaluation locations\nderivative::Int64: Derivative order (default 0)\n\nReturns\n\nMatrix{Float64} of size (length(points), bDim)\n\nSee also: SItransform\n\n\n\n\n\n","title":"Springsteel.CubicBSpline.SItransform_matrix"},{"category":"function","location":"cubicbspline/#Springsteel.CubicBSpline.SIxtransform","page":"CubicBSpline","text":"SIxtransform(spline::Spline1D) -> Vector{Float64}\nSIxtransform(spline::Spline1D, uprime::AbstractVector) -> Vector{Float64}\nSIxtransform(spline::Spline1D, points::Vector{Float64}, uprime::AbstractVector) -> Vector{Float64}\nSIxtransform(sp::SplineParameters, a::Vector{Float64}, points::AbstractVector) -> Vector{Float64}\n\nEvaluate the first derivative u(x) = partial u  partial x of the B-spline expansion at physical locations.\n\nAll variants delegate to SItransform(..., derivative=1).\n\nVariants\n\nSIxtransform(spline) — allocates; evaluates at all mish points\nSIxtransform(spline, uprime) — in-place; uprime is written with derivatives at mish points\nSIxtransform(spline, points, uprime) — in-place at custom points\nSIxtransform(sp, a, points) — allocating at custom points given raw parameters\n\nReturns\n\nVector{Float64}: First-derivative values at the requested points\n\nSee also: SItransform, SIxxtransform\n\n\n\n\n\n","title":"Springsteel.CubicBSpline.SIxtransform"},{"category":"function","location":"cubicbspline/#Springsteel.CubicBSpline.SIxxtransform","page":"CubicBSpline","text":"SIxxtransform(spline::Spline1D) -> Vector{Float64}\nSIxxtransform(spline::Spline1D, uprime2::AbstractVector) -> Vector{Float64}\nSIxxtransform(spline::Spline1D, points::Vector{Float64}, uprime2::AbstractVector) -> Vector{Float64}\nSIxxtransform(sp::SplineParameters, a::Vector{Float64}, points::Vector{Float64}) -> Vector{Float64}\n\nEvaluate the second derivative u(x) = partial^2 u  partial x^2 of the B-spline expansion at physical locations.\n\nAll variants delegate to SItransform(..., derivative=2).\n\nVariants\n\nSIxxtransform(spline) — allocates; evaluates at all mish points\nSIxxtransform(spline, uprime2) — in-place; uprime2 is written at mish points\nSIxxtransform(spline, points, uprime2) — in-place at custom points\nSIxxtransform(sp, a, points) — allocating at custom points given raw parameters\n\nReturns\n\nVector{Float64}: Second-derivative values at the requested points\n\nSee also: SItransform, SIxtransform\n\n\n\n\n\n","title":"Springsteel.CubicBSpline.SIxxtransform"},{"category":"function","location":"cubicbspline/#Springsteel.CubicBSpline.SIIntcoefficients","page":"CubicBSpline","text":"SIIntcoefficients(sp::SplineParameters, gammaBC::Matrix{Float64}, p1Factor, uMish::Vector{Float64}) -> Vector{Float64}\nSIIntcoefficients(spline::Spline1D, uMish::Vector{Float64}) -> Vector{Float64}\n\nCompute the integral spectral coefficients of the indefinite integral of uMish.\n\nFollows Ooyama (2002) Eqs. 4.23–4.25.  Builds the right-hand side vector r_m = int varphi_m(x) f(x)dx via quadrature, then solves the variational system using the pre-factored (P^(1) + Q) matrix:\n\nhata^textint = Gamma^T Bigl(Gamma(P^(1)+Q)Gamma^T)^-1Gamma rBigr\n\nwhere P^(1)_m_1 m_2 = int varphi_m_1varphi_m_2dx.\n\nNote: r_m = -textSBxtransform(f 0 0) — the raw derivative-basis inner product without boundary correction.  Boundary values of f are not required; the constant of integration is applied separately via C0 in SIInttransform.\n\nVariants\n\nSIIntcoefficients(sp, gammaBC, p1Factor, uMish) — lowest-level; all components explicit\nSIIntcoefficients(spline, uMish) — convenience form using cached spline internals\n\nArguments\n\nsp / spline: Spline parameters or a constructed Spline1D\nuMish::Vector{Float64}: Field values at the mish points (the function to be integrated)\n\nReturns\n\nVector{Float64}: Spectral coefficient vector aInt of length num_cells + 3\n\nSee also: SIInttransform, SBxtransform, calcP1factor\n\n\n\n\n\n","title":"Springsteel.CubicBSpline.SIIntcoefficients"},{"category":"function","location":"cubicbspline/#Springsteel.CubicBSpline.SIIntcoefficients!","page":"CubicBSpline","text":"SIIntcoefficients!(spline::Spline1D, uMish_input::AbstractVector) -> spline.a\n\nIn-place form of SIIntcoefficients. Writes the integral A-coefficients into spline.a (clobbering any prior contents) using _scratch_bx, _scratch_Min, and _scratch_Mout. Allocation-free.\n\nLike SAtransform!, this overwrites spline.a.\n\n\n\n\n\n","title":"Springsteel.CubicBSpline.SIIntcoefficients!"},{"category":"function","location":"cubicbspline/#Springsteel.CubicBSpline.SIInttransform","page":"CubicBSpline","text":"SIInttransform(sp::SplineParameters, gammaBC::Matrix{Float64}, p1Factor, uMish::Vector{Float64}, C0::Float64 = 0.0) -> Vector{Float64}\nSIInttransform(spline::Spline1D, uMish::Vector{Float64}, C0::Float64 = 0.0) -> Vector{Float64}\n\nCompute the indefinite integral of the B-spline expansion in physical space.\n\nApplies SIIntcoefficients (Ooyama 2002, Eqs. 4.23–4.25) to obtain the spectral coefficients of the antiderivative, then evaluates the expansion at the mish points via SItransform.  The optional constant of integration C0 is added uniformly to every physical output value.\n\nVariants\n\nSIInttransform(sp, gammaBC, p1Factor, uMish, C0) — lowest-level call; all components explicit\nSIInttransform(spline, uMish, C0) — convenience form using cached spline internals\n\nArguments\n\nsp / spline: Spline parameters or a constructed Spline1D\nuMish::Vector{Float64}: Field values at the mish points (the function to be integrated)\nC0::Float64: Constant of integration (default 0.0); added uniformly to the output\n\nReturns\n\nVector{Float64}: Integral field values of length num_cells * 3 at the mish points\n\nSee also: SIIntcoefficients, SItransform, SIxtransform\n\n\n\n\n\n","title":"Springsteel.CubicBSpline.SIInttransform"},{"category":"function","location":"cubicbspline/#Springsteel.CubicBSpline.SIInttransform!","page":"CubicBSpline","text":"SIInttransform!(spline::Spline1D, uMish_input::AbstractVector,\n                uInt::AbstractVector, C0::Real = 0.0) -> uInt\n\nIn-place form of SIInttransform. Computes the indefinite integral of uMish_input and writes the physical-space result into the user-provided uInt buffer (length mishDim). Allocation-free; clobbers spline.a.\n\n\n\n\n\n","title":"Springsteel.CubicBSpline.SIInttransform!"},{"category":"function","location":"cubicbspline/#Springsteel.CubicBSpline.set_ahat_r3x!","page":"CubicBSpline","text":"set_ahat_r3x!(spline::Spline1D, u0::Real, u1::Real, u2::Real, side::Symbol)\n\nSet the inhomogeneous R3X boundary conditions on spline.\n\nComputes the border coefficients that produce the specified boundary values u(x₀) = u0, u'(x₀) = u1, u''(x₀) = u2 and stores them in spline.ahat.\n\nArguments\n\nspline::Spline1D: Spline with R3X boundary condition on the specified side\nu0::Real: Desired field value at the boundary\nu1::Real: Desired first derivative at the boundary\nu2::Real: Desired second derivative at the boundary\nside::Symbol: :left or :right\n\nExample\n\nsp = SplineParameters(xmin=0.0, xmax=1.0, num_cells=10,\n                      BCL=CubicBSpline.R3X, BCR=CubicBSpline.R3X)\nspline = Spline1D(sp)\nset_ahat_r3x!(spline, 1.0, 0.0, 0.0, :left)   # u(0) = 1\nset_ahat_r3x!(spline, 2.0, 0.0, 0.0, :right)  # u(1) = 2\n\nSee also: R3X (interface BC constant), _border_matrix (internal)\n\n\n\n\n\n","title":"Springsteel.CubicBSpline.set_ahat_r3x!"},{"category":"function","location":"cubicbspline/#Springsteel.CubicBSpline.spline_basis_matrix","page":"CubicBSpline","text":"spline_basis_matrix(spline::Spline1D; gammaBC=nothing) -> Matrix{Float64}\n\nBuild the (M times N) cubic B-spline evaluation matrix.\n\nEntry [i, j] is the value of the j-th B-spline basis function varphi_j(x_i) at the i-th mish point. If gammaBC is provided, returns the boundary-condition-folded form mathbfM boldsymbolGamma^T with reduced column count.\n\nKeywords\n\ngammaBC::Union{Matrix{Float64}, Nothing}: If provided, fold boundary conditions to produce the reduced matrix of size (mishDim, Minterior).\n\nSee also: spline_1st_derivative_matrix, spline_2nd_derivative_matrix\n\n\n\n\n\n","title":"Springsteel.CubicBSpline.spline_basis_matrix"},{"category":"function","location":"cubicbspline/#Springsteel.CubicBSpline.spline_1st_derivative_matrix","page":"CubicBSpline","text":"spline_1st_derivative_matrix(spline::Spline1D; gammaBC=nothing) -> Matrix{Float64}\n\nBuild the (M times N) cubic B-spline first-derivative matrix.\n\nEntry [i, j] is the first derivative of the j-th B-spline basis function varphi_j(x_i) at the i-th mish point. If gammaBC is provided, returns the boundary-condition-folded form mathbfM_x boldsymbolGamma^T with reduced column count.\n\nKeywords\n\ngammaBC::Union{Matrix{Float64}, Nothing}: If provided, fold boundary conditions to produce the reduced matrix of size (mishDim, Minterior).\n\nSee also: spline_basis_matrix, spline_2nd_derivative_matrix\n\n\n\n\n\n","title":"Springsteel.CubicBSpline.spline_1st_derivative_matrix"},{"category":"function","location":"cubicbspline/#Springsteel.CubicBSpline.spline_2nd_derivative_matrix","page":"CubicBSpline","text":"spline_2nd_derivative_matrix(spline::Spline1D; gammaBC=nothing) -> Matrix{Float64}\n\nBuild the (M times N) cubic B-spline second-derivative matrix.\n\nEntry [i, j] is the second derivative of the j-th B-spline basis function varphi_j(x_i) at the i-th mish point. If gammaBC is provided, returns the boundary-condition-folded form mathbfM_xx boldsymbolGamma^T with reduced column count.\n\nKeywords\n\ngammaBC::Union{Matrix{Float64}, Nothing}: If provided, fold boundary conditions to produce the reduced matrix of size (mishDim, Minterior).\n\nSee also: spline_basis_matrix, spline_1st_derivative_matrix\n\n\n\n\n\n","title":"Springsteel.CubicBSpline.spline_2nd_derivative_matrix"},{"category":"function","location":"cubicbspline/#Springsteel.CubicBSpline.Btransform","page":"CubicBSpline","text":"Generic B-transform wrapper for Spline1D. Delegates to SBtransform.\n\n\n\n\n\n","title":"Springsteel.CubicBSpline.Btransform"},{"category":"function","location":"cubicbspline/#Springsteel.CubicBSpline.Btransform!","page":"CubicBSpline","text":"Generic in-place B-transform wrapper for Spline1D. Delegates to SBtransform!.\n\n\n\n\n\n","title":"Springsteel.CubicBSpline.Btransform!"},{"category":"function","location":"cubicbspline/#Springsteel.CubicBSpline.Bxtransform","page":"CubicBSpline","text":"Generic Bx-transform wrapper for Spline1D. Delegates to SBxtransform.\n\n\n\n\n\n","title":"Springsteel.CubicBSpline.Bxtransform"},{"category":"function","location":"cubicbspline/#Springsteel.CubicBSpline.Atransform","page":"CubicBSpline","text":"Generic A-transform wrapper for Spline1D (allocating). Delegates to SAtransform.\n\n\n\n\n\nGeneric A-transform wrapper for Spline1D (inhomogeneous form). Delegates to SAtransform.\n\n\n\n\n\n","title":"Springsteel.CubicBSpline.Atransform"},{"category":"function","location":"cubicbspline/#Springsteel.CubicBSpline.Atransform!","page":"CubicBSpline","text":"Generic in-place A-transform wrapper for Spline1D. Delegates to SAtransform!.\n\n\n\n\n\n","title":"Springsteel.CubicBSpline.Atransform!"},{"category":"function","location":"cubicbspline/#Springsteel.CubicBSpline.Itransform!","page":"CubicBSpline","text":"Generic in-place I-transform wrapper for Spline1D. Delegates to SItransform!.\n\n\n\n\n\n","title":"Springsteel.CubicBSpline.Itransform!"},{"category":"function","location":"cubicbspline/#Springsteel.CubicBSpline.Itransform","page":"CubicBSpline","text":"Generic I-transform wrapper for Spline1D (mish points, in-place output). Delegates to SItransform.\n\n\n\n\n\nGeneric I-transform wrapper for Spline1D (custom points, in-place output). Delegates to SItransform.\n\n\n\n\n\n","title":"Springsteel.CubicBSpline.Itransform"},{"category":"function","location":"cubicbspline/#Springsteel.CubicBSpline.Ixtransform","page":"CubicBSpline","text":"Generic Ix-transform wrapper for Spline1D (allocating, mish points). Delegates to SIxtransform.\n\n\n\n\n\nGeneric Ix-transform wrapper for Spline1D (mish points, in-place output). Delegates to SIxtransform.\n\n\n\n\n\nGeneric Ix-transform wrapper for Spline1D (custom points, in-place output). Delegates to SIxtransform.\n\n\n\n\n\n","title":"Springsteel.CubicBSpline.Ixtransform"},{"category":"function","location":"cubicbspline/#Springsteel.CubicBSpline.Ixxtransform","page":"CubicBSpline","text":"Generic Ixx-transform wrapper for Spline1D (allocating, mish points). Delegates to SIxxtransform.\n\n\n\n\n\nGeneric Ixx-transform wrapper for Spline1D (mish points, in-place output). Delegates to SIxxtransform.\n\n\n\n\n\nGeneric Ixx-transform wrapper for Spline1D (custom points, in-place output). Delegates to SIxxtransform.\n\n\n\n\n\n","title":"Springsteel.CubicBSpline.Ixxtransform"},{"category":"function","location":"cubicbspline/#Springsteel.CubicBSpline.IInttransform","page":"CubicBSpline","text":"Generic indefinite-integral transform wrapper for Spline1D. Delegates to SIInttransform.\n\n\n\n\n\n","title":"Springsteel.CubicBSpline.IInttransform"},{"category":"section","location":"interpolation/#Interpolation","page":"Interpolation","text":"Springsteel's interpolation framework moves data between grids of different resolutions, domains, and geometries while preserving the spectral representation as much as possible. It has three layers:\n\nData import — lift regularly-gridded data (from NetCDF, array inputs, external model output) onto a freshly constructed SpringsteelGrid.\nSame-geometry interpolation — move data between two grids with matching {G, I, J, K} type parameters (e.g., RR → RR).\nCross-geometry interpolation — move data between grids of different geometries (e.g., RR → RL, SL → RLZ), using coordinate mappings to link the physical coordinate systems.\n\nThe fourth capability layered on top — unstructured evaluation at arbitrary Cartesian / cylindrical / spherical points — is the building block for both cross-geometry interpolation and user-facing tools like evaluate_unstructured.","title":"Interpolation"},{"category":"section","location":"interpolation/#Layer-1-—-Data-import","page":"Interpolation","text":"If you're starting from raw arrays of values on a regular grid, grid_from_regular_data wraps the whole setup: it creates a SpringsteelGrid with the right dimensions, fills the physical array, and runs spectralTransform! so the result is ready for transforms, derivatives, or further interpolation.\n\nThe signature is overloaded for 1D, 2D, and 3D inputs. Coordinate vectors must be uniformly spaced (the factory checks this), and their length must be divisible by mubar (default 3) so the input aligns to the internal Gauss–Legendre quadrature nodes. Domain bounds are inferred as [x[1] - h/2, x[end] + h/2] — regular-midpoint convention, which matches the layout of typical NetCDF and raw-array data sources.\n\nusing Springsteel\n\nx = collect(0.0:0.05:1.0)        # 21 points, h = 0.05\ndata = @. exp(-(x - 0.5)^2 / 0.01)\n\ngrid = grid_from_regular_data(x, data;\n    mubar = 1,                   # regular (non-Gauss) quadrature\n    BCL   = DirichletBC(),\n    BCR   = DirichletBC(),\n    vars  = Dict(\"u\" => 1),\n)\n\n# grid is now a SpringsteelGrid that you can pass to downstream code\n\nBCs may be bare BoundaryConditions (applied to every variable) or per-variable Dicts (Dict(\"u\" => DirichletBC(), \"v\" => NaturalBC())). Derivative slots are left as NaN to catch accidental use before the spectral round trip completes.","title":"Layer 1 — Data import"},{"category":"section","location":"interpolation/#NetCDF","page":"Interpolation","text":"Reads a NetCDF file and builds a grid from one or more variables. You pass a tuple of dimension names in i/j/k order and a list of variable names (or nothing to import every variable). The factory handles dimension permutation so Springsteel's i-outer / k-inner layout is respected regardless of how the file was written.\n\ngrid = grid_from_netcdf(\"rainfall.nc\";\n    dim_names = (\"lon\", \"lat\"),\n    var_names = [\"precip_mm\"],\n    BCL = NaturalBC(), BCR = NaturalBC(),\n    BCD = NaturalBC(), BCU = NaturalBC(),\n)","title":"NetCDF"},{"category":"section","location":"interpolation/#Layer-2-—-Same-geometry-interpolation","page":"Interpolation","text":"When source and target grids share the same {G, I, J, K} parameters (same geometry, same basis types), interpolation reuses the basis evaluation machinery directly — there are no coordinate transformations needed. The source's spectral coefficients are evaluated at the target's gridpoints via tensor-product B-spline / Fourier / Chebyshev basis matrices.\n\nThe bang version writes into target.physical; the non-bang version returns the values as a matrix.\n\nOut-of-bounds handling is controlled by out_of_bounds:\n\nValue Behaviour\n:nan (default) fill OOB target points with NaN\n:error throw an ArgumentError\n<Number> fill OOB points with the given constant\n\nVariable matching is by name — the intersection of source and target vars dicts is interpolated; unmatched target variables are left unchanged and a warning is printed so you don't silently miss data.\n\n# Interpolate from a coarse RR grid to a fine RR grid\ncoarse = createGrid(gp_coarse)\nfine   = createGrid(gp_fine)\n# ... fill coarse.physical and spectralTransform!(coarse) ...\ninterpolate_to_grid!(coarse, fine)","title":"Layer 2 — Same-geometry interpolation"},{"category":"section","location":"interpolation/#Layer-3-—-Cross-geometry-interpolation","page":"Interpolation","text":"When source and target live on different geometries (e.g., a Cartesian RR analysis grid → a cylindrical RL hurricane grid) there's no way to share a basis evaluation path — the target gridpoints live in a different coordinate system from the source. The framework bridges this by converting each target point's coordinates into the source's frame, then evaluating the source spectrally at those unstructured points.\n\nDefault coordinate mappings are provided for the common 3D cases:\n\nCartesian ↔ Cylindrical (2D and 3D)\nCartesian ↔ Spherical (3D)\nCylindrical ↔ Spherical (3D)\n\nFor 2D Cartesian ↔ spherical, 2D cylindrical ↔ spherical, and RR ↔ SL there is no natural default — you must pass an explicit coordinate_map function that takes a matrix of target points pts[:, dims] and returns the corresponding source-frame points.\n\n# RR → RL: Cartesian analysis to cylindrical hurricane grid centred\n#          at (x0, y0).\nrl_grid = interpolate_to_grid(rr_grid, rl_target;\n    coordinate_map = pts -> begin\n        r = pts[:, 1]; λ = pts[:, 2]\n        hcat(x0 .+ r .* cos.(λ), y0 .+ r .* sin.(λ))\n    end,\n)\n\nIf your source and target grid happen to have matching {G, I, J, K} parameters but you want to go through a coordinate mapping anyway (e.g., to shift the origin), pass the mapping explicitly and the framework will route through the unstructured path rather than the fast tensor-product path.","title":"Layer 3 — Cross-geometry interpolation"},{"category":"section","location":"interpolation/#Unstructured-point-evaluation","page":"Interpolation","text":"The workhorse underlying cross-geometry interpolation is evaluate_unstructured, which evaluates a SpringsteelGrid at a set of arbitrary points in its native coordinate system:\n\nDispatches by basis: 1D B-spline (R), 2D B-spline × B-spline (RR), 2D B-spline × Chebyshev (RZ), 3D B-spline × B-spline × B-spline (RRR), batched-Fourier RL / SL (2D cylindrical / spherical), and 3D batched Fourier + Chebyshev RLZ / SLZ. The input points are a matrix with one row per point and columns matching the grid's coordinate order:\n\n# Evaluate an RL grid at 1000 random polar points\nnpts = 1000\npts  = hcat(rand(npts) .* 50.0,          # r\n            rand(npts) .* 2π)             # λ\nvals = evaluate_unstructured(rl_grid, pts; vars=[\"u\"])\n\nPoints that fall outside the source grid's bounds are filtered before evaluation (B-spline evaluation errors on out-of-domain inputs), and the returned values at those rows follow the out_of_bounds policy (:nan by default).\n\nPer-call caching of the γ-folded ahat stripes keeps repeated evaluations against the same spectral state allocation-free — RL interpolation allocations dropped from 25 MB to 494 kB between v0.2 and v1.0, and RLZ from 27 MB to 75 kB, via this cache. You don't need to manage it; it keys on (objectid, variable) and invalidates on the spectral column hash changing.","title":"Unstructured point evaluation"},{"category":"section","location":"interpolation/#Coordinate-transforms","page":"Interpolation","text":"These helpers are used internally by the default cross-geometry mappings, but you can call them directly when building a custom coordinate_map. Note the spherical convention: the returned tuple is (θ, λ, r) where θ is colatitude (0 at the north pole) and λ is azimuth — this matches Springsteel's SLZ grid layout. The latlon_to_spherical(lon_deg, lat_deg) helper adapts the more common lon/lat geographic order.","title":"Coordinate transforms"},{"category":"section","location":"interpolation/#Setting-boundary-values","page":"Interpolation","text":"Some workflows need to pin boundary values (e.g., interpolating a coarse field into the border of a fine domain without touching the interior). set_boundary_values! is the per-geometry helper for that.","title":"Setting boundary values"},{"category":"section","location":"interpolation/#See-also","page":"Interpolation","text":"SpringsteelGrid — grid construction + geometry type parameters\nGrid Relocation — closely related: RL/RLZ grid shift via the same unstructured evaluation path\nTutorial — worked examples using imported data","title":"See also"},{"category":"function","location":"interpolation/#Springsteel.grid_from_regular_data","page":"Interpolation","text":"grid_from_regular_data(x, data; kwargs...) -> SpringsteelGrid\ngrid_from_regular_data(x, y, data; kwargs...) -> SpringsteelGrid\ngrid_from_regular_data(x, y, z, data; kwargs...) -> SpringsteelGrid\n\nCreate a CubicBSpline grid with quadrature=:regular from regularly-spaced data.\n\nThe coordinate vectors must be uniformly spaced. The number of points in each dimension must be divisible by mubar.\n\nDimension inference\n\nGiven N regularly-spaced points and target mubar, num_cells = N ÷ mubar. Domain bounds are inferred from the coordinate vectors: since regular quadrature places points at cell midpoints, the domain extends half a grid spacing beyond the first and last data points.\n\nData layout\n\ndata is always (total_points, nvars) where total_points is the product of all coordinate vector lengths. For multi-dimensional data, the ordering is i-outer, j-inner (j varies fastest), matching Springsteel's physical array layout.\n\nKeyword arguments\n\nmubar::Int=3: Quadrature points per cell\nl_q=2.0: Filter length (scalar or Dict)\nBCL, BCR: i-dimension boundary conditions (default CubicBSpline.R0)\nBCU, BCD: j-dimension boundary conditions (2D/3D only)\nBCB, BCT: k-dimension boundary conditions (3D only)\nvars::Vector{String}=String[]: Variable names (auto-generated if empty)\n\nReturns\n\nA SpringsteelGrid with physical data populated in derivative slot 1. Derivative slots are filled with NaN — call spectralTransform! followed by gridTransform! to compute derivatives.\n\nSee also: grid_from_netcdf, interpolate_to_grid\n\n\n\n\n\n","title":"Springsteel.grid_from_regular_data"},{"category":"function","location":"interpolation/#Springsteel.grid_from_netcdf","page":"Interpolation","text":"grid_from_netcdf(filename::String; dim_names=nothing, var_names=nothing,\n    kwargs...) -> SpringsteelGrid\n\nLoad regularly-spaced data from a NetCDF file into a CubicBSpline grid.\n\nIf dim_names is not specified, infers coordinate dimensions from the file (variables whose names match dimension names, up to 3). If var_names is not specified, reads all non-coordinate variables.\n\nAll remaining keyword arguments are forwarded to grid_from_regular_data.\n\nSee also: grid_from_regular_data, read_netcdf\n\n\n\n\n\n","title":"Springsteel.grid_from_netcdf"},{"category":"function","location":"interpolation/#Springsteel.interpolate_to_grid","page":"Interpolation","text":"interpolate_to_grid(source::SpringsteelGrid, target::SpringsteelGrid;\n    vars=nothing, out_of_bounds=:nan) -> Matrix{Float64}\n\nEvaluate the source grid's spectral representation at the target grid's physical points. Both grids must have the same geometry and basis types.\n\nThe source grid must have valid spectral coefficients — call spectralTransform!(source) before interpolation.\n\nArguments\n\nsource: Source grid with populated spectral array\ntarget: Target grid defining the evaluation points\nvars: Variable names to interpolate (default: all common variables)\nout_of_bounds: How to handle target points outside the source domain. :nan (default), :error, or a numeric fill value.\n\nReturns\n\nMatrix{Float64} of size (target_phys_dim, n_target_vars) with interpolated function values. Unmatched target variables are filled with NaN.\n\nSee also: interpolate_to_grid!, grid_from_regular_data\n\n\n\n\n\ninterpolate_to_grid(source::SpringsteelGrid{G1,I1,J1,K1},\n                    target::SpringsteelGrid{G2,I2,J2,K2};\n                    vars=nothing, out_of_bounds=:nan,\n                    coordinate_map=nothing) -> Matrix{Float64}\n\nCross-geometry interpolation. Maps target gridpoints to source coordinates and evaluates the source spectral representation at those points.\n\nBoth grids may have different geometry types and basis types. The source grid must have valid spectral coefficients.\n\nArguments\n\ncoordinate_map: function (target_coords...) -> (source_coords...). If not provided, selects a default based on the geometry pair. Required for pairs without a natural default (e.g., 2D Cartesian → Spherical).\nvars: variable names to interpolate (default: all common variables)\nout_of_bounds: :nan (default), :error, or a numeric fill value\n\nSee also: evaluate_unstructured, interpolate_to_grid!\n\n\n\n\n\n","title":"Springsteel.interpolate_to_grid"},{"category":"function","location":"interpolation/#Springsteel.interpolate_to_grid!","page":"Interpolation","text":"interpolate_to_grid!(source::SpringsteelGrid, target::SpringsteelGrid;\n    vars=nothing, out_of_bounds=:nan) -> SpringsteelGrid\n\nLike interpolate_to_grid, but writes the interpolated values directly into target.physical[:, :, 1]. Derivative slots for interpolated variables are filled with NaN to indicate they need recomputation via spectralTransform! followed by gridTransform!.\n\nReturns the modified target grid.\n\n\n\n\n\ninterpolate_to_grid!(source::SpringsteelGrid{G1,I1,J1,K1},\n                     target::SpringsteelGrid{G2,I2,J2,K2};\n                     vars=nothing, out_of_bounds=:nan,\n                     coordinate_map=nothing) -> SpringsteelGrid\n\nCross-geometry in-place interpolation. Writes results to target.physical[:, :, 1] and fills derivative slots with NaN.\n\n\n\n\n\n","title":"Springsteel.interpolate_to_grid!"},{"category":"method","location":"interpolation/#Springsteel.interpolate_to_grid-Tuple{SpringsteelGrid, SpringsteelGrid}","page":"Interpolation","text":"interpolate_to_grid(source::SpringsteelGrid{G1,I1,J1,K1},\n                    target::SpringsteelGrid{G2,I2,J2,K2};\n                    vars=nothing, out_of_bounds=:nan,\n                    coordinate_map=nothing) -> Matrix{Float64}\n\nCross-geometry interpolation. Maps target gridpoints to source coordinates and evaluates the source spectral representation at those points.\n\nBoth grids may have different geometry types and basis types. The source grid must have valid spectral coefficients.\n\nArguments\n\ncoordinate_map: function (target_coords...) -> (source_coords...). If not provided, selects a default based on the geometry pair. Required for pairs without a natural default (e.g., 2D Cartesian → Spherical).\nvars: variable names to interpolate (default: all common variables)\nout_of_bounds: :nan (default), :error, or a numeric fill value\n\nSee also: evaluate_unstructured, interpolate_to_grid!\n\n\n\n\n\n","title":"Springsteel.interpolate_to_grid"},{"category":"function","location":"interpolation/#Springsteel.evaluate_unstructured","page":"Interpolation","text":"evaluate_unstructured(source::SpringsteelGrid, pts::AbstractMatrix{Float64};\n    vars=nothing, out_of_bounds=:nan) -> Matrix{Float64}\n\nEvaluate the source grid's spectral representation at arbitrary points pts.\n\npts is (N, ndims) where columns are in (i, j, k) order matching the source grid's coordinate system. The source grid must have valid spectral coefficients — call spectralTransform! before evaluation.\n\nArguments\n\nvars: variable names to evaluate (default: all)\nout_of_bounds: :nan (default), :error, or a numeric fill value\n\nReturns\n\nMatrix{Float64} of size (N, nvars) with interpolated function values.\n\nSee also: interpolate_to_grid, regularGridTransform\n\n\n\n\n\n","title":"Springsteel.evaluate_unstructured"},{"category":"function","location":"interpolation/#Springsteel.cartesian_to_cylindrical","page":"Interpolation","text":"cartesian_to_cylindrical(x, y) -> (r, λ)\n\nMap 2D Cartesian (x, y) to cylindrical (r, λ) with λ ∈ [0, 2π).\n\n\n\n\n\n","title":"Springsteel.cartesian_to_cylindrical"},{"category":"function","location":"interpolation/#Springsteel.cylindrical_to_cartesian","page":"Interpolation","text":"cylindrical_to_cartesian(r, λ) -> (x, y)\n\nMap cylindrical (r, λ) to 2D Cartesian (x, y).\n\n\n\n\n\n","title":"Springsteel.cylindrical_to_cartesian"},{"category":"function","location":"interpolation/#Springsteel.cartesian_to_cylindrical_3d","page":"Interpolation","text":"cartesian_to_cylindrical_3d(x, y, z) -> (r, λ, z)\n\nMap 3D Cartesian (x, y, z) to cylindrical (r, λ, z) with λ ∈ [0, 2π).\n\n\n\n\n\n","title":"Springsteel.cartesian_to_cylindrical_3d"},{"category":"function","location":"interpolation/#Springsteel.cylindrical_to_cartesian_3d","page":"Interpolation","text":"cylindrical_to_cartesian_3d(r, λ, z) -> (x, y, z)\n\nMap cylindrical (r, λ, z) to 3D Cartesian (x, y, z).\n\n\n\n\n\n","title":"Springsteel.cylindrical_to_cartesian_3d"},{"category":"function","location":"interpolation/#Springsteel.cartesian_to_spherical","page":"Interpolation","text":"cartesian_to_spherical(x, y, z) -> (R, θ, φ)\n\nMap 3D Cartesian (x, y, z) to spherical (R, θ, φ) where θ is colatitude [0, π] and φ is azimuth [0, 2π).\n\n\n\n\n\n","title":"Springsteel.cartesian_to_spherical"},{"category":"function","location":"interpolation/#Springsteel.spherical_to_cartesian","page":"Interpolation","text":"spherical_to_cartesian(R, θ, φ) -> (x, y, z)\n\nMap spherical (R, θ, φ) to 3D Cartesian (x, y, z). θ is colatitude, φ is azimuth.\n\n\n\n\n\n","title":"Springsteel.spherical_to_cartesian"},{"category":"function","location":"interpolation/#Springsteel.cylindrical_to_spherical","page":"Interpolation","text":"cylindrical_to_spherical(r, λ, z) -> (R, θ, φ)\n\nMap cylindrical (r, λ, z) to spherical (R, θ, φ). Azimuth is preserved: φ = λ.\n\n\n\n\n\n","title":"Springsteel.cylindrical_to_spherical"},{"category":"function","location":"interpolation/#Springsteel.spherical_to_cylindrical","page":"Interpolation","text":"spherical_to_cylindrical(R, θ, φ) -> (r, λ, z)\n\nMap spherical (R, θ, φ) to cylindrical (r, λ, z). Azimuth is preserved: λ = φ.\n\n\n\n\n\n","title":"Springsteel.spherical_to_cylindrical"},{"category":"function","location":"interpolation/#Springsteel.latlon_to_spherical","page":"Interpolation","text":"latlon_to_spherical(lon_deg, lat_deg) -> (θ, λ)\n\nConvert geographic (longitude, latitude) in degrees to spherical (θ colatitude, λ azimuth) in radians. θ ∈ [0, π], λ ∈ [0, 2π).\n\n\n\n\n\n","title":"Springsteel.latlon_to_spherical"},{"category":"function","location":"interpolation/#Springsteel.spherical_to_latlon","page":"Interpolation","text":"spherical_to_latlon(θ, λ) -> (lon_deg, lat_deg)\n\nConvert spherical (θ colatitude, λ azimuth) in radians to geographic (longitude, latitude) in degrees.\n\n\n\n\n\n","title":"Springsteel.spherical_to_latlon"},{"category":"function","location":"interpolation/#Springsteel.set_boundary_values!","page":"Interpolation","text":"set_boundary_values!(grid::R_Grid, side::Symbol, var::String,\n                     u0::Real, u1::Real, u2::Real)\n\nSet inhomogeneous R3X boundary conditions on a 1D R grid.\n\nArguments\n\ngrid: 1D spline grid with R3X boundary condition\nside: :left or :right\nvar: Variable name\nu0, u1, u2: Desired value, first derivative, and second derivative at the boundary\n\nSee also: CubicBSpline.R3X, CubicBSpline.set_ahat_r3x!\n\n\n\n\n\nset_boundary_values!(grid::RR_Grid, side::Symbol, var::String,\n                     u0::AbstractVector, u1::AbstractVector, u2::AbstractVector)\n\nSet inhomogeneous R3X boundary conditions on a 2D RR grid (Spline×Spline).\n\nAccepts physical-space arrays along the j-boundary, transforms them to j-spectral space, and sets per-mode ahat on each i-spline.\n\nArguments\n\ngrid: 2D RR grid with R3X boundary condition on the i-dimension\nside: :left or :right (i-dimension boundary)\nvar: Variable name\nu0, u1, u2: Vectors of length jDim with boundary values along the j-direction\n\nSee also: CubicBSpline.R3X\n\n\n\n\n\nset_boundary_values!(grid::RZ_Grid, side::Symbol, var::String,\n                     u0::AbstractVector, u1::AbstractVector, u2::AbstractVector)\n\nSet inhomogeneous R3X boundary conditions on a 2D RZ grid (Spline×Chebyshev, i-dim only).\n\nAccepts physical-space arrays along the k-boundary, transforms them to Chebyshev spectral space, and sets per-mode ahat on each i-spline.\n\nArguments\n\ngrid: 2D RZ grid with R3X boundary condition on the i-dimension\nside: :left or :right (i-dimension boundary)\nvar: Variable name\nu0, u1, u2: Vectors of length kDim with boundary values along the k-direction\n\nSee also: CubicBSpline.R3X\n\n\n\n\n\n","title":"Springsteel.set_boundary_values!"},{"category":"section","location":"solver/#Solver-Framework","page":"Solver Framework","text":"The Springsteel solver framework assembles and solves linear boundary value problems on any supported grid geometry, using the grid's native basis (B-spline / Fourier / Chebyshev / mixed). It provides two usage styles depending on your needs:\n\na declarative operator algebra DSL ((∂_x^2 + ∂_y^2) * u => :f) that reads like the underlying mathematics and caches a workspace for repeated solves;\na direct kwarg form (SpringsteelProblem(grid; operator=L, rhs=f)) for callers who already have an assembled operator matrix and want a one-shot solve.\n\nBoth styles flow through the same workspace-based solver path — they differ only in how the problem is constructed.","title":"Solver Framework"},{"category":"section","location":"solver/#Quick-start","page":"Solver Framework","text":"using Springsteel\n\ngp = SpringsteelGridParameters(\n    geometry = \"Z\",\n    iMin = 0.0, iMax = 1.0, iDim = 25, b_iDim = 25,\n    vars = Dict(\"u\" => 1, \"f\" => 2),\n    BCL  = Dict(\"u\" => DirichletBC(), \"f\" => NaturalBC()),\n    BCR  = Dict(\"u\" => DirichletBC(), \"f\" => NaturalBC()),\n)\ngrid = createGrid(gp)\n\n# Fill the RHS into its own grid slot\npts = solver_gridpoints(grid, \"u\")\ngrid.physical[:, 2, 1] .= -π^2 .* sin.(π .* pts)\n\n# Build the problem and solve in place; result goes into grid.physical[:, 1, 1]\nu = SpringsteelField(grid, \"u\")\nprob = SpringsteelProblem(grid, ∂ᵢ^2 * u => :f)\nsolve!(prob)\n\nmaximum(abs.(grid.physical[:, 1, 1] .- sin.(π .* pts)))   # ≈ machine ε\n\nThe RHS is kept in its own grid variable (f, slot 2) so that the writeback of u into slot 1 doesn't destroy it. Reusing the same slot for both the unknown and the RHS is legal for a one-shot solve but breaks repeated solve! — see §5 of the Tutorial for a full discussion.\n\nThe Pair-based constructor is the preferred v1.0 style. It builds a workspace at construction (gammaBC fold, BC row detection, cached factorisation) so subsequent solve! calls only pay for the backsolve.","title":"Quick start"},{"category":"section","location":"solver/#Fields","page":"Solver Framework","text":"An unknown variable in an operator expression is represented by a SpringsteelField (aliased as Field). It holds the variable name and its integer slot index in grid.params.vars:\n\nu = Field(grid, \"u\")      # == Field(\"u\", 1) if \"u\" is slot 1\n\nField is the short name used throughout the examples. If it collides with a type from another package loaded in the same session, use the qualified SpringsteelField directly.","title":"Fields"},{"category":"section","location":"solver/#Operator-algebra","page":"Solver Framework","text":"Differential operators are built from derivative atoms combined with scalar / vector coefficients and Julia's +, -, *, ^ operators. The result is an OperatorExpr that multiplies by a Field to produce a TypedOperator — the object that lives on the left of the => in the Pair-based constructor.\n\nL = ∂_x^2 + ∂_y^2                     # 2D Cartesian Laplacian\nL = ∂_r^2 + (1/r) * ∂_r                # cylindrical radial Laplacian\nL = α * ∂_x^2 + β * ∂_x + γ            # varying coefficients","title":"Operator algebra"},{"category":"section","location":"solver/#Derivative-atoms","page":"Solver Framework","text":"The derivative atoms come in two flavours — generic (axis-indexed) and physical (named). The physical names are resolved against the grid's geometry at lowering time, so the same symbol means different things on a Cartesian vs cylindrical vs spherical grid.\n\nGeneric atoms act directly on the grid's i / j / k axes. Use these in geometry-agnostic code:\n\nSymbol ASCII Axis\n∂ᵢ d_i :i\n∂ⱼ d_j :j\n∂ₖ d_k :k\n\nPhysical atoms resolve per geometry:\n\nSymbol ASCII Cartesian Cylindrical Spherical\n∂_x d_x :i — —\n∂_y d_y :j — —\n∂_z d_z :k :k :k (radial)\n∂_r d_r — :i —\n∂_θ d_theta — :j :i (colatitude)\n∂_λ d_lambda — :j :j (azimuth)\n\nPowers build second derivatives: ∂_x^2, ∂_r^2, etc. Products build mixed partials: ∂_x * ∂_y is ∂²/∂x∂y.","title":"Derivative atoms"},{"category":"section","location":"solver/#Coefficients","page":"Solver Framework","text":"Any term can be scaled by a coefficient. Supported coefficient types:\n\nType Interpretation\nNumber scalar\nVector{Float64} pre-built spatially-varying coefficient\nFunction evaluated against solver_gridpoints(grid)\nSymbol pulled from grid.physical[:, var, 1]\nNothing unit coefficient\n\n# Scalar\nprob = SpringsteelProblem(grid, 0.5 * ∂_x^2 * u => :f)\n\n# Vector (must match the grid's physical size)\nκ = compute_kappa(grid)\nprob = SpringsteelProblem(grid, κ * ∂_x^2 * u => :f)\n\n# Function — evaluated at gridpoints at construction\nprob = SpringsteelProblem(grid, (x -> 1 + x[1]^2) * ∂_x^2 * u => :f)\n\n# Symbol — reads the named grid variable at construction\nprob = SpringsteelProblem(grid, :κ * ∂_x^2 * u => :f)","title":"Coefficients"},{"category":"section","location":"solver/#Right-hand-sides","page":"Solver Framework","text":"The RHS in L * u => rhs can be:\n\na Symbol — the name of a grid variable; the workspace reads grid.physical[:, rhs_idx, 1] at each solve. Ideal for time-stepping loops.\na Vector{Float64} — a literal RHS, snapshotted once at construction.\na Real — a constant, broadcast to every physical gridpoint.","title":"Right-hand sides"},{"category":"section","location":"solver/#Problem-construction","page":"Solver Framework","text":"Two constructors are available. They produce the same struct and flow through the same solver path.","title":"Problem construction"},{"category":"section","location":"solver/#Pair-based-(preferred)","page":"Solver Framework","text":"SpringsteelProblem(grid, L * u => rhs;\n                   backend = :auto,\n                   preconditioner = :default)\n\nBuild a problem from an operator expression. backend can be a Symbol (:auto, :dense, :sparse, :krylov) or a backend instance. The default :auto picks LocalLinearBackend for pure-Chebyshev grids (where structural sparsity is low) and SparseLinearBackend everywhere else.\n\nprob = SpringsteelProblem(grid, ∂_x^2 * u => :f)                  # auto\nprob = SpringsteelProblem(grid, ∂_x^2 * u => :f; backend=:krylov)","title":"Pair-based (preferred)"},{"category":"section","location":"solver/#Kwarg-form-(direct-operator/rhs)","page":"Solver Framework","text":"SpringsteelProblem(grid;\n                   operator = L::Matrix{Float64},\n                   rhs      = f::Vector{Float64},\n                   parameters = Dict{String,Any}(),\n                   backend = LocalLinearBackend())\n\nUse this when you've assembled the operator yourself — for example via assemble_from_equation or through custom code that doesn't fit the operator algebra DSL. Both operator and rhs are required for linear backends; the workspace is built at construction time.\n\nL = assemble_from_equation(grid, \"u\"; d_ii=1.0)\npts = solver_gridpoints(grid, \"u\")\nf = -π^2 .* sin.(π .* pts)\nprob = SpringsteelProblem(grid; operator=L, rhs=f)\nsol  = solve(prob)","title":"Kwarg form (direct operator/rhs)"},{"category":"section","location":"solver/#Solving","page":"Solver Framework","text":"Two entry points, sharing one implementation:\n\nsolve!(prob) mutates grid.physical[:, var_idx, 1] in place and returns prob. This is the fast path — use it in time-stepping loops where you update the RHS grid variable between calls.\nsolve(prob) runs the same computation but writes the result into a fresh independent SpringsteelGrid narrowed to just the solved field(s), then returns a SpringsteelSolution owning that grid. The grid originally passed to SpringsteelProblem is untouched.\n\nBoth entry points work with every linear backend (LocalLinearBackend, SparseLinearBackend, KrylovLinearBackend). For OptimizationBackend, only solve is defined (via the Optimization.jl package extension).\n\nsol.physical and sol.coefficients are views into the owned solution grid — no data is duplicated inside the Solution itself. sol.grid can be passed directly to downstream operations like gridTransform!, evaluate_unstructured, regularGridTransform, or write_grid.","title":"Solving"},{"category":"section","location":"solver/#Backends","page":"Solver Framework","text":"","title":"Backends"},{"category":"section","location":"solver/#LocalLinearBackend-—-dense-LU","page":"Solver Framework","text":"Best for small, dense systems — typically pure-Chebyshev grids where the assembled operator has little exploitable structure. The default for such grids under backend = :auto.","title":"LocalLinearBackend — dense LU"},{"category":"section","location":"solver/#SparseLinearBackend-—-sparse-LU","page":"Solver Framework","text":"The default for any grid involving a B-spline or Fourier dimension. B-spline/Fourier/Chebyshev tensor products produce structurally sparse operators; sparse LU keeps the cached factor small as num_cells grows and is typically an order of magnitude faster than dense LU at moderate sizes.","title":"SparseLinearBackend — sparse LU"},{"category":"section","location":"solver/#KrylovLinearBackend-—-iterative","page":"Solver Framework","text":"Uses Krylov.gmres for square systems and Krylov.lsmr for rectangular ones (which arise after gammaBC folding). Suitable when the operator is ill-conditioned or when memory for a direct factor is a concern. The backend accepts a preconditioner:\n\nprob = SpringsteelProblem(grid, L * u => :f;\n                           backend = :krylov,\n                           preconditioner = :diag)\n\n# Or pass an explicit inverse operator:\nMinv = compute_preconditioner(grid)\nprob = SpringsteelProblem(grid, L * u => :f;\n                           backend = KrylovLinearBackend(Minv))\n\nPreconditioner options: nothing (no preconditioner), :diag (diagonal of the assembled operator), or an explicit left-inverse.","title":"KrylovLinearBackend — iterative"},{"category":"section","location":"solver/#AbstractLinearBackend","page":"Solver Framework","text":"Supertype for the three linear backends above. solve and solve! dispatch on {<:AbstractLinearBackend} so that all three share one implementation and OptimizationBackend cleanly hits its own method in the extension.","title":"AbstractLinearBackend"},{"category":"section","location":"solver/#OptimizationBackend","page":"Solver Framework","text":"Minimises a user-supplied cost functional J(u, p) using Optimization.jl. Requires using Optimization to be loaded in the same session to pick up the package extension method.\n\nusing Springsteel, Optimization\n\ncost(phys, params) = sum((phys .- target).^2)\nprob = SpringsteelProblem(grid;\n                           cost    = cost,\n                           backend = OptimizationBackend(:LBFGS))\nsol = solve(prob)","title":"OptimizationBackend"},{"category":"section","location":"solver/#Block-multi-variable-systems","page":"Solver Framework","text":"The Pair-based constructor accepts a Vector of equations for coupled multi-variable problems. Each element is one equation L_i => RHS_i, where L_i is a sum of TypedTerms that bind to one or more unknowns. The number of equations must equal the number of unique unknowns.\n\nu = Field(grid, \"u\")\nv = Field(grid, \"v\")\n\neqs = [\n    ∂_x * u + ∂_y * v => :f1,\n    ∂_x * v - ∂_y * u => :f2,\n]\nprob = SpringsteelProblem(grid, eqs; backend = :sparse)\nsolve!(prob)\n\nUnder the hood prob.workspace is a BlockLinearWorkspace that assembles the full block operator and factorises it once. Each call to solve! refreshes the per-equation RHS slices, overwrites BC rows, and writes back per-variable into the grid.\n\nBlock systems do not currently support R3X inhomogeneous BCs; use the single-variable path for problems that need the ahat correction.","title":"Block multi-variable systems"},{"category":"section","location":"solver/#Boundary-conditions","page":"Solver Framework","text":"BCs are per-variable and per-direction, configured on the grid via SpringsteelGridParameters. The solver automatically applies them at problem construction time:\n\nCubicBSpline — the Γ_BC projection matrix folds the boundary constraint into the operator, reducing the system size by the BC rank.\nChebyshev — the boundary row of the assembled operator is replaced with an evaluation or derivative constraint row; the RHS at that row is overwritten with the BC value.\nFourier — periodic by construction; no modification.\n\nFor the full BC specification syntax see Boundary Conditions.","title":"Boundary conditions"},{"category":"section","location":"solver/#Operator-assembly-(low-level)","page":"Solver Framework","text":"If you need to build an operator matrix outside the DSL — for example to inspect it, to combine it with an external term, or to use the kwarg constructor — assemble_from_equation is the ergonomic entry point:\n\nassemble_from_equation accepts keyword arguments for every supported derivative term (d0, d_i, d_ii, d_j, d_jj, d_k, d_kk, plus mixed partials) and returns the assembled Matrix{Float64} with gammaBC folding applied. operator_matrix gives you a single-dimension 1D basis matrix (evaluation, first, or second derivative) at the lowest level.","title":"Operator assembly (low level)"},{"category":"section","location":"solver/#Gridpoints","page":"Solver Framework","text":"Returns the physical gridpoints at which the RHS and solution live — the same layout as grid.physical[:, var_idx, 1]. Use this when you need to evaluate an analytic expression for an inhomogeneous RHS or boundary.","title":"Gridpoints"},{"category":"section","location":"solver/#See-also","page":"Solver Framework","text":"Boundary Conditions — BC type system and per-variable Dict spec\nSpringsteelGrid — grid construction\nTutorial — end-to-end examples\nCubicBSpline / Fourier / Chebyshev — underlying basis modules and their derivative matrices","title":"See also"},{"category":"type","location":"solver/#Springsteel.SpringsteelField","page":"Solver Framework","text":"SpringsteelField(grid, var_name::AbstractString)\nSpringsteelField(var_name::AbstractString, var_idx::Int)\n\nHandle for a grid variable that can stand in as the unknown in operator algebra (L * u => rhs).\n\nHolds only the variable's name and its integer slot index — no grid reference. The primary constructor takes a grid to resolve the slot from grid.params.vars, but the resulting SpringsteelField is independent of that grid and can be reused across grids with the same variable schema.\n\nBoundary conditions come from the grid's per-variable BC spec at problem construction time, not from the SpringsteelField itself.\n\nnote: Field alias\nField is exported as a convenience alias for SpringsteelField and appears throughout the examples (u = Field(grid, \"u\")). Because Field is a short, generic name it can collide with definitions from other packages loaded in the same session. If you hit an ambiguity warning or method error, use the qualified SpringsteelField directly — it behaves identically.\n\n\n\n\n\n","title":"Springsteel.SpringsteelField"},{"category":"constant","location":"solver/#Springsteel.∂ᵢ","page":"Solver Framework","text":"Derivative atoms\n\nThese constants are the building blocks of the operator algebra DSL. Each is a one-order DerivMono that can be multiplied by a Field, combined with +/-/*, and raised to integer powers to build OperatorExprs:\n\nL = ∂_x^2 + ∂_y^2         # 2D Laplacian on a Cartesian grid\nL = ∂_r^2 + (1/r)*∂_r     # cylindrical radial Laplacian\nprob = SpringsteelProblem(grid, L * u => :f)\n\nGeneric (axis-indexed) atoms\n\nAct directly on the grid's i/j/k axes regardless of geometry. Use these when your code is geometry-agnostic or when you want to avoid the geometry-mapping layer.\n\nSymbol ASCII alias Axis\n∂ᵢ d_i :i\n∂ⱼ d_j :j\n∂ₖ d_k :k\n\nPhysical (named) atoms\n\nResolved against grid.params.geometry at lowering time. Using a physical alias on a geometry that doesn't define it raises an error.\n\nSymbol ASCII alias Cartesian Cylindrical Spherical\n∂_x d_x :i (x) — —\n∂_y d_y :j (y) — —\n∂_z d_z :k (z) :k (z) :k (radial)\n∂_r d_r — :i (r) —\n∂_θ d_theta — :j (θ=λ) :i (colatitude)\n∂_λ d_lambda — :j (λ) :j (azimuth)\n\nSecond derivatives are built with ^: ∂_x^2, ∂_r^2, etc. Mixed partials multiply: ∂_x * ∂_y is ∂²/∂x∂y.\n\n\n\n\n\n","title":"Springsteel.∂ᵢ"},{"category":"type","location":"solver/#Springsteel.DerivMono","page":"Solver Framework","text":"DerivMono\n\nA product of per-axis derivative orders, e.g. ∂ᵢ² or ∂ᵢ ∂ⱼ.\n\nAxes are stored as Symbol → Int pairs. Symbols may be generic (:i, :j, :k) or physical (:r, :z, :θ, :λ, :x, :y); physical names are translated to the corresponding generic axis at lowering time based on the grid's geometry, and an error is raised if the mapping is invalid.\n\n\n\n\n\n","title":"Springsteel.DerivMono"},{"category":"type","location":"solver/#Springsteel.ScaledMono","page":"Solver Framework","text":"ScaledMono\n\nA single term in an operator expression: coefficient * DerivMono.\n\nThe coefficient may be:\n\nNothing (treated as 1.0 at lowering)\nFloat64 / Int (scalar)\nVector{Float64} (spatially varying, pre-built)\n\nS5 of the solver refactor will extend this to accept Function and Symbol (grid-variable lookup) via _resolve_coeff.\n\n\n\n\n\n","title":"Springsteel.ScaledMono"},{"category":"type","location":"solver/#Springsteel.OperatorExpr","page":"Solver Framework","text":"OperatorExpr\n\nSum of ScaledMonos. This is the object users hold after combining derivative atoms with +, -, *, ^. Lowers to Vector{OperatorTerm} via _lower.\n\n\n\n\n\n","title":"Springsteel.OperatorExpr"},{"category":"type","location":"solver/#Springsteel.TypedOperator","page":"Solver Framework","text":"TypedOperator(terms)\n\nA sum of TypedTerms — the left-hand side of one equation. Produced by multiplying an operator-algebra expression by a SpringsteelField and then combining via +/-. For single-variable problems it holds one term; for block systems each term can bind to a different field.\n\n\n\n\n\n","title":"Springsteel.TypedOperator"},{"category":"type","location":"solver/#Springsteel.SpringsteelProblem","page":"Solver Framework","text":"SpringsteelProblem{B <: AbstractSolverBackend}\n\nBundled grid + cached solver state for a linear or optimisation problem.\n\nFor linear-backend problems (the common path), workspace holds the compiled operator, gammaBC fold, BC row indices, factorisation, and solve scratch buffers — built once at construction time and reused by every subsequent solve / solve!. For OptimizationBackend, workspace is nothing and cost / parameters carry the problem description.\n\nFields\n\ngrid: the discretised domain\nbackend: solver backend sentinel\nworkspace: cached linear-solve state (nothing for Optimization)\ncost: cost functional J(u p) (Optimization only)\nparameters: kwargs bag passed through to Optimization cost callbacks\n\nConstructors\n\n# v1.0 operator-algebra form (preferred)\nu = Field(grid, \"u\")\nprob = SpringsteelProblem(grid, (∂_x^2 + ∂_y^2) * u => :rhs_var)\n\n# Legacy kwarg form — user-assembled operator and RHS\nprob = SpringsteelProblem(grid; operator=L, rhs=f)\n\nSee also: solve, solve!, SpringsteelSolution, assemble_operator\n\n\n\n\n\n","title":"Springsteel.SpringsteelProblem"},{"category":"function","location":"solver/#Springsteel.solve!","page":"Solver Framework","text":"solve!(prob::SpringsteelProblem) -> SpringsteelProblem\n\nStateful solve that reuses prob.workspace. Refreshes each RHS (from the grid if given as a Symbol), applies cached ahat/BC corrections, backsolves against the cached factorisation, and writes each variable's physical-space solution into prob.grid.physical[:, var_idx, 1]. Returns prob.\n\n\n\n\n\n","title":"Springsteel.solve!"},{"category":"function","location":"solver/#Springsteel.solve","page":"Solver Framework","text":"solve(prob::SpringsteelProblem) -> SpringsteelSolution\n\nNon-mutating twin of solve!. Runs the cached workspace compute path, writes the result into a fresh SpringsteelGrid narrowed to just the solved field(s), and returns a SpringsteelSolution that owns that grid. The grid originally passed to SpringsteelProblem is left untouched.\n\nWorks on any linear backend that solve! supports (LocalLinearBackend, SparseLinearBackend, KrylovLinearBackend). For OptimizationBackend a separate method in the Optimization.jl package extension applies.\n\nUse solve when you want the solution as a return value bundled with its own grid — for single-shot solves, for piping into downstream operations like gridTransform! / evaluate_unstructured / write_grid, or when preserving the original grid state matters. Use solve! when you're iterating in a time-stepping loop and want the fastest path with in-place updates to grid.physical.\n\nReturns\n\nA SpringsteelSolution whose physical and coefficients are views into the owned solution grid.\n\nSee also: solve!, SpringsteelProblem, SpringsteelSolution\n\n\n\n\n\n","title":"Springsteel.solve"},{"category":"type","location":"solver/#Springsteel.SpringsteelSolution","page":"Solver Framework","text":"SpringsteelSolution\n\nResult of a non-mutating solve.\n\nA SpringsteelSolution owns an independent SpringsteelGrid that is narrowed to just the solved field(s). The solver writes its output into this grid, leaving the grid that was passed to solve untouched.\n\nFields\n\ngrid: narrowed solution grid — an independent copy that only holds the solved variable(s). Can be passed directly to gridTransform!, evaluate_unstructured, write_grid, etc.\nvar_idx: slot of the primary solved variable in grid.physical (always 1 for single-field problems; for block systems, points at the first field — use grid.physical[:, idx, 1] with the block's own var dict to read other unknowns).\nconverged: whether the solver converged.\n\nProperty forwards\n\nFor backward compatibility and ergonomic access:\n\nsol.physical → @view grid.physical[:, var_idx, 1]\nsol.coefficients → @view grid.spectral[:, var_idx]\n\nThese are views into the owned solution grid, so no data is duplicated.\n\nSee also: solve, SpringsteelProblem\n\n\n\n\n\n","title":"Springsteel.SpringsteelSolution"},{"category":"type","location":"solver/#Springsteel.LocalLinearBackend","page":"Solver Framework","text":"LocalLinearBackend <: AbstractLinearBackend\n\nDense LU factorisation backend. Best for small, dense systems — typically pure-Chebyshev grids where structural sparsity is limited.\n\n\n\n\n\n","title":"Springsteel.LocalLinearBackend"},{"category":"type","location":"solver/#Springsteel.SparseLinearBackend","page":"Solver Framework","text":"SparseLinearBackend <: AbstractLinearBackend\n\nSparse LU factorisation backend. The assembled operator is converted to a SparseMatrixCSC and factorised via SparseArrays.lu, so the cached factor stays small for the structurally sparse operators that come out of B-spline / Fourier / Chebyshev tensor products. The solve! hot path is identical to the dense backend — only the factorisation type differs.\n\n\n\n\n\n","title":"Springsteel.SparseLinearBackend"},{"category":"type","location":"solver/#Springsteel.KrylovLinearBackend","page":"Solver Framework","text":"KrylovLinearBackend(; preconditioner=nothing) <: AbstractLinearBackend\n\nKrylov iterative backend. Uses Krylov.gmres for square systems and Krylov.lsmr for rectangular ones. The optional preconditioner is passed through as the M (left) kwarg to Krylov.\n\nv1.0 note: this backend still builds the assembled sparse operator (same as SparseLinearBackend) — the truly-matrix-free path that avoids the kron materialisation is deferred to a future phase. The immediate win here is the iterative-solver option and preconditioner plumbing for ill-conditioned problems.\n\n\n\n\n\n","title":"Springsteel.KrylovLinearBackend"},{"category":"type","location":"solver/#Springsteel.AbstractSolverBackend","page":"Solver Framework","text":"AbstractSolverBackend\n\nAbstract supertype for solver backend sentinel types. Concrete subtypes select the algorithm used by solve / solve!.\n\nSee also: AbstractLinearBackend, OptimizationBackend\n\n\n\n\n\n","title":"Springsteel.AbstractSolverBackend"},{"category":"type","location":"solver/#Springsteel.AbstractLinearBackend","page":"Solver Framework","text":"AbstractLinearBackend <: AbstractSolverBackend\n\nSupertype for backends that solve linear systems mathbfLmathbfa=mathbff through the cached workspace path. Current concrete subtypes: LocalLinearBackend, SparseLinearBackend, KrylovLinearBackend.\n\nsolve and solve! share a single implementation across all AbstractLinearBackends — they differ only in the factorisation strategy and (for Krylov) the preconditioner plumbing.\n\n\n\n\n\n","title":"Springsteel.AbstractLinearBackend"},{"category":"type","location":"solver/#Springsteel.OptimizationBackend","page":"Solver Framework","text":"OptimizationBackend <: AbstractSolverBackend\n\nBackend sentinel for the Optimization.jl extension. Requires the Optimization package to be loaded; the actual solve dispatch lives in ext/SpringsteelOptimizationExt.jl.\n\nFields\n\nalgorithm::Symbol: Optimisation algorithm (e.g. :LBFGS, :NelderMead)\noptions::Dict{String, Any}: Algorithm-specific options\n\nSee also: solve, SpringsteelProblem\n\n\n\n\n\n","title":"Springsteel.OptimizationBackend"},{"category":"function","location":"solver/#Springsteel.assemble_from_equation","page":"Solver Framework","text":"assemble_from_equation(grid::AbstractGrid, var::String=\"\";\n    d0=nothing, d_i=nothing, d_j=nothing, d_k=nothing,\n    d_ii=nothing, d_jj=nothing, d_kk=nothing,\n    d_ij=nothing, d_ik=nothing, d_jk=nothing,\n    d_iij=nothing, d_iik=nothing, d_ijj=nothing,\n    d_jjk=nothing, d_ikk=nothing, d_jkk=nothing,\n    d_ijk=nothing,\n    d_iijj=nothing, d_iikk=nothing, d_jjkk=nothing,\n    d_iijk=nothing, d_ijjk=nothing, d_ijkk=nothing,\n    d_iijjk=nothing, d_iijkk=nothing, d_ijjkk=nothing,\n    d_iijjkk=nothing) -> Matrix{Float64}\n\nBuild an operator matrix from an equation-oriented specification. Non-nothing keyword arguments are converted to OperatorTerm objects and assembled via assemble_operator.\n\nEach keyword encodes a derivative monomial where letter repetitions denote derivative order in that dimension: d_ijj → partial^3  partial ipartial j^2 (i.e. OperatorTerm(1, 2, 0, c)).  All 27 combinations of orders 0–2 in three dimensions are available.\n\nKeywords\n\nPure derivatives (single dimension, order 0–2)\n\nd0: identity (0th derivative)\nd_i, d_j, d_k: first derivatives\nd_ii, d_jj, d_kk: second derivatives\n\nMixed 2nd-order (two dimensions, each order 1)\n\nd_ij: partial^2partial ipartial j\nd_ik: partial^2partial ipartial k\nd_jk: partial^2partial jpartial k\n\nMixed 3rd-order\n\nd_iij: partial^3partial i^2partial j\nd_iik: partial^3partial i^2partial k\nd_ijj: partial^3partial ipartial j^2\nd_jjk: partial^3partial j^2partial k\nd_ikk: partial^3partial ipartial k^2\nd_jkk: partial^3partial jpartial k^2\nd_ijk: partial^3partial ipartial jpartial k\n\nMixed 4th-order\n\nd_iijj: partial^4partial i^2partial j^2\nd_iikk: partial^4partial i^2partial k^2\nd_jjkk: partial^4partial j^2partial k^2\nd_iijk: partial^4partial i^2partial jpartial k\nd_ijjk: partial^4partial ipartial j^2partial k\nd_ijkk: partial^4partial ipartial jpartial k^2\n\nMixed 5th-order\n\nd_iijjk: partial^5partial i^2partial j^2partial k\nd_iijkk: partial^5partial i^2partial jpartial k^2\nd_ijjkk: partial^5partial ipartial j^2partial k^2\n\nMixed 6th-order\n\nd_iijjkk: partial^6partial i^2partial j^2partial k^2\n\nEach coefficient can be a Float64 (scalar), Vector{Float64} (spatially varying), or nothing (term not included).\n\nnote: Prefer the Pair-based `SpringsteelProblem` constructor\nAs of the v1.0 solver refactor, new code should build operators via the operator algebra DSL and pass them to SpringsteelProblem with a Pair:u = Field(grid, \"u\")\nprob = SpringsteelProblem(grid, (∂ᵢ^2 + ∂ⱼ^2) * u => :f_rhs)\nsolve!(prob)That path caches a stateful workspace (factorisation, BC rows, eval matrix, scratch buffers) so repeated solve! calls are allocation-free, and gives you backend choice (:dense / :sparse / :krylov) plus Function/Symbol coefficient resolution.assemble_from_equation is kept as a thin compatibility shim that internally builds the same DSL expression and delegates to _lower, so the kwarg form and the DSL form always produce identical term vectors.\n\nExample\n\n# 1D Poisson: u'' = f\nL = assemble_from_equation(grid; d_ii=1.0)\n\n# 2D Laplacian: ∂²u/∂r² + ∂²u/∂z²\nL = assemble_from_equation(grid; d_ii=1.0, d_kk=1.0)\n\n# General 2nd-order ODE: au'' + bu' + cu = f\nL = assemble_from_equation(grid; d_ii=a, d_i=b, d0=c)\n\n# 2D with mixed derivative: ∂²u/∂i² + 2∂²u/∂i∂j + ∂²u/∂j²\nL = assemble_from_equation(grid; d_ii=1.0, d_ij=2.0, d_jj=1.0)\n\n# 3D mixed: ∂³u/∂i∂j²\nL = assemble_from_equation(grid; d_ijj=1.0)\n\nSee also: assemble_operator, OperatorTerm\n\n\n\n\n\n","title":"Springsteel.assemble_from_equation"},{"category":"function","location":"solver/#Springsteel.operator_matrix","page":"Solver Framework","text":"operator_matrix(grid::AbstractGrid, dim::Symbol, order::Int, var::String=\"\") -> Matrix{Float64}\n\nExtract a 1D operator matrix for dimension dim (:i, :j, or :k) at derivative order (0 = evaluation, 1 = first derivative, 2 = second derivative).\n\nDispatches to the appropriate basis module's matrix function based on the grid's basis type for that dimension.\n\nArguments\n\ngrid::AbstractGrid: The grid whose basis objects provide the matrix\ndim::Symbol: Which dimension (:i, :j, :k)\norder::Int: Derivative order (0, 1, or 2)\nvar::String: Variable name (default: first variable)\n\nReturns\n\nMatrix{Float64}: Size (physical_dim, spectral_dim) for the requested dimension\n\nSee also: assemble_operator, OperatorTerm\n\n\n\n\n\n","title":"Springsteel.operator_matrix"},{"category":"function","location":"solver/#Springsteel.assemble_operator","page":"Solver Framework","text":"assemble_operator(grid::AbstractGrid, terms::Vector{OperatorTerm}, var::String=\"\") -> Matrix{Float64}\n\nBuild a multi-dimensional operator matrix from Kronecker products of 1D matrices.\n\nFor each OperatorTerm, the 1D matrices for active dimensions are combined via kron() and optionally scaled by the term's coefficient. The total operator is the sum of all terms.\n\nArguments\n\ngrid::AbstractGrid: The grid providing basis objects\nterms::Vector{OperatorTerm}: List of operator terms\nvar::String: Variable name (default: first variable)\n\nReturns\n\nMatrix{Float64}: Assembled operator of size (N_phys, N_spec) where N_phys and N_spec are the total physical and spectral dimensions\n\nSee also: OperatorTerm, operator_matrix, assemble_from_equation\n\n\n\n\n\n","title":"Springsteel.assemble_operator"},{"category":"type","location":"solver/#Springsteel.OperatorTerm","page":"Solver Framework","text":"OperatorTerm\n\nDescribes a single term in a multi-dimensional linear differential operator.\n\nEach term specifies derivative orders in the i, j, and k dimensions, plus an optional scalar or spatially varying coefficient.\n\nFields\n\ni_order::Int: Derivative order in i-dimension (0, 1, or 2)\nj_order::Int: Derivative order in j-dimension (0, 1, or 2)\nk_order::Int: Derivative order in k-dimension (0, 1, or 2)\ncoefficient::Union{Float64, Vector{Float64}, Nothing}: Scalar multiplier, diagonal (spatially varying) coefficient vector, or nothing (treated as 1.0)\n\nExample\n\n# Laplacian term: ∂²/∂i² with unit coefficient\nOperatorTerm(2, 0, 0, 1.0)\n\n# Variable-coefficient term: a(x) ∂²/∂k²\nOperatorTerm(0, 0, 2, a_values)\n\nSee also: assemble_operator, assemble_from_equation\n\n\n\n\n\n","title":"Springsteel.OperatorTerm"},{"category":"function","location":"solver/#Springsteel.solver_gridpoints","page":"Solver Framework","text":"solver_gridpoints(grid::AbstractGrid, var::String=\"\") -> Vector{Float64}\n\nExtract the physical gridpoints from a grid's i-dimension basis. Works for any grid type including those without a getGridpoints method.\n\nFor 1D grids returns the i-dimension mish points. For multi-D grids returns the Kronecker-product physical point locations for the i-dimension.\n\n\n\n\n\n","title":"Springsteel.solver_gridpoints"},{"category":"section","location":"boundary_conditions/#Boundary-Conditions","page":"Boundary Conditions","text":"Springsteel separates what a boundary condition means (prescribe a value, a derivative, periodicity, a Robin combination) from how a particular basis enforces it (spline Γ_BC projection, Chebyshev row replacement, Fourier periodicity). User code writes a single basis-agnostic BoundaryConditions spec; the grid factory translates it to the basis-specific internal form at construction time.","title":"Boundary Conditions"},{"category":"section","location":"boundary_conditions/#The-BoundaryConditions-type","page":"Boundary Conditions","text":"BoundaryConditions has four constraint slots plus a periodic flag:\n\nSlot Constraint Meaning\nu u(x₀) = v Dirichlet value\ndu u'(x₀) = v Neumann (first derivative)\nd2u u''(x₀) = v Second derivative\nrobin αu + βu' = γ Three-coefficient linear combination\nperiodic — Periodic boundary (Fourier-only)\n\nnothing in a constraint slot means \"unconstrained\" (natural boundary). Robin is mutually exclusive with the first three slots; periodic is mutually exclusive with everything else. These rules are enforced by the inner constructor.","title":"The BoundaryConditions type"},{"category":"section","location":"boundary_conditions/#Convenience-constructors","page":"Boundary Conditions","text":"Use the named constructors rather than calling BoundaryConditions(...) directly — they cover every supported case and fail fast if you try to combine constraints that don't make sense together.","title":"Convenience constructors"},{"category":"section","location":"boundary_conditions/#No-constraint","page":"Boundary Conditions","text":"# A free-boundary spline — no value, derivative, or Robin constraint\nBCL = Dict(\"u\" => NaturalBC())","title":"No constraint"},{"category":"section","location":"boundary_conditions/#Dirichlet-—-value","page":"Boundary Conditions","text":"# u(x_left) = 0 (default)\nBCL = Dict(\"u\" => DirichletBC())\n# u(x_right) = 273.15 (inhomogeneous)\nBCR = Dict(\"u\" => DirichletBC(273.15))","title":"Dirichlet — value"},{"category":"section","location":"boundary_conditions/#Neumann-—-first-derivative","page":"Boundary Conditions","text":"# Zero-flux (symmetric) wall\nBCL = Dict(\"u\" => NeumannBC())\n# Prescribed gradient\nBCR = Dict(\"u\" => NeumannBC(-1.5))","title":"Neumann — first derivative"},{"category":"section","location":"boundary_conditions/#Second-derivative","page":"Boundary Conditions","text":"","title":"Second derivative"},{"category":"section","location":"boundary_conditions/#Robin-—-linear-combination","page":"Boundary Conditions","text":"# Newton cooling: αu + βu' = γ\nBCR = Dict(\"T\" => RobinBC(1.0, 0.2, 293.15))","title":"Robin — linear combination"},{"category":"section","location":"boundary_conditions/#Periodic","page":"Boundary Conditions","text":"Periodic is the only legal BC for Fourier dimensions and is automatic on those dimensions — you rarely need to write it explicitly. The factory will reject a Fourier dimension paired with a non-periodic BoundaryConditions.","title":"Periodic"},{"category":"section","location":"boundary_conditions/#Compound-/-aliased-forms","page":"Boundary Conditions","text":"ExponentialBC(λ) implements the Ooyama (2002) outward-decay condition u = λ u' as a Robin constraint. SymmetricBC is a zero-Neumann alias; AntisymmetricBC enforces u = u'' = 0 (rank 2). ZerosBC is a rank-3 homogeneous clamp used at grid interiors that should never carry energy. FixedBC is the R3X interface BC — it uses NaN sentinels to activate the inhomogeneous ahat code path so that update_interface! can fill in runtime values from a neighbour patch.","title":"Compound / aliased forms"},{"category":"section","location":"boundary_conditions/#Specifying-BCs-on-a-grid","page":"Boundary Conditions","text":"Per-variable dictionaries are the spec format. Each direction has its own dict keyed by variable name, with an optional \"default\" fallback:\n\nusing Springsteel\n\ngp = SpringsteelGridParameters(\n    geometry  = \"RZ\",\n    iMin      = 0.0, iMax = 1.0, num_cells = 10,\n    kMin      = 0.0, kMax = 1.0, kDim = 16,\n    vars      = Dict(\"u\" => 1, \"v\" => 2),\n    BCL       = Dict(\"u\" => DirichletBC(),     \"v\" => NaturalBC()),\n    BCR       = Dict(\"u\" => DirichletBC(),     \"v\" => NaturalBC()),\n    BCB       = Dict(\"u\" => NeumannBC(),       \"v\" => DirichletBC()),\n    BCT       = Dict(\"u\" => NeumannBC(),       \"v\" => DirichletBC(1.0)),\n)\ngrid = createGrid(gp)\n\nDirection keys:\n\nKey Side Active for\nBCL i-min (left) spline i-axis\nBCR i-max (right) spline i-axis\nBCD j-min (down) spline/Fourier j-axis\nBCU j-max (up) spline/Fourier j-axis\nBCB k-min (bottom) Chebyshev/spline k-axis\nBCT k-max (top) Chebyshev/spline k-axis\n\nIf every variable shares the same BC, use \"default\" instead of listing each name:\n\nBCL = Dict(\"default\" => DirichletBC())\n\nVariables without an explicit entry fall back to \"default\"; variables with an entry override the default.","title":"Specifying BCs on a grid"},{"category":"section","location":"boundary_conditions/#Basis-specific-behaviour","page":"Boundary Conditions","text":"The factory converts each BoundaryConditions to the internal form required by the basis on that dimension:\n\nCubicBSpline — BCs become a Γ_BC projection matrix (Ooyama 2002) that folds the boundary constraints directly into the operator. Rank k BCs reduce the system size by k spectral coefficients per boundary. See the CubicBSpline page for the underlying representation.\nChebyshev — BCs are applied as coefficient corrections during the forward transform via a per-column gammaBC stripe (vector for Dirichlet, full N times N matrix for Neumann using the Wang et al. (1993) global coefficient method). When the solver framework assembles an explicit operator, it additionally replaces the boundary rows of mathbfL with evaluation or first-derivative constraint rows at the boundary CGL nodes. Currently only homogeneous Dirichlet (R1T0 / DirichletBC()) and homogeneous Neumann (R1T1 / NeumannBC()) are supported on Chebyshev dimensions; higher-rank and inhomogeneous BCs throw at grid construction. See the Chebyshev page for the exact gammaBC formulation.\nFourier — PeriodicBC is automatic; any non-periodic BoundaryConditions on a Fourier dimension is a configuration error.\n\nThe conversion is per-dimension and happens once at createGrid time, so there is no per-transform cost for BC handling.","title":"Basis-specific behaviour"},{"category":"section","location":"boundary_conditions/#Legacy-Dict-boundary-specs","page":"Boundary Conditions","text":"Pre-v0.3 code specified BCs as raw Dicts from the submodules:\n\nBCL = Dict(\"u\" => CubicBSpline.R0)     # legacy natural BC\nBCR = Dict(\"u\" => CubicBSpline.R1T0)   # legacy Dirichlet\nBCB = Dict(\"u\" => Chebyshev.R1T0)      # legacy Chebyshev Dirichlet\n\nThese still work in v1.0 — the factory passes raw Dicts through unchanged and the downstream transforms read them directly. You can mix legacy and new forms in the same grid spec, or use whichever you prefer. New code should use the BoundaryConditions constructors: they validate eagerly, make the physical meaning explicit, and don't require knowing the per-basis Dict keys.","title":"Legacy Dict boundary specs"},{"category":"section","location":"boundary_conditions/#Inspecting-a-BC","page":"Boundary Conditions","text":"bc = RobinBC(1.0, 0.2, 293.15)\nbc_rank(bc)            # 1\nis_periodic(bc)        # false\nis_inhomogeneous(bc)   # true (γ ≠ 0)\n\nbc2 = DirichletBC()\nbc_rank(bc2)           # 1\nis_inhomogeneous(bc2)  # false (v = 0)\n\nbc_rank returns the number of independent constraint equations the BC imposes on the spectral coefficients — it's what the spline Γ_BC projection eliminates from the reduced spectral space. Robin and FixedBC() (R3X interface) count as rank 1 and rank 3 respectively.","title":"Inspecting a BC"},{"category":"section","location":"boundary_conditions/#See-also","page":"Boundary Conditions","text":"SpringsteelGridParameters — where per-variable BC dicts live\ncreateGrid — applies BCs during grid construction\nSolver Framework — how BCs flow through solve / solve!","title":"See also"},{"category":"type","location":"boundary_conditions/#Springsteel.BoundaryConditions","page":"Boundary Conditions","text":"BoundaryConditions\n\nBasis-agnostic boundary condition specification.  Four semantic slots:\n\nu   : prescribed function value (Dirichlet) — nothing if unconstrained\ndu  : prescribed first derivative (Neumann)  — nothing if unconstrained\nd2u : prescribed second derivative            — nothing if unconstrained\nrobin: (α, β, γ) for the Robin condition αu + βu' = γ — nothing if unused\nperiodic: true for periodic boundaries\n\nRobin is mutually exclusive with slots 1–3; periodic is mutually exclusive with all other slots.  Use the convenience constructors NaturalBC, DirichletBC, NeumannBC, SecondDerivativeBC, RobinBC, PeriodicBC, CauchyBC, ExponentialBC, SymmetricBC, AntisymmetricBC.\n\nThe rank (number of constrained derivative orders) is computed automatically by bc_rank.\n\n\n\n\n\n","title":"Springsteel.BoundaryConditions"},{"category":"function","location":"boundary_conditions/#Springsteel.NaturalBC","page":"Boundary Conditions","text":"No boundary constraint (free). Equivalent to the legacy R0.\n\n\n\n\n\n","title":"Springsteel.NaturalBC"},{"category":"function","location":"boundary_conditions/#Springsteel.DirichletBC","page":"Boundary Conditions","text":"Dirichlet BC: prescribe u(x₀) = v.  Default v = 0.\n\n\n\n\n\n","title":"Springsteel.DirichletBC"},{"category":"function","location":"boundary_conditions/#Springsteel.NeumannBC","page":"Boundary Conditions","text":"Neumann BC: prescribe u'(x₀) = v.  Default v = 0.\n\n\n\n\n\n","title":"Springsteel.NeumannBC"},{"category":"function","location":"boundary_conditions/#Springsteel.SecondDerivativeBC","page":"Boundary Conditions","text":"Prescribe the second derivative u''(x₀) = v.  Default v = 0.\n\n\n\n\n\n","title":"Springsteel.SecondDerivativeBC"},{"category":"function","location":"boundary_conditions/#Springsteel.RobinBC","page":"Boundary Conditions","text":"Robin BC: αu + βu' = γ.  Default γ = 0.\n\n\n\n\n\n","title":"Springsteel.RobinBC"},{"category":"function","location":"boundary_conditions/#Springsteel.PeriodicBC","page":"Boundary Conditions","text":"Periodic boundary condition.\n\n\n\n\n\n","title":"Springsteel.PeriodicBC"},{"category":"function","location":"boundary_conditions/#Springsteel.CauchyBC","page":"Boundary Conditions","text":"CauchyBC(u, du)\n\nCauchy (compound) BC constraining both value and first derivative. Equivalent to the legacy R2T10 when u = du = 0.\n\n\n\n\n\n","title":"Springsteel.CauchyBC"},{"category":"function","location":"boundary_conditions/#Springsteel.ExponentialBC","page":"Boundary Conditions","text":"ExponentialBC(λ)\n\nOoyama (2002) R1T10(λ): outward exponential decay u = +λ u'. Implemented as RobinBC(1, -λ, 0).\n\n\n\n\n\n","title":"Springsteel.ExponentialBC"},{"category":"function","location":"boundary_conditions/#Springsteel.SymmetricBC","page":"Boundary Conditions","text":"Symmetric (reflecting) BC: u'(x₀) = 0.  Alias for NeumannBC(0).\n\n\n\n\n\n","title":"Springsteel.SymmetricBC"},{"category":"function","location":"boundary_conditions/#Springsteel.AntisymmetricBC","page":"Boundary Conditions","text":"AntisymmetricBC()\n\nAntisymmetric BC: u(x₀) = 0 and u''(x₀) = 0. Equivalent to the legacy R2T20.\n\n\n\n\n\n","title":"Springsteel.AntisymmetricBC"},{"category":"function","location":"boundary_conditions/#Springsteel.ZerosBC","page":"Boundary Conditions","text":"ZerosBC()\n\nHomogeneous rank-3 BC: u(x₀) = u'(x₀) = u''(x₀) = 0. Equivalent to the legacy R3.  Permanently eliminates the 3 border degrees of freedom — no ahat mechanism is used.\n\n\n\n\n\n","title":"Springsteel.ZerosBC"},{"category":"function","location":"boundary_conditions/#Springsteel.FixedBC","page":"Boundary Conditions","text":"FixedBC()\nFixedBC(u::Real, du::Real=0.0, d2u::Real=0.0)\n\nRank-3 BC constraining value, first derivative, and second derivative.\n\nFixedBC() — interface BC with values determined at runtime by update_interface!.  Uses NaN sentinels to ensure the R3X (inhomogeneous) code path is activated, so the ahat mechanism is available.\nFixedBC(u, du, d2u) — prescribe specific boundary values.\n\n\n\n\n\n","title":"Springsteel.FixedBC"},{"category":"function","location":"boundary_conditions/#Springsteel.bc_rank","page":"Boundary Conditions","text":"bc_rank(bc::BoundaryConditions) -> Int\n\nNumber of derivative orders constrained.  Robin counts as rank 1. Periodic returns 0 (rank is handled internally by each basis).\n\n\n\n\n\n","title":"Springsteel.bc_rank"},{"category":"function","location":"boundary_conditions/#Springsteel.is_periodic","page":"Boundary Conditions","text":"True if the BC is periodic.\n\n\n\n\n\n","title":"Springsteel.is_periodic"},{"category":"function","location":"boundary_conditions/#Springsteel.is_inhomogeneous","page":"Boundary Conditions","text":"True if any constrained value is nonzero (inhomogeneous BC).\n\n\n\n\n\n","title":"Springsteel.is_inhomogeneous"},{"category":"section","location":"relocation/#Grid-Relocation","page":"Grid Relocation","text":"Grid relocation shifts the origin of a cylindrical grid (RL or RLZ) to a new Cartesian center, reprojecting all spectral data onto the new coordinate system. The most common use case is vortex tracking: keep a hurricane's eye centred on the grid origin even as the storm translates through its environment.\n\nRelocation is available for 2D cylindrical (RL) and 3D cylindrical (RLZ) grids. Cartesian and spherical grids are not currently supported.","title":"Grid Relocation"},{"category":"section","location":"relocation/#The-idea","page":"Grid Relocation","text":"For a cylindrical grid with center c_old = (x₀, y₀) in Cartesian space, a point at radius r and azimuth λ maps to Cartesian coordinates (x₀ + r cos λ, y₀ + r sin λ). To relocate the grid to c_new = c_old + (Δx, Δy), each new-grid point (r', λ') must pull its value from the source grid at the corresponding point in the source's coordinate frame:\n\nx_src = r' cos λ' + Δx\ny_src = r' sin λ' + Δy\nr_src = √(x_src² + y_src²)\nλ_src = atan2(y_src, x_src)\n\nThe source grid is then evaluated at (r_src, λ_src) (and optionally z) via evaluate_unstructured. This is a natural fit for the spectral representation — no interpolation error beyond the source's native truncation.","title":"The idea"},{"category":"section","location":"relocation/#Basic-usage","page":"Grid Relocation","text":"using Springsteel\n\n# Build a cylindrical grid centred at the origin\ngp = SpringsteelGridParameters(\n    geometry = \"RL\",\n    iMin = 0.0, iMax = 100.0, num_cells = 30,\n    vars = Dict(\"u\" => 1),\n    BCL = Dict(\"u\" => NaturalBC()),\n    BCR = Dict(\"u\" => NaturalBC()),\n)\ngrid = createGrid(gp)\n# ... fill grid.physical with a vortex ...\nspectralTransform!(grid)\n\n# Non-mutating: get a fresh grid shifted by (5, 3) km\nnew_grid = relocate_grid(grid, (5.0, 3.0))\n\n# In-place: reuse the same grid, cumulative center tracked automatically\nrelocate_grid!(grid, (5.0, 3.0))\ngrid_center(grid)   # → (5.0, 3.0)\nrelocate_grid!(grid, (2.0, 0.0))\ngrid_center(grid)   # → (7.0, 3.0)\n\nThe mutating relocate_grid! is the one you want inside a time-stepping loop. It reuses the grid's storage (snapshotting source data before evaluation so the operation is safe) and tracks the cumulative center shift in a per-grid registry.\n\ngrid_center returns (0.0, 0.0) for grids that have never been relocated. It reflects the cumulative displacement from the grid's original construction origin, not the absolute Cartesian position.","title":"Basic usage"},{"category":"section","location":"relocation/#Boundary-strategies","page":"Grid Relocation","text":"Some new-grid points will map to source radii beyond iMax — they lie outside the source's valid domain. Four strategies control how those out-of-bounds points are filled:\n\nStrategy Behaviour\n:nan Fill OOB points with NaN\n:nearest Clamp source radius to iMax, evaluate there\n:azimuthal_mean (default) Use only the k=0 (azimuthal-mean) coefficient of the source\n:bc_respecting Extrapolate using the grid's radial BCs (naive path)\n\n:azimuthal_mean is the best default for atmospheric vortex tracking: it preserves the background environment (slowly-varying in azimuth) at the outer edge, avoids ringing artefacts, and has no NaN propagation problems downstream. :nan is useful when you want to detect OOB regions explicitly. :nearest gives a physically-plausible clamp for smooth fields; :bc_respecting is the most expensive and least commonly needed — it goes through the naive per-point evaluation path instead of the fast per-radius path.","title":"Boundary strategies"},{"category":"section","location":"relocation/#Taper-zone","page":"Grid Relocation","text":"For smooth transitions between the full-evaluation interior and the OOB fill at the outer edge, pass taper_width > 0. The last taper_width radial rings cosine-blend between the evaluated value and the azimuthal-mean fill, eliminating the discontinuity at the OOB boundary:\n\nrelocate_grid!(grid, (5.0, 3.0);\n    boundary    = :azimuthal_mean,\n    taper_width = 3,\n)\n\nZero (default) applies the OOB strategy sharply at iMax. A value of 2–4 cells is usually enough for smooth fields.","title":"Taper zone"},{"category":"section","location":"relocation/#Per-radius-fast-path","page":"Grid Relocation","text":"relocate_grid / relocate_grid! dispatch on a per-radius fast path that batches B-spline evaluations across each ring. For a 30×30 RL grid this is roughly 14× faster than the naive evaluate_unstructured dispatch; for RLZ the gap is wider because the Chebyshev column evaluation is reused across all points of a ring. Typical time-stepping relocations on 30 × 120 RL grids complete in under a millisecond warm.\n\nThe fast path runs for every boundary strategy except :bc_respecting, which requires the naive per-point path to handle BC-based extrapolation correctly.","title":"Per-radius fast path"},{"category":"section","location":"relocation/#Multi-patch-grid-relocation","page":"Grid Relocation","text":"For a SpringsteelMultiGrid — a nest of RL / RLZ patches — call relocate_grid! on the container. The entire nest shifts as a unit, and the outer patch provides OOB lookup data to the inner patches when their remapped points cross what was previously the inner → outer boundary:\n\nmg = createMultiGrid(Dict(\n    :topology   => :embedded,\n    :geometry   => \"RL\",\n    :domains    => [(0.0, 100.0), (0.0, 50.0)],\n    :cells      => [10, 20],\n    :vars       => Dict(\"u\" => 1),\n    :BCL        => Dict(\"u\" => NaturalBC()),\n    :BCR        => Dict(\"u\" => NaturalBC()),\n))\n# ... fill and spectralTransform!(mg) ...\n\nrelocate_grid!(mg, (2.5, 0.0); boundary=:azimuthal_mean)\ngrid_center(mg)   # delegates to the innermost patch\n\nWhen the multigrid is embedded in a still-larger outer environment grid, pass that outer grid in the configuration and the relocation will fall back to it for points outside every patch in the nest. This is set up by createMultiGrid via the embedded_in kwarg; see that function's docs for the full config.","title":"Multi-patch grid relocation"},{"category":"section","location":"relocation/#Snap-quantization","page":"Grid Relocation","text":"For embedded multigrids, the shift (Δx, Δy) is snapped to the nearest multiple of the outer grid's radial node spacing so that the inner patch's half-gridpoint offset constraint is preserved across relocations. This is automatic — you don't need to compute the snap quantum yourself, but be aware that a requested 0.3 km shift on a 1 km-resolution outer grid may become 0.5 km or 0 km depending on the snap. Inspect with grid_center(mg) after the call.","title":"Snap quantization"},{"category":"section","location":"relocation/#See-also","page":"Grid Relocation","text":"evaluate_unstructured — the low-level point evaluation path under the hood\nMulti-Patch Grids — multigrid topology and construction\nInterpolation — related cross-grid tools","title":"See also"},{"category":"function","location":"relocation/#Springsteel.relocate_grid","page":"Grid Relocation","text":"relocate_grid(grid, new_center; boundary=:azimuthal_mean, out_of_bounds=:nan)\n\nReturn a new grid with spectral/physical data reprojected to new_center.\n\nnew_center is (Δx, Δy) — the displacement of the new center relative to the current center in Cartesian coordinates. The new grid's coordinate system has its origin at the shifted center.\n\nBoundary strategies for OOB points (where mapped radius exceeds iMax):\n\n:nan — fill with NaN\n:nearest — clamp source radius to iMax\n:azimuthal_mean — use only the k=0 (azimuthal mean) coefficient\n:bc_respecting — extrapolate using the grid's radial BCs\n\nReturns\n\nA new SpringsteelGrid with relocated spectral and physical data.\n\nSee also: relocate_grid!\n\n\n\n\n\n","title":"Springsteel.relocate_grid"},{"category":"function","location":"relocation/#Springsteel.relocate_grid!","page":"Grid Relocation","text":"relocate_grid!(grid, new_center; boundary=:azimuthal_mean, out_of_bounds=:nan)\n\nRelocate grid in place by reprojecting its spectral/physical data to new_center.\n\nThe grid's .spectral and .physical arrays are overwritten. The source data is snapshot before evaluation, so in-place operation is safe.\n\nReturns the modified grid.\n\nSee also: relocate_grid\n\n\n\n\n\nrelocate_grid!(mg::SpringsteelMultiGrid, new_center; snap=:auto,\n               boundary=:azimuthal_mean, out_of_bounds=:nan, taper_width=0)\n\nRelocate a multigrid (patchChain) to a new center.\n\nFor each patch, reprojects spectral/physical data via the same coordinate transform as solo relocate_grid!. On the outermost patch, OOB points are filled from the embedded outer environment if one was declared via the :embedded_in config key at construction.\n\nSnap modes\n\n:auto — if embedded_in is set, quantize the shift to the snap quantum (outer grid node spacing). Otherwise, use the shift as-is.\n:outer_nodes — force quantization; errors if no outer environment.\nfalse — use the shift as-is, no quantization.\n\n\n\n\n\n","title":"Springsteel.relocate_grid!"},{"category":"function","location":"relocation/#Springsteel.grid_center","page":"Grid Relocation","text":"grid_center(mg::SpringsteelMultiGrid) -> Tuple{Float64, Float64}\n\nReturn the cumulative center shift of the multigrid's first patch.\n\n\n\n\n\n","title":"Springsteel.grid_center"},{"category":"section","location":"springsteel_grid/#SpringsteelGrid","page":"SpringsteelGrid","text":"The SpringsteelGrid type is the unified parametric grid struct that supports multiple basis functions and geometries.","title":"SpringsteelGrid"},{"category":"section","location":"springsteel_grid/#Grid-Struct-and-Aliases","page":"SpringsteelGrid","text":"","title":"Grid Struct and Aliases"},{"category":"section","location":"springsteel_grid/#Grid-Parameters","page":"SpringsteelGrid","text":"","title":"Grid Parameters"},{"category":"section","location":"springsteel_grid/#Geometry-Types","page":"SpringsteelGrid","text":"","title":"Geometry Types"},{"category":"section","location":"springsteel_grid/#Basis-Array-Types","page":"SpringsteelGrid","text":"","title":"Basis Array Types"},{"category":"section","location":"springsteel_grid/#Basis-type-sentinels","page":"SpringsteelGrid","text":"","title":"Basis type sentinels"},{"category":"section","location":"springsteel_grid/#Basis-interface-methods","page":"SpringsteelGrid","text":"","title":"Basis interface methods"},{"category":"section","location":"springsteel_grid/#Grid-Factory","page":"SpringsteelGrid","text":"","title":"Grid Factory"},{"category":"section","location":"springsteel_grid/#Transforms","page":"SpringsteelGrid","text":"","title":"Transforms"},{"category":"section","location":"springsteel_grid/#Tiling","page":"SpringsteelGrid","text":"","title":"Tiling"},{"category":"section","location":"springsteel_grid/#Basis-template-cache","page":"SpringsteelGrid","text":"Every SpringsteelGrid construction shares immutable basis state (parameters, quadrature points, gammaBC matrices, FFT plans, etc.) through per-basis global template caches. Construction is sub-millisecond warm for typical grid sizes.","title":"Basis template cache"},{"category":"section","location":"springsteel_grid/#I/O","page":"SpringsteelGrid","text":"","title":"I/O"},{"category":"section","location":"springsteel_grid/#Grid-Type-Reference","page":"SpringsteelGrid","text":"The table below lists every grid type available in Springsteel together with its canonical geometry string, descriptive alias strings, and the corresponding SpringsteelGrid type alias.","title":"Grid Type Reference"},{"category":"section","location":"springsteel_grid/#Spline-based-grids","page":"SpringsteelGrid","text":"Canonical geometry Basis (i × j × k) Primary type alias Descriptive aliases\n\"R\" Spline R_Grid Spline1D_Grid\n\"RZ\" Spline × Chebyshev RZ_Grid —\n\"RL\" Spline × Fourier (cyl.) RL_Grid Polar_Grid\n\"RR\" Spline × Spline RR_Grid Spline2D_Grid\n\"RLZ\" Spline × Fourier × Chebyshev (cyl.) RLZ_Grid Cylindrical_Grid\n\"RRR\" Spline × Spline × Spline RRR_Grid Spline3D_Grid, Samurai_Grid\n\"SL\" Spline × Fourier (sph.) SL_Grid SphericalShell_Grid\n\"SLZ\" Spline × Fourier × Chebyshev (sph.) SLZ_Grid Sphere_Grid","title":"Spline-based grids"},{"category":"section","location":"springsteel_grid/#Fourier-based-grids","page":"SpringsteelGrid","text":"Canonical geometry Basis (i × j × k) Primary type alias Descriptive aliases\n\"L\" Fourier L_Grid Ring1D_Grid\n\"LL\" Fourier × Fourier LL_Grid Ring2D_Grid\n\"LLZ\" Fourier × Fourier × Chebyshev LLZ_Grid DoublyPeriodic_Grid","title":"Fourier-based grids"},{"category":"section","location":"springsteel_grid/#Chebyshev-based-grids","page":"SpringsteelGrid","text":"Canonical geometry Basis (i × j × k) Primary type alias Descriptive aliases\n\"Z\" Chebyshev Z_Grid Column1D_Grid\n\"ZZ\" Chebyshev × Chebyshev ZZ_Grid Column2D_Grid\n\"ZZZ\" Chebyshev × Chebyshev × Chebyshev ZZZ_Grid Column3D_Grid","title":"Chebyshev-based grids"},{"category":"section","location":"springsteel_grid/#Alias-naming-convention","page":"SpringsteelGrid","text":"All geometry strings are normalised though a two-step look-up:\n\nDescriptive aliases (right-hand column in the tables above) are defined in _GEOMETRY_ALIASES and map to the canonical short code on the left.\nThe canonical code then determines which creation function is called.\n\nThe rule is: the more descriptive name is always the alias; the short code is always the canonical target.  For example, \"Ring1D\" → \"L\", \"DoublyPeriodic\" → \"LLZ\", \"Polar\" → \"RL\".","title":"Alias naming convention"},{"category":"type","location":"springsteel_grid/#Springsteel.SpringsteelGrid","page":"SpringsteelGrid","text":"SpringsteelGrid{G, I, J, K} <: AbstractGrid\n\nUnified parametric grid struct for semi-spectral numerical models.  A single type replaces the eight legacy grid structs (R_Grid, RL_Grid, RZ_Grid, RR_Grid, RLZ_Grid, RRR_Grid, Spline1D_Grid, Spline2D_Grid) and adds support for spherical geometry (SL_Grid, SLZ_Grid).\n\nType Parameters\n\nG <: AbstractGeometry: Geometry sentinel.  Selects the transform pipeline and ring-size formula.\nCartesianGeometry   — tensor-product (all non-periodic dimensions)\nCylindricalGeometry — Fourier ring sizes grow linearly with radius\nSphericalGeometry   — Fourier ring sizes follow sin(θ) distribution\nI: i-dimension (radial / primary) basis container. Typically SplineBasisArray.\nJ: j-dimension (azimuthal / secondary) basis container. FourierBasisArray, SplineBasisArray, or NoBasisArray.\nK: k-dimension (vertical / tertiary) basis container. ChebyshevBasisArray, SplineBasisArray, or NoBasisArray.\n\nFields\n\nparams::SpringsteelGridParameters: All grid configuration (domain bounds, cell counts, boundary conditions, variable map, tiling indices).\nibasis::I: i-dimension basis objects (e.g. SplineBasisArray).\njbasis::J: j-dimension basis objects (or NoBasisArray if unused).\nkbasis::K: k-dimension basis objects (or NoBasisArray if unused).\nspectral::Array{Float64}: Spectral coefficient array, size (spectral_total, nvars).\nphysical::Array{Float64}: Physical-space values and derivatives, size (physical_total, nvars, nderiv) where nderiv = num_deriv_slots(jbasis, kbasis).\n\nBackward-compatible type aliases\n\nconst R_Grid        = SpringsteelGrid{CartesianGeometry,   SplineBasisArray, NoBasisArray,      NoBasisArray}\nconst Spline1D_Grid = R_Grid\nconst RZ_Grid       = SpringsteelGrid{CartesianGeometry,   SplineBasisArray, NoBasisArray,      ChebyshevBasisArray}\nconst RL_Grid       = SpringsteelGrid{CylindricalGeometry, SplineBasisArray, FourierBasisArray, NoBasisArray}\nconst RR_Grid       = SpringsteelGrid{CartesianGeometry,   SplineBasisArray, SplineBasisArray,  NoBasisArray}\nconst RLZ_Grid      = SpringsteelGrid{CylindricalGeometry, SplineBasisArray, FourierBasisArray, ChebyshevBasisArray}\nconst RRR_Grid      = SpringsteelGrid{CartesianGeometry,   SplineBasisArray, SplineBasisArray,  SplineBasisArray}\nconst SL_Grid       = SpringsteelGrid{SphericalGeometry,   SplineBasisArray, FourierBasisArray, NoBasisArray}\nconst SLZ_Grid      = SpringsteelGrid{SphericalGeometry,   SplineBasisArray, FourierBasisArray, ChebyshevBasisArray}\n\nExample\n\ngp = SpringsteelGridParameters(geometry=\"RL\", num_cells=10,\n    iMin=0.0, iMax=100.0,\n    vars=Dict(\"u\" => 0), BCL=Dict(\"u\" => CubicBSpline.R0), BCR=Dict(\"u\" => CubicBSpline.R0))\ngrid = createGrid(gp)   # returns SpringsteelGrid{CylindricalGeometry, SplineBasisArray, FourierBasisArray, NoBasisArray}\nspectralTransform!(grid)\ngridTransform!(grid)\n\nSee also: createGrid, SpringsteelGridParameters, spectralTransform!, gridTransform!, num_deriv_slots\n\n\n\n\n\n","title":"Springsteel.SpringsteelGrid"},{"category":"type","location":"springsteel_grid/#Springsteel.SpringsteelGridParameters","page":"SpringsteelGrid","text":"SpringsteelGridParameters\n\nConfiguration structure for spectral grid construction using mixed basis functions. Uses dimension-agnostic field names (iMin/iMax, jMin/jMax, kMin/kMax) that map to the i, j, k dimensions of the grid regardless of physical interpretation.\n\nConstruct with @kwdef keyword syntax.  Many fields are auto-calculated from primary parameters (marked auto below).\n\nFields\n\nGeometry\n\ngeometry::String = \"1D\": Grid geometry type. Options: \"R\", \"RZ\", \"RL\", \"RR\", \"RLZ\", \"RRR\", \"SL\", \"SLZ\", \"Z\", \"ZZ\", \"ZZZ\", \"L\", \"LL\", \"LLZ\"\n\ni-Dimension (CubicBSpline, Chebyshev, or Fourier depending on geometry)\n\niMin::Float64 = 0.0: Minimum i-coordinate\niMax::Float64 = 0.0: Maximum i-coordinate\nnum_cells::Int64 = 0: Number of cubic B-spline cells (spline geometries)\nmubar::Int64 = 3: Quadrature points per cell (1–5 for Gauss; any ≥1 for regular)\nquadrature::Symbol = :gauss: Quadrature type (:gauss or :regular)\niDim::Int64: Physical gridpoints (auto: num_cells * mubar)\nb_iDim::Int64: Spectral coefficients (auto: num_cells + 3)\nl_q::Dict = Dict(\"default\" => 2.0): Spline filter length (per variable)\nBCL::Dict = CubicBSpline.R0: Left boundary condition (per variable)\nBCR::Dict = CubicBSpline.R0: Right boundary condition (per variable)\n\nj-Dimension (Fourier or CubicBSpline depending on geometry)\n\njMin::Float64 = 0.0: Minimum j-coordinate\njMax::Float64 = 2π: Maximum j-coordinate\nmax_wavenumber::Dict = Dict(\"default\" => -1): Max Fourier wavenumber (-1 = ring-specific)\njDim::Int64 = 0: Physical gridpoints\nb_jDim::Int64 = 0: Spectral modes/coefficients\nBCU::Dict = Fourier.PERIODIC: j-left/upper boundary condition (per variable)\nBCD::Dict = Fourier.PERIODIC: j-right/lower boundary condition (per variable)\n\nk-Dimension (Chebyshev or CubicBSpline depending on geometry)\n\nkMin::Float64 = 0.0: Minimum k-coordinate\nkMax::Float64 = 0.0: Maximum k-coordinate\nkDim::Int64 = 0: Physical gridpoints\nb_kDim::Int64: Spectral coefficients (auto: anti-aliased from kDim)\nBCB::Dict = Chebyshev.R0: k-bottom boundary condition (per variable)\nBCT::Dict = Chebyshev.R0: k-top boundary condition (per variable)\n\nVariables\n\nvars::Dict = Dict(\"u\" => 1): Variable name → index mapping\n\nSpectral Filters\n\nfourier_filter::Dict = Dict(): Fourier filter config (per variable, \"default\" fallback)\nchebyshev_filter::Dict = Dict(): Chebyshev filter config (per variable, \"default\" fallback)\n\nTiling (distributed computing)\n\nspectralIndexL::Int64 = 1: Left spectral index for tile\nspectralIndexR::Int64: Right spectral index (auto)\npatchOffsetL::Int64: Left patch offset in gridpoints (auto)\npatchOffsetR::Int64: Right patch offset (auto)\ntile_num::Int64 = 0: Tile number identifier\n\nRegular Output Grid\n\ni_regular_out::Int64: i-points for regular output (auto: num_cells + 1)\nj_regular_out::Int64: j-points for regular output (auto: iDim*2 + 1)\nk_regular_out::Int64: k-points for regular output (auto: kDim + 1)\n\nBoundary Condition Options\n\nBoundary conditions can be specified using the basis-agnostic BoundaryConditions type or the legacy module-qualified Dict constants.  Both forms are accepted in the per-variable Dict values.\n\nGeneric constructors (recommended):\n\nNaturalBC() — no constraint (free)\nDirichletBC(v=0) — fix value\nNeumannBC(v=0) — fix first derivative\nSecondDerivativeBC(v=0) — fix second derivative\nRobinBC(α, β, γ=0) — linear combination αu + βu' = γ (CubicBSpline only)\nExponentialBC(λ) — outward decay u = +λu' (CubicBSpline only)\nPeriodicBC() — periodic\nCauchyBC(u, du) — compound: fix value and first derivative (CubicBSpline only)\nSymmetricBC() — alias for NeumannBC(0)\nAntisymmetricBC() — fix value and second derivative to zero (CubicBSpline only)\n\nLegacy Dict constants (still supported):\n\nCubicBSpline: R0, R1T0, R1T1, R1T2, R2T10, R2T20, R3, R3X, PERIODIC\nFourier: PERIODIC\nChebyshev: R0, R1T0, R1T1\n\nExample: 1D Spline Grid (generic BCs)\n\ngp = SpringsteelGridParameters(\n    geometry = \"R\",\n    iMin = 0.0, iMax = 10.0,\n    num_cells = 20,\n    BCL = Dict(\"u\" => DirichletBC()),\n    BCR = Dict(\"u\" => DirichletBC()),\n    vars = Dict(\"u\" => 1))\ngrid = createGrid(gp)\n\nExample: 1D Spline Grid (legacy BCs)\n\ngp = SpringsteelGridParameters(\n    geometry = \"R\",\n    iMin = 0.0, iMax = 10.0,\n    num_cells = 20,\n    BCL = Dict(\"u\" => CubicBSpline.R1T0),\n    BCR = Dict(\"u\" => CubicBSpline.R1T0),\n    vars = Dict(\"u\" => 1))\ngrid = createGrid(gp)\n\nExample: 2D Spline × Chebyshev (RZ)\n\ngp = SpringsteelGridParameters(\n    geometry = \"RZ\",\n    iMin = 0.0, iMax = 1.0, num_cells = 15,\n    kMin = 0.0, kMax = 1.0, kDim = 20,\n    BCL = Dict(\"u\" => CubicBSpline.R1T0),\n    BCR = Dict(\"u\" => CubicBSpline.R1T0),\n    BCB = Dict(\"u\" => Chebyshev.R1T0),\n    BCT = Dict(\"u\" => Chebyshev.R1T0),\n    vars = Dict(\"u\" => 1))\n\nExample: 2D Spline × Fourier (RL, cylindrical)\n\ngp = SpringsteelGridParameters(\n    geometry = \"RL\",\n    iMin = 1.0, iMax = 5.0, num_cells = 30,\n    max_wavenumber = Dict(\"v\" => 100),\n    BCL = Dict(\"v\" => CubicBSpline.R0),\n    BCR = Dict(\"v\" => CubicBSpline.R0),\n    vars = Dict(\"v\" => 1))\n\nSee also: createGrid, R_Grid\n\n\n\n\n\n","title":"Springsteel.SpringsteelGridParameters"},{"category":"type","location":"springsteel_grid/#Springsteel.AbstractGeometry","page":"SpringsteelGrid","text":"AbstractGeometry\n\nAbstract supertype for all geometry sentinel types used as type parameters of SpringsteelGrid.  Dispatch on the concrete subtype selects transform logic and ring-size formulas appropriate for each geometry.\n\nSee also: CartesianGeometry, CylindricalGeometry, SphericalGeometry\n\n\n\n\n\n","title":"Springsteel.AbstractGeometry"},{"category":"type","location":"springsteel_grid/#Springsteel.CartesianGeometry","page":"SpringsteelGrid","text":"CartesianGeometry <: AbstractGeometry\n\nGeometry sentinel for grids whose dimensions are all Cartesian (non-periodic, tensor-product).  Used by R_Grid, RR_Grid, RZ_Grid, RRR_Grid. Transform logic is a simple tensor-product pipeline (separable in every dimension).\n\nSee also: AbstractGeometry, CylindricalGeometry\n\n\n\n\n\n","title":"Springsteel.CartesianGeometry"},{"category":"type","location":"springsteel_grid/#Springsteel.CylindricalGeometry","page":"SpringsteelGrid","text":"CylindricalGeometry <: AbstractGeometry\n\nGeometry sentinel for grids that include a periodic azimuthal (Fourier) dimension whose ring size varies linearly with radius: textring_size(r_i) = 4 + 4 r_i. Used by RL_Grid (2-D) and RLZ_Grid (3-D).\n\nSee also: AbstractGeometry, SphericalGeometry\n\n\n\n\n\n","title":"Springsteel.CylindricalGeometry"},{"category":"type","location":"springsteel_grid/#Springsteel.SphericalGeometry","page":"SpringsteelGrid","text":"SphericalGeometry <: AbstractGeometry\n\nGeometry sentinel for grids that include a periodic azimuthal (Fourier) dimension whose ring size follows a sin(θ) distribution along the latitudinal i-dimension.  Used by SL_Grid (2-D) and SLZ_Grid (3-D).\n\nSee also: AbstractGeometry, CylindricalGeometry\n\n\n\n\n\n","title":"Springsteel.SphericalGeometry"},{"category":"type","location":"springsteel_grid/#Springsteel.SplineBasisArray","page":"SpringsteelGrid","text":"SplineBasisArray{N}\n\nTyped container parameterized by dimensionality N for an N-dimensional array of CubicBSpline.Spline1D objects. Using a concrete dimensionality preserves type stability — the compiler can inline the inner-array indexing since data has fully specified type Array{Spline1D, N}.\n\nType parameter\n\nN::Int: dimensionality of the underlying storage. Determined automatically by Julia's type inference at construction sites — e.g., SplineBasisArray(splines) where splines isa Matrix{Spline1D} produces SplineBasisArray{2}.\n\nFields\n\ndata::Array{CubicBSpline.Spline1D, N}: N-dimensional array of spline objects.\n\nSee also: FourierBasisArray, ChebyshevBasisArray, NoBasisArray\n\n\n\n\n\n","title":"Springsteel.SplineBasisArray"},{"category":"type","location":"springsteel_grid/#Springsteel.FourierBasisArray","page":"SpringsteelGrid","text":"FourierBasisArray{N}\n\nTyped container parameterized by dimensionality N for an N-dimensional array of Fourier.Fourier1D objects.\n\nType parameter\n\nN::Int: dimensionality of the underlying storage.\n\nFields\n\ndata::Array{Fourier.Fourier1D, N}: N-dimensional array of Fourier rings.\n\nSee also: SplineBasisArray\n\n\n\n\n\n","title":"Springsteel.FourierBasisArray"},{"category":"type","location":"springsteel_grid/#Springsteel.ChebyshevBasisArray","page":"SpringsteelGrid","text":"ChebyshevBasisArray{N}\n\nTyped container parameterized by dimensionality N for an N-dimensional array of Chebyshev.Chebyshev1D objects.\n\nType parameter\n\nN::Int: dimensionality of the underlying storage.\n\nFields\n\ndata::Array{Chebyshev.Chebyshev1D, N}: N-dimensional array of Chebyshev columns.\n\nSee also: SplineBasisArray\n\n\n\n\n\n","title":"Springsteel.ChebyshevBasisArray"},{"category":"type","location":"springsteel_grid/#Springsteel.NoBasisArray","page":"SpringsteelGrid","text":"NoBasisArray\n\nSentinel container representing an unused dimension.  Holds no data. Used as the jbasis or kbasis field for grid types whose j- or k-dimension is inactive.\n\nSee also: SplineBasisArray, num_deriv_slots(@ref)\n\n\n\n\n\n","title":"Springsteel.NoBasisArray"},{"category":"type","location":"springsteel_grid/#Springsteel.AbstractBasisType","page":"SpringsteelGrid","text":"AbstractBasisType\n\nAbstract supertype for basis sentinel singletons.  These are used only as type parameters in SpringsteelGrid dispatch; they carry no data.\n\nSee also: SplineBasisType, FourierBasisType, ChebyshevBasisType, NoBasisType\n\n\n\n\n\n","title":"Springsteel.AbstractBasisType"},{"category":"type","location":"springsteel_grid/#Springsteel.SplineBasisType","page":"SpringsteelGrid","text":"SplineBasisType <: AbstractBasisType\n\nBasis sentinel indicating the cubic B-spline basis (CubicBSpline.Spline1D) is active in a given dimension.\n\n\n\n\n\n","title":"Springsteel.SplineBasisType"},{"category":"type","location":"springsteel_grid/#Springsteel.FourierBasisType","page":"SpringsteelGrid","text":"FourierBasisType <: AbstractBasisType\n\nBasis sentinel indicating the Fourier basis (Fourier.Fourier1D) is active in a given dimension.\n\n\n\n\n\n","title":"Springsteel.FourierBasisType"},{"category":"type","location":"springsteel_grid/#Springsteel.ChebyshevBasisType","page":"SpringsteelGrid","text":"ChebyshevBasisType <: AbstractBasisType\n\nBasis sentinel indicating the Chebyshev basis (Chebyshev.Chebyshev1D) is active in a given dimension.\n\n\n\n\n\n","title":"Springsteel.ChebyshevBasisType"},{"category":"type","location":"springsteel_grid/#Springsteel.NoBasisType","page":"SpringsteelGrid","text":"NoBasisType <: AbstractBasisType\n\nBasis sentinel indicating that a dimension slot is unused.  A grid whose k-dimension has NoBasisType is at most 2-D.\n\n\n\n\n\n","title":"Springsteel.NoBasisType"},{"category":"function","location":"springsteel_grid/#Springsteel.gridpoints","page":"SpringsteelGrid","text":"gridpoints(obj) -> Vector{Float64}\n\ngridpoints(obj::CubicBSpline.Spline1D)   -> obj.mishPoints\ngridpoints(obj::Fourier.Fourier1D)        -> obj.mishPoints\ngridpoints(obj::Chebyshev.Chebyshev1D)   -> obj.mishPoints\n\nReturn the physical grid-point locations stored in the basis object obj.\n\nAll three basis types store this information in the field mishPoints, but the accessor ensures forward-compatible, type-stable access regardless of internal layout changes.\n\nArguments\n\nobj: a Spline1D, Fourier1D, or Chebyshev1D basis object.\n\nReturns\n\nA Vector{Float64} of length physical_dim(obj) containing the coordinates of the evaluation points in physical space.\n\nExample\n\npts = gridpoints(grid.ibasis.data[var])   # radial gridpoints for variable var\n\nSee also: spectral_dim, physical_dim\n\n\n\n\n\n","title":"Springsteel.gridpoints"},{"category":"function","location":"springsteel_grid/#Springsteel.spectral_dim","page":"SpringsteelGrid","text":"spectral_dim(obj) -> Int\n\nspectral_dim(obj::CubicBSpline.Spline1D)   -> obj.params.bDim\nspectral_dim(obj::Fourier.Fourier1D)        -> obj.params.bDim\nspectral_dim(obj::Chebyshev.Chebyshev1D)   -> obj.params.bDim\n\nReturn the number of spectral (basis) coefficients associated with obj.\n\nFor Spline1D, bDim = num_cells + 3 is stored as a direct field. For Fourier1D and Chebyshev1D, the value lives in the nested params struct; this accessor hides that distinction.\n\nArguments\n\nobj: a Spline1D, Fourier1D, or Chebyshev1D basis object.\n\nReturns\n\nAn Int equal to the length of the spectral coefficient vector for this object.\n\nExample\n\nnb = spectral_dim(grid.ibasis.data[var])   # number of spline coefficients\n\nSee also: physical_dim, gridpoints\n\n\n\n\n\n","title":"Springsteel.spectral_dim"},{"category":"function","location":"springsteel_grid/#Springsteel.physical_dim","page":"SpringsteelGrid","text":"physical_dim(obj) -> Int\n\nphysical_dim(obj::CubicBSpline.Spline1D)   -> obj.params.mishDim\nphysical_dim(obj::Fourier.Fourier1D)        -> obj.params.yDim\nphysical_dim(obj::Chebyshev.Chebyshev1D)   -> obj.params.zDim\n\nReturn the number of physical gridpoints for the basis object obj.\n\nFor Spline1D, mishDim = num_cells * CubicBSpline.mubar is a direct field. For Fourier1D and Chebyshev1D, the values are yDim and zDim respectively inside the nested params struct.\n\nArguments\n\nobj: a Spline1D, Fourier1D, or Chebyshev1D basis object.\n\nReturns\n\nAn Int equal to the number of physical-space evaluation points.\n\nExample\n\nnp = physical_dim(grid.ibasis.data[var])   # number of radial gridpoints\n\nSee also: spectral_dim, gridpoints\n\n\n\n\n\n","title":"Springsteel.physical_dim"},{"category":"function","location":"springsteel_grid/#Springsteel.createGrid","page":"SpringsteelGrid","text":"createGrid(gp::SpringsteelGridParameters) -> SpringsteelGrid\n\nCreate a SpringsteelGrid from a SpringsteelGridParameters specification. This overload handles all geometry types and replaces the separate create_*_Grid functions.\n\nArguments\n\ngp::SpringsteelGridParameters: Fully or partially specified grid configuration. Derived dimensions (jDim, b_jDim, kDim, b_kDim) are computed automatically by compute_derived_params if not provided.\n\nReturns\n\nA SpringsteelGrid{G, I, J, K} where the type parameters are determined by gp.geometry:\n\ngeometry Returned type\n\"R\", \"Spline1D\" SpringsteelGrid{CartesianGeometry, SplineBasisArray, NoBasisArray, NoBasisArray}\n\"RZ\" SpringsteelGrid{CartesianGeometry, SplineBasisArray, NoBasisArray, ChebyshevBasisArray}\n\"RL\" SpringsteelGrid{CylindricalGeometry, SplineBasisArray, FourierBasisArray, NoBasisArray}\n\"RR\", \"Spline2D\" SpringsteelGrid{CartesianGeometry, SplineBasisArray, SplineBasisArray, NoBasisArray}\n\"RLZ\" SpringsteelGrid{CylindricalGeometry, SplineBasisArray, FourierBasisArray, ChebyshevBasisArray}\n\"RRR\" SpringsteelGrid{CartesianGeometry, SplineBasisArray, SplineBasisArray, SplineBasisArray}\n\"SL\" SpringsteelGrid{SphericalGeometry, SplineBasisArray, FourierBasisArray, NoBasisArray}\n\"SLZ\" SpringsteelGrid{SphericalGeometry, SplineBasisArray, FourierBasisArray, ChebyshevBasisArray}\n\nExample\n\ngp = SpringsteelGridParameters(\n    geometry = \"RL\",\n    num_cells = 10,\n    iMin = 0.0,\n    iMax = 100.0,\n    vars = Dict(\"u\" => 1),\n    BCL = Dict(\"u\" => CubicBSpline.R0),\n    BCR = Dict(\"u\" => CubicBSpline.R0))\n\ngrid = createGrid(gp)\n# typeof(grid) == RL_Grid (a type alias for SpringsteelGrid{CylindricalGeometry, SplineBasisArray, FourierBasisArray, NoBasisArray})\n\nSee also: SpringsteelGridParameters, parse_geometry, compute_derived_params, SpringsteelGrid\n\n\n\n\n\n","title":"Springsteel.createGrid"},{"category":"function","location":"springsteel_grid/#Springsteel.parse_geometry","page":"SpringsteelGrid","text":"parse_geometry(geometry::String) -> Tuple\n\nparse_geometry(\"R\")             -> (CartesianGeometry(),   SplineBasisType(),    NoBasisType(),        NoBasisType())\nparse_geometry(\"Spline1D\")      -> (CartesianGeometry(),   SplineBasisType(),    NoBasisType(),        NoBasisType())\nparse_geometry(\"RZ\")            -> (CartesianGeometry(),   SplineBasisType(),    NoBasisType(),        ChebyshevBasisType())\nparse_geometry(\"RL\")            -> (CylindricalGeometry(), SplineBasisType(),    FourierBasisType(),   NoBasisType())\nparse_geometry(\"Polar\")         -> (CylindricalGeometry(), SplineBasisType(),    FourierBasisType(),   NoBasisType())  # alias for RL\nparse_geometry(\"RR\")            -> (CartesianGeometry(),   SplineBasisType(),    SplineBasisType(),    NoBasisType())\nparse_geometry(\"Spline2D\")      -> (CartesianGeometry(),   SplineBasisType(),    SplineBasisType(),    NoBasisType())\nparse_geometry(\"RLZ\")           -> (CylindricalGeometry(), SplineBasisType(),    FourierBasisType(),   ChebyshevBasisType())\nparse_geometry(\"Cylindrical\")   -> (CylindricalGeometry(), SplineBasisType(),    FourierBasisType(),   ChebyshevBasisType())  # alias for RLZ\nparse_geometry(\"RRR\")           -> (CartesianGeometry(),   SplineBasisType(),    SplineBasisType(),    SplineBasisType())\nparse_geometry(\"Spline3D\")      -> (CartesianGeometry(),   SplineBasisType(),    SplineBasisType(),    SplineBasisType())  # alias for RRR\nparse_geometry(\"Samurai\")       -> (CartesianGeometry(),   SplineBasisType(),    SplineBasisType(),    SplineBasisType())  # alias for RRR\nparse_geometry(\"SL\")            -> (SphericalGeometry(),   SplineBasisType(),    FourierBasisType(),   NoBasisType())\nparse_geometry(\"SphericalShell\")-> (SphericalGeometry(),   SplineBasisType(),    FourierBasisType(),   NoBasisType())  # alias for SL\nparse_geometry(\"SLZ\")           -> (SphericalGeometry(),   SplineBasisType(),    FourierBasisType(),   ChebyshevBasisType())\nparse_geometry(\"Sphere\")        -> (SphericalGeometry(),   SplineBasisType(),    FourierBasisType(),   ChebyshevBasisType())  # alias for SLZ\nparse_geometry(\"L\")             -> (CartesianGeometry(),   FourierBasisType(),   NoBasisType(),        NoBasisType())\nparse_geometry(\"Ring1D\")        -> (CartesianGeometry(),   FourierBasisType(),   NoBasisType(),        NoBasisType())  # alias for L\nparse_geometry(\"LL\")            -> (CartesianGeometry(),   FourierBasisType(),   FourierBasisType(),   NoBasisType())\nparse_geometry(\"Ring2D\")        -> (CartesianGeometry(),   FourierBasisType(),   FourierBasisType(),   NoBasisType())  # alias for LL\nparse_geometry(\"LLZ\")           -> (CartesianGeometry(),   FourierBasisType(),   FourierBasisType(),   ChebyshevBasisType())\nparse_geometry(\"DoublyPeriodic\")-> (CartesianGeometry(),   FourierBasisType(),   FourierBasisType(),   ChebyshevBasisType())  # alias for LLZ\nparse_geometry(\"Z\")             -> (CartesianGeometry(),   ChebyshevBasisType(), NoBasisType(),        NoBasisType())\nparse_geometry(\"Column1D\")      -> (CartesianGeometry(),   ChebyshevBasisType(), NoBasisType(),        NoBasisType())  # alias for Z\nparse_geometry(\"ZZ\")            -> (CartesianGeometry(),   ChebyshevBasisType(), ChebyshevBasisType(), NoBasisType())\nparse_geometry(\"Column2D\")      -> (CartesianGeometry(),   ChebyshevBasisType(), ChebyshevBasisType(), NoBasisType())  # alias for ZZ\nparse_geometry(\"ZZZ\")           -> (CartesianGeometry(),   ChebyshevBasisType(), ChebyshevBasisType(), ChebyshevBasisType())\nparse_geometry(\"Column3D\")      -> (CartesianGeometry(),   ChebyshevBasisType(), ChebyshevBasisType(), ChebyshevBasisType())  # alias for ZZZ\n\nMap a geometry string to a 4-tuple of sentinel type instances (G, It, Jt, Kt) used as type parameters of SpringsteelGrid:\n\nG  — geometry sentinel (CartesianGeometry, CylindricalGeometry, SphericalGeometry)\nIt — i-dimension basis sentinel (always SplineBasisType)\nJt — j-dimension basis sentinel or NoBasisType\nKt — k-dimension basis sentinel or NoBasisType\n\nArguments\n\ngeometry::String: Canonical geometry identifiers: \"R\", \"Spline1D\", \"RZ\", \"RL\", \"RR\", \"Spline2D\", \"RLZ\", \"RRR\", \"SL\", \"SLZ\", \"L\", \"LL\", \"LLZ\", \"Z\", \"ZZ\", \"ZZZ\". Descriptive aliases: \"Polar\", \"Cylindrical\", \"Spline3D\", \"Samurai\", \"SphericalShell\", \"Sphere\", \"Ring1D\", \"Ring2D\", \"DoublyPeriodic\", \"Column1D\", \"Column2D\", \"Column3D\".\n\nReturns\n\nA Tuple{AbstractGeometry, AbstractBasisType, AbstractBasisType, AbstractBasisType} of singleton sentinel instances ready for use with typeof() in SpringsteelGrid{G,I,J,K}.\n\nThrows\n\nDomainError if geometry is not a recognised string.\n\nExample\n\nG, It, Jt, Kt = parse_geometry(\"RL\")\nG isa CylindricalGeometry   # true\nJt isa FourierBasisType      # true\n\nSee also: SpringsteelGrid, createGrid\n\n\n\n\n\n","title":"Springsteel.parse_geometry"},{"category":"function","location":"springsteel_grid/#Springsteel.compute_derived_params","page":"SpringsteelGrid","text":"compute_derived_params(gp::SpringsteelGridParameters) -> SpringsteelGridParameters\n\nCompute derived dimension fields (jDim, b_jDim, kDim, b_kDim) and return a new, fully-populated SpringsteelGridParameters.  For most geometries the i-dimension fields are already set by the @kwdef defaults; only geometries that have variable or domain-aspect-ratio-dependent j/k dimensions require recomputation.\n\nGeometry What is derived\n\"R\", \"Spline1D\", \"RZ\" Nothing — jDim/kDim stay at user-provided values\n\"RL\", \"RLZ\" jDim, b_jDim from cylindrical ring formula ∑(4+4rᵢ)\n\"SL\", \"SLZ\" jDim, b_jDim from spherical sin(θ) ring formula\n\"RR\", \"Spline2D\" jDim, b_jDim from domain aspect ratio\n\"RRR\" jDim, b_jDim AND kDim, b_kDim from domain aspect ratio\n\nSee also: parse_geometry, createGrid\n\n\n\n\n\n","title":"Springsteel.compute_derived_params"},{"category":"function","location":"springsteel_grid/#Springsteel.num_deriv_slots","page":"SpringsteelGrid","text":"num_deriv_slots(jbasis, kbasis) -> Int\n\nnum_deriv_slots(::NoBasisArray, ::NoBasisArray) -> 3\nnum_deriv_slots(::NoBasisArray, _)              -> 5\nnum_deriv_slots(_, ::NoBasisArray)              -> 5\nnum_deriv_slots(_, _)                           -> 7\n\nReturn the number of derivative slots in physical[:, var, :] based on which j- and k-dimension basis containers are active.\n\nj-basis k-basis slots Derivative layout\nNoBasisArray NoBasisArray 3 [f, ∂f/∂i, ∂²f/∂i²]\nany NoBasisArray 5 [f, ∂f/∂i, ∂²f/∂i², ∂f/∂j, ∂²f/∂j²]\nNoBasisArray any 5 same layout (k mapped to second active dim)\nany any 7 [f, ∂f/∂i, ∂²f/∂i², ∂f/∂j, ∂²f/∂j², ∂f/∂k, ∂²f/∂k²]\n\nExample\n\nnum_deriv_slots(NoBasisArray(), NoBasisArray())  # → 3  (1D)\nnum_deriv_slots(FourierBasisArray(...), NoBasisArray())  # → 5  (2D cylindrical)\nnum_deriv_slots(NoBasisArray(), ChebyshevBasisArray(...))  # → 5  (2D Spline×Cheb)\nnum_deriv_slots(SplineBasisArray(...), ChebyshevBasisArray(...))  # → 7  (3D)\n\nSee also: SpringsteelGrid\n\n\n\n\n\n","title":"Springsteel.num_deriv_slots"},{"category":"function","location":"springsteel_grid/#Springsteel.spectralTransform!","page":"SpringsteelGrid","text":"spectralTransform!(grid::SpringsteelGrid{CartesianGeometry, SplineBasisArray, NoBasisArray, NoBasisArray})\n\nTransform field values from physical to spectral space for a 1-D Cartesian grid.\n\nReads grid.physical[:, :, 1] (field values for all variables), computes B-spline coefficients via SBtransform, and writes results into grid.spectral.\n\nArguments\n\ngrid: A 1-D Cartesian SpringsteelGrid.\n\nReturns\n\ngrid.spectral (the modified in-place spectral array).\n\nExample\n\n# Fill physical values\npts = getGridpoints(grid)\nfor i in eachindex(pts)\n    grid.physical[i, 1, 1] = sin(pts[i])\nend\n\n# Forward transform\nspectralTransform!(grid)\n\n# grid.spectral now holds B-spline coefficients\n\nSee also: gridTransform!, spectralTransform\n\n\n\n\n\nspectralTransform!(grid::SpringsteelGrid{CartesianGeometry, SplineBasisArray, SplineBasisArray, NoBasisArray})\n\nIn-place forward transform for a 2-D Cartesian Spline×Spline grid.  Reads grid.physical[:, :, 1] and writes grid.spectral.\n\nSee also: spectralTransform, gridTransform!\n\n\n\n\n\nspectralTransform!(grid::SpringsteelGrid{CartesianGeometry, SplineBasisArray, NoBasisArray, ChebyshevBasisArray})\n\nIn-place forward transform for a 2-D Cartesian Spline×Chebyshev grid.  Reads grid.physical[:, :, 1] and writes grid.spectral.\n\nSee also: spectralTransform, gridTransform!\n\n\n\n\n\nspectralTransform!(grid::SpringsteelGrid{CartesianGeometry, SplineBasisArray, SplineBasisArray, SplineBasisArray})\n\nIn-place forward transform for a 3-D Cartesian Spline×Spline×Spline grid.  Reads grid.physical[:, :, 1] and writes grid.spectral.\n\nSee also: spectralTransform, gridTransform!\n\n\n\n\n\nspectralTransform!(grid::SpringsteelGrid{CylindricalGeometry, SplineBasisArray, FourierBasisArray, NoBasisArray})\nspectralTransform(grid, physical, spectral)\n\nForward (physical → spectral) transform for the 2-D cylindrical (RL) grid.\n\nThe transform proceeds in two stages per variable:\n\nFourier stage: FBtransform! on each radial ring, producing Fourier B-coefficients (real and imaginary parts) per ring.\nSpline stage: SBtransform! on the wavenumber-0 spline from the ring-averaged values, then SBtransform! on the real/imaginary splines for each wavenumber k = 1..kDim.\n\nThe resulting spline B-coefficients are stored in the spectral array using the wavenumber-interleaved layout:\n\nspectral[1 : b_iDim, v]                  — wavenumber 0\nspectral[b_iDim+1 : 2*b_iDim, v]         — wavenumber 1, real part\nspectral[2*b_iDim+1 : 3*b_iDim, v]       — wavenumber 1, imaginary part\nspectral[(2k-1)*b_iDim+1 : 2k*b_iDim, v] — wavenumber k, real part\nspectral[2k*b_iDim+1 : (2k+1)*b_iDim, v] — wavenumber k, imaginary part\n\nkDim = iDim + patchOffsetL is the largest wavenumber that has at least one ring that can represent it.\n\n⚠️ The offset formula is p = k*2 for RL (NOT (k-1)*2).  The 2-D (RL) and 3-D (RLZ) layouts use different offset formulas because they were developed independently: RL counts from the absolute array start, while RLZ counts from the end of the k=0 block within each z-level.  See Developer Notes §TRAP-1.\n\nArguments\n\ngrid: A cylindrical SpringsteelGrid with CylindricalGeometry.\nphysical (explicit variant): Physical-space array, size (jDim, nvars, nderiv).\nspectral (explicit variant): Spectral array to fill, size (b_jDim, nvars).\n\nReturns\n\nspectral array (in-place variant modifies grid.spectral).\n\nSee also: gridTransform!\n\n\n\n\n\nspectralTransform!(grid::SpringsteelGrid{CylindricalGeometry, SplineBasisArray, FourierBasisArray, ChebyshevBasisArray})\nspectralTransform(grid, physical, spectral)\n\nForward (physical → spectral) transform for the 3-D cylindrical (RLZ) grid.\n\nTransform order (physical → spectral):\n\nChebyshev stage: CBtransform! on each (r, λ) physical column → [b_kDim, lpoints] Chebyshev coefficients per ring.\nFourier stage: Load Chebyshev coefficients into each ring's uMish, FBtransform! on each (r, z_coeff) ring.\nSpline stage (per z-coefficient): SBtransform! on the wavenumber-0 spline, then SBtransform! on real/imaginary splines for wavenumbers k = 1..kDim_wn.\n\nSpectral layout (z-major, wavenumber-interleaved per z-level):\n\nspectral[(z-1)*b_iDim*(1+kDim_wn*2) + 1 : +b_iDim]          — z-level z, k=0\nspectral[(z-1)*b_iDim*(1+kDim_wn*2) + b_iDim   + (k-1)*2*b_iDim + 1]  — k≥1 real\nspectral[(z-1)*b_iDim*(1+kDim_wn*2) + b_iDim + (k-1)*2*b_iDim + b_iDim + 1]  — k≥1 imag\n\nwhere kDim_wn = iDim + patchOffsetL.\n\n⚠️ RLZ uses p = (k-1)*2 for k ≥ 1 (unlike RL which uses k*2).  See Developer Notes §TRAP-1.\n\nSee also: gridTransform!\n\n\n\n\n\nspectralTransform!(grid::SpringsteelGrid{SphericalGeometry, SplineBasisArray, FourierBasisArray, NoBasisArray})\nspectralTransform(grid, physical, spectral)\n\nForward (physical → spectral) transform for the 2-D spherical (SL) grid.\n\nThe transform is structurally identical to the cylindrical RL transform with sin(θ)-dependent ring sizes:\n\nFourier stage: FBtransform! on each colatitudinal ring →  Fourier B-coefficients per ring.\nSpline stage: SBtransform! on the wavenumber-0 spline, then for each wavenumber k = 1..kDim (where kDim = iDim + patchOffsetL), SBtransform! on the real/imaginary splines.\n\nRing sizes follow lpoints = 4 * kmax_ring where kmax_ring ≈ round(sin(θ) * max_ri).  Near the poles lpoints is small (≥ 4); at the equator lpoints reaches its maximum 4 * max_ri.\n\nSpectral layout: wavenumber-interleaved, same as RL:\n\nspectral[1 : b_iDim, v]                   — wavenumber 0\nspectral[(2k-1)*b_iDim+1 : 2k*b_iDim, v]  — wavenumber k, real\nspectral[2k*b_iDim+1 : (2k+1)*b_iDim, v]  — wavenumber k, imaginary\n\nUsing offset formula p = k*2 (SL/RL convention — distinct from (k-1)*2 used in SLZ/RLZ).\n\nSee also: gridTransform!\n\n\n\n\n\nspectralTransform!(grid::SpringsteelGrid{SphericalGeometry, SplineBasisArray, FourierBasisArray, ChebyshevBasisArray})\nspectralTransform(grid, physical, spectral)\n\nForward (physical → spectral) transform for the 3-D spherical (SLZ) grid.\n\nIdentical in structure to the cylindrical RLZ transform with sin(θ)-dependent ring sizes:\n\nChebyshev stage: CBtransform! on each (θ, λ) physical column.\nFourier stage: FBtransform! on each (θ, z_b) ring per Chebyshev mode.\nSpline stage: Wavenumber-interleaved SBtransform! per Chebyshev mode.\n\nSpectral layout (z-major, wavenumber-interleaved per z-level): Uses p = (k-1)*2 for k ≥ 1 (SLZ/RLZ convention — see Developer Notes §TRAP-1).\n\nRing sizes follow sin(θ): lpoints = ring.params.yDim, kmax_ring = ring.params.kmax.\n\nSee also: gridTransform!\n\n\n\n\n\n","title":"Springsteel.spectralTransform!"},{"category":"function","location":"springsteel_grid/#Springsteel.spectralTransform","page":"SpringsteelGrid","text":"spectralTransform(grid, physical, spectral)\n\nExplicit-array helper for the 1-D Cartesian forward transform.\n\nApplies SBtransform to each variable's physical values and writes the resulting B-spline coefficients into spectral.  Both physical and spectral are caller-supplied arrays, so this is safe to use in distributed workflows where the grid's own arrays are not used directly.\n\nArguments\n\ngrid::SpringsteelGrid{CartesianGeometry, SplineBasisArray, NoBasisArray, NoBasisArray}: Grid providing the Spline1D basis objects.\nphysical::Array{Float64}: Shape (iDim, nvars, 3).  Only the [:, :, 1] slice (field values) is read; derivative slots are ignored.\nspectral::Array{Float64}: Destination array of shape (b_iDim, nvars).\n\nReturns\n\nspectral (mutated in-place).\n\nSee also: spectralTransform!, gridTransform\n\n\n\n\n\nspectralTransform(grid::SpringsteelGrid{CartesianGeometry, SplineBasisArray, SplineBasisArray, NoBasisArray}, physical, spectral)\nspectralTransform!(grid::SpringsteelGrid{CartesianGeometry, SplineBasisArray, SplineBasisArray, NoBasisArray})\n\nForward transform (physical → spectral) for a 2-D Cartesian Spline×Spline grid.\n\nTransform order (physical → spectral):\n\nj-direction SBtransform! for each i gridpoint → temporary buffer [b_jDim, iDim]\ni-direction SBtransform! for each j spectral mode → spectral[…, v]\n\nSpectral layout: consecutive b_iDim-element blocks, one per j-mode: spectral[(l-1)*b_iDim+1 : l*b_iDim, v] holds the i-direction B-coefficients for j-mode l.\n\nPhysical layout: (r-1)*jDim + l is the flat index for i-gridpoint r, j-gridpoint l.\n\nSee also: gridTransform!\n\n\n\n\n\nspectralTransform(grid::SpringsteelGrid{CartesianGeometry, SplineBasisArray, NoBasisArray, ChebyshevBasisArray}, physical, spectral)\nspectralTransform!(grid::SpringsteelGrid{CartesianGeometry, SplineBasisArray, NoBasisArray, ChebyshevBasisArray})\n\nForward transform (physical → spectral) for a 2-D Cartesian Spline×Chebyshev grid.\n\nTransform order (physical → spectral):\n\nk-direction CBtransform! for each i gridpoint → temporary buffer [b_kDim, iDim]\ni-direction SBtransform! for each k spectral mode → spectral[…, v]\n\nSpectral layout: consecutive b_iDim-element blocks, one per k-mode: spectral[(z-1)*b_iDim+1 : z*b_iDim, v] holds the i-direction B-coefficients for Chebyshev mode z.\n\nPhysical layout: (r-1)*kDim + z is the flat index for i-gridpoint r, k-gridpoint z.\n\nSee also: gridTransform!\n\n\n\n\n\nspectralTransform(grid::SpringsteelGrid{CartesianGeometry, SplineBasisArray, SplineBasisArray, SplineBasisArray}, physical, spectral)\nspectralTransform!(grid::SpringsteelGrid{CartesianGeometry, SplineBasisArray, SplineBasisArray, SplineBasisArray})\n\nForward transform (physical → spectral) for a 3-D Cartesian Spline×Spline×Spline grid (RRR).\n\nTransform order (physical → spectral): k-direction first, then j, then i.\n\nk-direction SBtransform! for each (r, l) gridpoint pair → tempsb_z[b_kDim, iDim, jDim]\nj-direction SBtransform! for each (r, z_coeff) → tempsb_l[b_jDim, b_kDim, iDim]\ni-direction SBtransform! for each (l_coeff, z_coeff) → spectral\n\nSpectral layout: z-major, then j-spectral, then i-spectral: spectral[(z-1)*b_jDim*b_iDim + (l-1)*b_iDim + 1 : …] holds the i-direction B-coefficients for j-mode l and k-mode z.\n\nPhysical layout: flat = (r-1)*jDim*kDim + (l-1)*kDim + z for gridpoint (r, l, z).\n\nSee also: gridTransform!\n\n\n\n\n\n","title":"Springsteel.spectralTransform"},{"category":"function","location":"springsteel_grid/#Springsteel.gridTransform!","page":"SpringsteelGrid","text":"gridTransform!(grid::SpringsteelGrid{CartesianGeometry, SplineBasisArray, NoBasisArray, NoBasisArray})\n\nTransform from spectral space to physical space with derivatives for a 1-D Cartesian grid.\n\nReads grid.spectral, solves the B→A system, and evaluates the spline representation at all mish points, writing values and derivatives into grid.physical.\n\nAfter this call:\n\ngrid.physical[:, v, 1] — field values\ngrid.physical[:, v, 2] — first derivatives ∂f/∂i\ngrid.physical[:, v, 3] — second derivatives ∂²f/∂i²\n\nArguments\n\ngrid: A 1-D Cartesian SpringsteelGrid.\n\nReturns\n\ngrid.physical (the modified in-place physical array).\n\nExample\n\nspectralTransform!(grid)     # forward transform first\ngridTransform!(grid)         # inverse transform with derivatives\n\nvalues      = grid.physical[:, 1, 1]\nfirst_deriv = grid.physical[:, 1, 2]\nsecond_deriv= grid.physical[:, 1, 3]\n\nSee also: spectralTransform!, gridTransform\n\n\n\n\n\ngridTransform!(grid::SpringsteelGrid{CartesianGeometry, SplineBasisArray, SplineBasisArray, NoBasisArray})\n\nIn-place inverse transform for a 2-D Cartesian Spline×Spline grid.  Reads grid.spectral and writes grid.physical (values + derivatives in all 5 slots).\n\nSee also: gridTransform, spectralTransform!\n\n\n\n\n\ngridTransform!(grid::SpringsteelGrid{CartesianGeometry, SplineBasisArray, NoBasisArray, ChebyshevBasisArray})\n\nIn-place inverse transform for a 2-D Cartesian Spline×Chebyshev grid.  Reads grid.spectral and writes grid.physical (values + derivatives in all 5 slots).\n\nSee also: gridTransform, spectralTransform!\n\n\n\n\n\ngridTransform!(grid::SpringsteelGrid{CartesianGeometry, SplineBasisArray, SplineBasisArray, SplineBasisArray})\n\nIn-place inverse transform for a 3-D Cartesian Spline×Spline×Spline grid.  Reads grid.spectral and writes grid.physical (values + derivatives in all 7 slots).\n\nSee also: gridTransform, spectralTransform!\n\n\n\n\n\ngridTransform!(grid::SpringsteelGrid{CylindricalGeometry, SplineBasisArray, FourierBasisArray, NoBasisArray})\ngridTransform(grid, physical, spectral)\n\nInverse (spectral → physical) transform for the 2-D cylindrical (RL) grid, including radial and azimuthal derivatives.\n\nThe inverse transform:\n\nReads wavenumber-interleaved spline coefficients from spectral.\nSAtransform! + SItransform! on each wavenumber spline → ring b-coefficients.\nFAtransform! + FItransform! on each ring → physical values (slot 1), FIxtransform → ∂f/∂λ (slot 4), FIxxtransform → ∂²f/∂λ² (slot 5).\nRepeat steps 2–3 using SIxtransform / SIxxtransform results for ∂f/∂r (slot 2) and ∂²f/∂r² (slot 3).\n\nPhysical array derivative slots\n\nSlot Contents\n1 Field values f(r, λ)\n2 ∂f/∂r  (first radial derivative)\n3 ∂²f/∂r² (second radial derivative)\n4 ∂f/∂λ  (first azimuthal derivative)\n5 ∂²f/∂λ² (second azimuthal derivative)\n\nSee also: spectralTransform!\n\n\n\n\n\ngridTransform!(grid::SpringsteelGrid{CylindricalGeometry, SplineBasisArray, FourierBasisArray, ChebyshevBasisArray})\ngridTransform(grid, physical, spectral)\n\nInverse (spectral → physical) transform for the 3-D cylindrical (RLZ) grid, including radial, azimuthal, and vertical derivatives.\n\nTransform order (spectral → physical):\n\nSpline inverse: SAtransform! + SItransform! / SIxtransform / SIxxtransform per wavenumber per z-coefficient → ring b-coefficients.\nFAtransform! + FItransform! (or FIxtransform / FIxxtransform) per ring.\nChebyshev inverse: CAtransform! + CItransform! (or CIxtransform / CIxxtransform) per (r, λ) column → physical values.\n\nPhysical derivative layout (7 slots): | Slot | Contents | |:––:|:–––– | | 1    | Field values f(r, λ, z) | | 2    | ∂f/∂r (first radial derivative) | | 3    | ∂²f/∂r² (second radial derivative) | | 4    | ∂f/∂λ (first azimuthal derivative) | | 5    | ∂²f/∂λ² (second azimuthal derivative) | | 6    | ∂f/∂z (first vertical derivative) | | 7    | ∂²f/∂z² (second vertical derivative) |\n\nBUG-1 fix: kDim_wn = iDim + patchOffsetL (old code incorrectly used iDim only, silently truncating higher wavenumbers in the inverse transform when patchOffsetL > 0).\n\nSee also: spectralTransform!\n\n\n\n\n\ngridTransform!(grid::SpringsteelGrid{SphericalGeometry, SplineBasisArray, FourierBasisArray, NoBasisArray})\ngridTransform(grid, physical, spectral)\n\nInverse (spectral → physical) transform for the 2-D spherical (SL) grid, including colatitudinal and azimuthal derivatives.\n\nIdentical in structure to the cylindrical RL inverse with sin(θ)-dependent ring sizes.\n\nPhysical derivative layout (5 slots): | Slot | Contents | |:––:|:–––– | | 1    | Field values f(θ, λ) | | 2    | ∂f/∂θ (first colatitudinal derivative) | | 3    | ∂²f/∂θ² (second colatitudinal derivative) | | 4    | ∂f/∂λ (first azimuthal derivative) | | 5    | ∂²f/∂λ² (second azimuthal derivative) |\n\nNear-pole accuracy: BCs enforce f = 0 at θ = iMin and θ = iMax (R0 BCs). Functions that vanish at the poles (e.g. sin(θ)·cos(λ)) roundtrip to machine precision.\n\nSee also: spectralTransform!\n\n\n\n\n\ngridTransform!(grid::SpringsteelGrid{SphericalGeometry, SplineBasisArray, FourierBasisArray, ChebyshevBasisArray})\ngridTransform(grid, physical, spectral)\n\nInverse (spectral → physical) transform for the 3-D spherical (SLZ) grid, including colatitudinal, azimuthal, and vertical derivatives.\n\nIdentical in structure to the cylindrical RLZ inverse with sin(θ)-dependent ring sizes.\n\nPhysical derivative layout (7 slots): | Slot | Contents | |:––:|:–––– | | 1    | Field values f(θ, λ, z) | | 2    | ∂f/∂θ (first colatitudinal derivative) | | 3    | ∂²f/∂θ² (second colatitudinal derivative) | | 4    | ∂f/∂λ (first azimuthal derivative) | | 5    | ∂²f/∂λ² (second azimuthal derivative) | | 6    | ∂f/∂z (first vertical derivative) | | 7    | ∂²f/∂z² (second vertical derivative) |\n\nSLZ/RLZ wavenumber offset: p = (k-1)*2 (TRAP-1 — different from SL/RL's k*2). kDim_wn = iDim + patchOffsetL (maximum representable wavenumber).\n\nSee also: spectralTransform!\n\n\n\n\n\ngridTransform!(patch, tile)\n\nEvaluate patch's spectral representation at tile's gridpoints and write the result into tile.physical (values and first + second i-derivatives).\n\nThis 2-argument form is the primary cross-grid evaluation interface.  It reads patch.spectral, runs the B→A transform on the patch's own spline objects, then evaluates at the tile gridpoints.\n\nSee also: gridTransform!, spectralTransform!\n\n\n\n\n\ngridTransform!(patchSplines, patchSpectral, patchParams, tile, splineBuffer)\n\n5-argument explicit form of the cross-grid inverse transform.  Reads B-coefficients from patchSpectral, uses patchSplines for the SA transform, and evaluates at tile's gridpoints.  splineBuffer is accepted for API uniformity but is unused for 1-D grids.\n\nSee also: gridTransform!\n\n\n\n\n\n","title":"Springsteel.gridTransform!"},{"category":"function","location":"springsteel_grid/#Springsteel.gridTransform","page":"SpringsteelGrid","text":"gridTransform(grid, physical, spectral)\n\nExplicit-array helper for the 1-D Cartesian inverse transform.\n\nFor each variable: solves SAtransform! (B → A coefficients), then evaluates SItransform, SIxtransform, and SIxxtransform at the mish points, writing the field value and its first and second derivatives into physical[:, v, 1], physical[:, v, 2], and physical[:, v, 3] respectively.\n\nArguments\n\ngrid::SpringsteelGrid{CartesianGeometry, SplineBasisArray, NoBasisArray, NoBasisArray}: Grid providing the Spline1D basis objects.\nphysical::Array{Float64}: Destination array of shape (iDim, nvars, 3). Slots [:, :, 1] (value), [:, :, 2] (∂/∂i), [:, :, 3] (∂²/∂i²) are all written.\nspectral::Array{Float64}: Source B-spline coefficient array of shape (b_iDim, nvars).\n\nReturns\n\nphysical (mutated in-place).\n\nSee also: gridTransform!, spectralTransform\n\n\n\n\n\ngridTransform(grid::SpringsteelGrid{CartesianGeometry, SplineBasisArray, SplineBasisArray, NoBasisArray}, physical, spectral)\ngridTransform!(grid::SpringsteelGrid{CartesianGeometry, SplineBasisArray, SplineBasisArray, NoBasisArray})\n\nInverse transform (spectral → physical + derivatives) for a 2-D Cartesian Spline×Spline grid.\n\nTransform order (spectral → physical):\n\ni-direction SAtransform! → SItransform! / SIxtransform / SIxxtransform for each j-mode → spline buffer [iDim, b_jDim]\nj-direction SAtransform! → SItransform! / SIxtransform / SIxxtransform for each i gridpoint → physical slots\n\nPhysical derivative layout (5 slots):\n\nphysical[:, v, 1] — field values\nphysical[:, v, 2] — ∂f/∂i (first derivative, i-direction)\nphysical[:, v, 3] — ∂²f/∂i² (second derivative, i-direction)\nphysical[:, v, 4] — ∂f/∂j (first derivative, j-direction)\nphysical[:, v, 5] — ∂²f/∂j² (second derivative, j-direction)\n\nSee also: spectralTransform!\n\n\n\n\n\ngridTransform(grid::SpringsteelGrid{CartesianGeometry, SplineBasisArray, NoBasisArray, ChebyshevBasisArray}, physical, spectral)\ngridTransform!(grid::SpringsteelGrid{CartesianGeometry, SplineBasisArray, NoBasisArray, ChebyshevBasisArray})\n\nInverse transform (spectral → physical + derivatives) for a 2-D Cartesian Spline×Chebyshev grid.\n\nTransform order (spectral → physical):\n\ni-direction SAtransform! → SItransform! / SIxtransform / SIxxtransform for each k-mode → spline buffer [iDim, b_kDim]\nk-direction CAtransform! → CItransform! / CIxtransform / CIxxtransform for each i gridpoint → physical slots\n\nPhysical derivative layout (5 slots):\n\nphysical[:, v, 1] — field values\nphysical[:, v, 2] — ∂f/∂i (first derivative, i-direction / spline)\nphysical[:, v, 3] — ∂²f/∂i² (second derivative, i-direction)\nphysical[:, v, 4] — ∂f/∂k (first derivative, k-direction / Chebyshev)\nphysical[:, v, 5] — ∂²f/∂k² (second derivative, k-direction)\n\nSee also: spectralTransform!\n\n\n\n\n\ngridTransform(grid::SpringsteelGrid{CartesianGeometry, SplineBasisArray, SplineBasisArray, SplineBasisArray}, physical, spectral)\ngridTransform!(grid::SpringsteelGrid{CartesianGeometry, SplineBasisArray, SplineBasisArray, SplineBasisArray})\n\nInverse transform (spectral → physical + derivatives) for a 3-D Cartesian grid (RRR).\n\nTransform order (spectral → physical): i-direction first, then j, then k.\n\ni-direction SAtransform! → SItransform! / SIxtransform / SIxxtransform for each (l_coeff, z_coeff) → splineBuffer_r[iDim, b_jDim, b_kDim]\nj-direction SAtransform! → SItransform! for each (r, z_coeff) → splineBuffer_l[jDim, b_kDim] (computed per r to avoid BUG-3 stale overwrite)\nk-direction SAtransform! → SItransform! for each (r, l) → physical slots\n\nPhysical derivative layout (7 slots):\n\nphysical[:, v, 1] — field values\nphysical[:, v, 2] — ∂f/∂i (first i-derivative)\nphysical[:, v, 3] — ∂²f/∂i² (second i-derivative)\nphysical[:, v, 4] — ∂f/∂j (first j-derivative) — BUG-2 fix applied\nphysical[:, v, 5] — ∂²f/∂j² (second j-derivative)\nphysical[:, v, 6] — ∂f/∂k (first k-derivative)\nphysical[:, v, 7] — ∂²f/∂k² (second k-derivative)\n\nBug fixes applied:\n\nBUG-2 (rrr_grid.jl §13.1): j-derivative slot assignment used wrong dimension; now computed via separate SIxtransform/SIxxtransform pass through the k-direction.\nBUG-3 (rrr_grid.jl §13.1): j-direction buffer was overwritten in outer r-loop, leaving only the last radial point's data; now the k-transform runs nested inside the r-loop.\n\nSee also: spectralTransform!\n\n\n\n\n\n","title":"Springsteel.gridTransform"},{"category":"function","location":"springsteel_grid/#Springsteel.calcTileSizes","page":"SpringsteelGrid","text":"calcTileSizes(patch::SpringsteelGrid{CartesianGeometry, <:SplineBasisArray, J, K}, num_tiles) -> Vector{SpringsteelGrid}\ncalcTileSizes(patch::SpringsteelGrid{CylindricalGeometry, <:SplineBasisArray, J, K}, num_tiles) -> Vector{SpringsteelGrid}\ncalcTileSizes(patch::SpringsteelGrid{SphericalGeometry, <:SplineBasisArray, J, K}, num_tiles) -> Vector{SpringsteelGrid}\ncalcTileSizes(patch::SpringsteelGrid, num_tiles) -> Vector{SpringsteelGrid}\n\nSplit a patch grid into num_tiles sub-domain tile grids and return them as a vector.\n\nEach tile is a fully-initialized SpringsteelGrid sharing the same geometry type, basis functions, boundary conditions, and variable map as patch, but spanning a sub-domain [iMin_tile, iMax_tile].  The tile's params.spectralIndexL records its position within the patch spectral array.\n\nSplitting strategies\n\nGeometry Strategy\nCartesianGeometry Uniform: distribute physical gridpoints (iDim) evenly; each tile gets ⌈iDim/num_tiles⌉ or ⌊iDim/num_tiles⌋ gridpoints\nCylindricalGeometry Workload-balanced: distribute total Fourier ring gridpoints (jDim) as evenly as possible while maintaining ≥ 3 cells per tile\nSphericalGeometry Workload-balanced: same as Cylindrical using sin(θ)-weighted ring sizes\n\nAll dispatch variants require the i-dimension to use a SplineBasisArray (spline basis is required for domain decomposition).  The fallback (non-SplineBasisArray i-basis) returns a single-element vector [patch] if num_tiles == 1, otherwise throws DomainError.\n\nArguments\n\npatch: Full-domain patch grid\nnum_tiles::Int64: Number of tiles to create\n\nReturns\n\nVector{typeof(patch)} of length num_tiles, each a valid SpringsteelGrid with:\n\nparams.num_cells >= 3 (minimum 3 spline cells per tile)\nparams.iDim >= 9 (minimum 9 physical gridpoints per tile)\nparams.spectralIndexL set to its global offset within the patch spectral array\n\nThrows\n\nDomainError if the patch cannot be split into num_tiles tiles each with ≥ 3 cells (or ≥ 9 gridpoints for Cartesian), or if tiling is requested on a non-Spline i-dimension.\n\nExample\n\ngp = SpringsteelGridParameters(geometry=\"R\", num_cells=12, iMin=0.0, iMax=100.0,\n                                 vars=Dict(\"u\"=>1),\n                                 BCL=Dict(\"u\"=>CubicBSpline.R0),\n                                 BCR=Dict(\"u\"=>CubicBSpline.R0))\npatch = createGrid(gp)\ntiles = calcTileSizes(patch, 4)\nlength(tiles) == 4  # true\nall(t.params.num_cells >= 3 for t in tiles)  # true\n\nSee also: calcPatchMap, calcHaloMap, allocateSplineBuffer\n\n\n\n\n\ncalcTileSizes(patch::SpringsteelGrid{CartesianGeometry, <:SplineBasisArray, <:SplineBasisArray, NoBasisArray},\n              tile_spec::NamedTuple) -> Vector{SpringsteelGrid}\n\nMulti-dimensional tile decomposition for a 2-D Cartesian Spline×Spline grid (RR or Spline2D).  Returns a flat Vector of ni × nj tile sub-grids.\n\ntile_spec is a NamedTuple with optional keys :i (number of i-tiles, default 1) and :j (number of j-tiles, default 1).  The resulting tiles are ordered with j varying fastest: tiles[(ti-1)*nj + tj] corresponds to i-strip ti, j-strip tj.\n\nEach tile is a fully-initialised SpringsteelGrid{CartesianGeometry, <:SplineBasisArray, <:SplineBasisArray, NoBasisArray} spanning the sub-domain [iMin_ti, iMax_ti] × [jMin_tj, jMax_tj].\n\nSplitting strategy\n\nBoth i and j dimensions are split by cell count (nc ≥ 3 per tile).  The i-direction spectral index (spectralIndexL) for each tile records its position within the patch i-spectral array; j-tiles have independent j-spectral layouts within each tile.\n\nThrows\n\nDomainError if any tile would have fewer than 3 cells in any tiled dimension.\n\nSee also\n\ncalcTileSizes(::SpringsteelGrid, ::Int64), calcPatchMap_multidim, calcHaloMap_multidim\n\n\n\n\n\ncalcTileSizes(patch::SpringsteelGrid{CartesianGeometry, <:SplineBasisArray, <:SplineBasisArray, <:SplineBasisArray},\n              tile_spec::NamedTuple) -> Vector{SpringsteelGrid}\n\nMulti-dimensional tile decomposition for a 3-D Cartesian Spline×Spline×Spline grid (RRR). Returns a flat Vector of ni × nj × nk tile sub-grids.\n\ntile_spec accepts optional keys :i, :j, :k (default 1 each).  Tiles are ordered with k varying fastest: tiles[(ti-1)*nj*nk + (tj-1)*nk + tk].\n\nThrows\n\nDomainError if any tile would have fewer than 3 cells in any tiled dimension.\n\nSee also\n\ncalcTileSizes(::_2DCartesianRR, ::NamedTuple)\n\n\n\n\n\ncalcTileSizes(patch::SpringsteelGrid{G, <:SplineBasisArray, J, K}, tile_spec::NamedTuple) -> Vector{SpringsteelGrid}\n\nGeneric NamedTuple dispatch for grids whose i-dimension uses a Spline basis.\n\nFor grids where only the i-dimension is tiled (tile_spec = (i=N,)) this delegates to the existing 1-D calcTileSizes overload.  If :j or :k counts exceed 1, but the corresponding basis is not a SplineBasisArray, a DomainError is thrown — tiling requires a Spline basis in the tiled dimension.\n\nThis is the entry point for calls such as calcTileSizes(rl_patch, (i=4,)) (single i-strip tiling via NamedTuple) or calcTileSizes(rl_patch, (i=2, j=2)) (error: j is Fourier, not Spline).\n\nThrows\n\nDomainError if :j > 1 and J is not SplineBasisArray, or :k > 1 and K is not SplineBasisArray.\n\n\n\n\n\n","title":"Springsteel.calcTileSizes"},{"category":"function","location":"springsteel_grid/#Springsteel.calcPatchMap","page":"SpringsteelGrid","text":"calcPatchMap(patch::SpringsteelGrid, tile::SpringsteelGrid) -> SparseMatrixCSC{Float64, Int64}\n\nReturn a sparse matrix marking the inner (non-halo) region of tile within the patch spectral array.\n\nThe inner region is defined as spectralIndexL : spectralIndexR - 3 in the 1-D (k=0 wavenumber) block.  The last 3 rows are the halo (overlap with the next tile) identified by calcHaloMap.  For cylindrical/spherical grids the same inner-region logic applies to every wavenumber block.\n\nReturns\n\nSparseMatrixCSC{Float64, Int64} of size (spectral_rows, nvars) with 1.0 entries at the inner-region rows and 0.0 elsewhere.\n\nNote\n\nThe Cartesian 1-D method returns nnz == (spectralIndexR-3 − spectralIndexL + 1) * nvars; the Cylindrical method returns nnz == (b_iDim-4) * nvars * (2*kDim+1).\n\nSee also: calcHaloMap, sumSharedSpectral\n\n\n\n\n\n","title":"Springsteel.calcPatchMap"},{"category":"function","location":"springsteel_grid/#Springsteel.calcPatchMap_multidim","page":"SpringsteelGrid","text":"calcPatchMap_multidim(patch::SpringsteelGrid, tile::SpringsteelGrid) -> SparseMatrixCSC\n\nCompute a SparseMatrixCSC marking the non-halo (interior) spectral rows of patch that belong exclusively to tile in a multi-dimensional tiling context.\n\nFor a 2-D Cartesian Spline×Spline grid the spectral array is laid out as spectral[(j_mode - 1) * b_iDim + i_mode, v].  The interior i-mode range for the tile is [siL, siR - 3] (excludes the 3-mode halo on the right boundary), and the interior j-mode range is [1, tile.params.b_jDim - 3] within the tile's own spectral array.  This function marks the corresponding rows in the patch spectral array.\n\nThe returned sparse matrix has shape (size(patch.spectral, 1), length(patch.params.vars)) with nonzero value 1.0 at each (row, var) that is exclusively owned by this tile.\n\nFor a 1-D (non-multi-dim) tile, this reduces to the same result as calcPatchMap.\n\nSee also: calcHaloMap_multidim, calcPatchMap\n\n\n\n\n\n","title":"Springsteel.calcPatchMap_multidim"},{"category":"function","location":"springsteel_grid/#Springsteel.calcHaloMap","page":"SpringsteelGrid","text":"calcHaloMap(patch, tile1, tile2) -> SparseMatrixCSC{Float64, Int64}\n\nReturn a sparse matrix marking the 3-row halo (overlap) region at the right boundary of tile1 within the patch spectral array.\n\nThe halo rows spectralIndexR-2 : spectralIndexR of tile1 overlap with the first 3 spectral rows contributed by the adjacent tile2.  These rows must be summed (not exclusively written) during distributed transforms.  See Developer Notes §TRAP-2 for the off-by-one derivation (-4 vs -3).\n\nFor cylindrical/spherical grids the same 3-row halo applies independently to every wavenumber block.\n\nArguments\n\npatch: Full-domain patch grid (provides spectral array dimensions)\ntile1: Left tile whose right-edge halo is identified\ntile2: Right tile (currently unused; included for API completeness and future validation)\n\nReturns\n\nSparseMatrixCSC{Float64, Int64} of size (spectral_rows, nvars) with 1.0 at the 3 halo rows and 0.0 elsewhere.  For nvars == 1: nnz == 3.\n\nConcurrency\n\nThe halo rows identified here must not be written simultaneously by two workers. Accumulate halo contributions serially or via explicit synchronisation. See also sumSharedSpectral.\n\nSee also: calcPatchMap, getBorderSpectral\n\n\n\n\n\ncalcHaloMap(patch, tile) -> SparseMatrixCSC{Float64, Int64}\n\n2-argument convenience form of calcHaloMap.  Calls the 3-argument internal implementation with tile used as both left and right neighbours (sufficient for the halo-map computation, which only reads tile1).\n\nSee also: calcHaloMap\n\n\n\n\n\n","title":"Springsteel.calcHaloMap"},{"category":"function","location":"springsteel_grid/#Springsteel.calcHaloMap_multidim","page":"SpringsteelGrid","text":"calcHaloMap_multidim(patch::SpringsteelGrid, tile_L::SpringsteelGrid, tile_R::SpringsteelGrid) -> SparseMatrixCSC\n\nCompute the face halo SparseMatrixCSC between two adjacent tiles that share an i-direction boundary in a multi-dimensional tiling context.\n\nThe halo region consists of the 3 i-spectral modes at the right boundary of tile_L (indices spectralIndexR - 2 : spectralIndexR in patch coordinates) for all j-mode blocks owned by tile_L.\n\nThe returned sparse matrix has shape (size(patch.spectral, 1), length(patch.params.vars)) with nonzero value 1.0 at each halo (row, var) entry.\n\nCorner halos (where two or three tiled dimensions overlap) require calling this function per face and intersecting the results — or using dedicated corner-halo logic for performance.  See Developer Notes §12 (Multi-Dimensional Tiling) for the full multi-dim halo design.\n\nArguments\n\npatch: Full-domain patch grid (provides spectral layout)\ntile_L: Left tile (halo is on its right i-boundary)\ntile_R: Right tile (not used directly; present for API symmetry with 1-D version)\n\nSee also: calcPatchMap_multidim, calcHaloMap\n\n\n\n\n\n","title":"Springsteel.calcHaloMap_multidim"},{"category":"function","location":"springsteel_grid/#Springsteel.allocateSplineBuffer","page":"SpringsteelGrid","text":"allocateSplineBuffer(tile::SpringsteelGrid) -> Array{Float64}\n\nAllocate the spline derivative buffer needed by tileTransform! for this tile type.\n\nGrid type Buffer shape Usage\n1-D Cartesian (R) (1,) trivial unused; included for API uniformity\n2-D Cylindrical (RL) (iDim, 2, nvars) real + imaginary radial evaluations\n2-D Spherical (SL) (iDim, 2, nvars) same as Cylindrical\n2-D Cartesian Spline×Spline (RR) (iDim, b_jDim, nvars) j-mode radial evaluations\n2-D Cartesian Spline×Chebyshev (RZ) (iDim, b_kDim, nvars) Chebyshev-mode radial evaluations\n3-D Cartesian Spline×Spline×Spline (RRR) (iDim, b_jDim, b_kDim, nvars) j×k mode radial evaluations\n3-D Cylindrical (RLZ) (iDim, 3, b_kDim, nvars) k=0 / real / imag × Chebyshev levels\n3-D Spherical (SLZ) (iDim, 3, b_kDim, nvars) same as RLZ\nAll other (1,) trivial fallback\n\nArguments\n\ntile: A SpringsteelGrid sub-domain tile (or the full patch for single-tile runs)\n\nReturns\n\nArray{Float64} of the appropriate shape; all zeros.\n\nSee also: tileTransform!, calcTileSizes\n\n\n\n\n\nallocateSplineBuffer(patch, tile) -> Array\n\n2-argument form: allocate a spline buffer sized for tile. Delegates to the 1-argument form allocateSplineBuffer(tile).\n\nSee also: allocateSplineBuffer\n\n\n\n\n\n","title":"Springsteel.allocateSplineBuffer"},{"category":"function","location":"springsteel_grid/#Springsteel.num_columns","page":"SpringsteelGrid","text":"num_columns(grid::SpringsteelGrid{CylindricalGeometry, SplineBasisArray, FourierBasisArray, NoBasisArray}) -> Int\n\nReturn the number of j×k columns for tiling purposes.  For 2-D cylindrical grids (RL) this is always 0 because the azimuthal dimension is not tiled.\n\nSee also: RL_Grid\n\n\n\n\n\nnum_columns(grid::SpringsteelGrid{CylindricalGeometry, SplineBasisArray, FourierBasisArray, ChebyshevBasisArray}) -> Int\n\nReturn the number of j×k columns for tiling purposes.  For 3-D cylindrical grids (RLZ) this is the total number of azimuthal physical points summed over all rings (jDim).\n\nSee also: RLZ_Grid\n\n\n\n\n\nnum_columns(grid::SpringsteelGrid{SphericalGeometry, SplineBasisArray, FourierBasisArray, NoBasisArray}) -> Int\n\nReturn 0 for 2-D spherical grids (no j×k column tiling for the azimuthal dimension).\n\n\n\n\n\nnum_columns(grid::SpringsteelGrid{SphericalGeometry, SplineBasisArray, FourierBasisArray, ChebyshevBasisArray}) -> Int\n\nReturn the total number of (λ̃, z̃) column pairs summed over all colatitudinal rings. For tiling purposes this equals jDim (total azimuthal physical points across all rings).\n\n\n\n\n\nnum_columns(grid::SpringsteelGrid{CartesianGeometry, <:SplineBasisArray, NoBasisArray, NoBasisArray}) -> Int\n\nReturn 1 for 1-D Cartesian grids.  The single radial spline corresponds to one spectral \"column\" per variable.\n\nSee also: allocateSplineBuffer\n\n\n\n\n\nnum_columns(grid::SpringsteelGrid{CartesianGeometry, <:SplineBasisArray, <:SplineBasisArray, NoBasisArray}) -> Int\n\nReturn b_jDim for 2-D Cartesian Spline×Spline (RR) grids: one radial spline column per j-spectral mode.\n\n\n\n\n\nnum_columns(grid::SpringsteelGrid{CartesianGeometry, <:SplineBasisArray, NoBasisArray, <:ChebyshevBasisArray}) -> Int\n\nReturn b_kDim for 2-D Cartesian Spline×Chebyshev (RZ) grids: one radial spline column per Chebyshev vertical mode.\n\n\n\n\n\nnum_columns(grid::SpringsteelGrid{CartesianGeometry, <:SplineBasisArray, <:SplineBasisArray, <:SplineBasisArray}) -> Int\n\nReturn b_jDim * b_kDim for 3-D Cartesian Spline×Spline×Spline (RRR) grids.\n\n\n\n\n\n","title":"Springsteel.num_columns"},{"category":"function","location":"springsteel_grid/#Springsteel.sumSpectralTile!","page":"SpringsteelGrid","text":"sumSpectralTile!(patch::SpringsteelGrid, tile::SpringsteelGrid) -> Array{Float64}\n\nAccumulate tile.spectral into the corresponding rows of patch.spectral.\n\nThis is the accumulation variant: it adds (not overwrites) the tile B-vector contributions.  Because the last 3 rows of each tile's spectral array overlap with the next tile's first rows, multiple calls (one per tile) must be made before the full patch spectral array is consistent.\n\nAPI variants\n\nGeometry Accumulation pattern\n1-D Cartesian patch.spectral[siL:siR, :] .+= tile.spectral[:, :]\n2-D Cylindrical Wavenumber-interleaved blocks (see spectral layout in transforms_cylindrical.jl)\nGeneric Same as 1-D Cartesian (safe for all other Cartesian types)\n\nConcurrency note\n\nDo NOT call this function concurrently from multiple threads/processes writing to overlapping spectral rows. See RACE-1 in Developer Notes.\n\nSee also: setSpectralTile!, sumSharedSpectral\n\n\n\n\n\n","title":"Springsteel.sumSpectralTile!"},{"category":"function","location":"springsteel_grid/#Springsteel.setSpectralTile!","page":"SpringsteelGrid","text":"setSpectralTile!(patch::SpringsteelGrid, tile::SpringsteelGrid) -> Array{Float64}\n\nZero the full patch spectral array and write tile.spectral into the appropriate rows.\n\nThis is the zero-then-write variant, suitable for single-tile workflows or testing. For multi-tile workflows use sumSpectralTile! after zeroing once.\n\nConcurrency note\n\nZeroes patch.spectral entirely before writing.  Do NOT call from multiple threads simultaneously.\n\nSee also: sumSpectralTile!\n\n\n\n\n\n","title":"Springsteel.setSpectralTile!"},{"category":"function","location":"springsteel_grid/#Springsteel.sumSpectralTile","page":"SpringsteelGrid","text":"sumSpectralTile(dest, tileSpectral, siL, siR)\n\nLower-level accumulation: add tileSpectral into dest[siL:siR, :].\n\nThis is the array-only variant (no SpringsteelGrid objects required). Multiple calls accumulate (sum) contributions.\n\nSee also: sumSpectralTile!\n\n\n\n\n\n","title":"Springsteel.sumSpectralTile"},{"category":"function","location":"springsteel_grid/#Springsteel.setSpectralTile","page":"SpringsteelGrid","text":"setSpectralTile(dest, patchParams, tile)\n\n3-argument array form: zero dest then write tile.spectral into the rows tile.params.spectralIndexL : tile.params.spectralIndexR.\n\nSee also: setSpectralTile!\n\n\n\n\n\n","title":"Springsteel.setSpectralTile"},{"category":"function","location":"springsteel_grid/#Springsteel.getBorderSpectral","page":"SpringsteelGrid","text":"getBorderSpectral(grid::SpringsteelGrid) -> SparseMatrixCSC{Float64, Int64}\n\nExtract the rightmost 3 B-vector rows of the tile spectral array as a sparse matrix keyed to the tile's global patch indices.  These rows overlap with the adjacent tile's leftmost halo zone and must be accumulated (not exclusively written) in sumSharedSpectral.\n\nFor a tile with b_iDim spline coefficients and spectralIndexR = spectralIndexL + b_iDim − 1:\n\nTile source rows: b_iDim-2 : b_iDim\nPatch destination rows: spectralIndexR-2 : spectralIndexR\n\nFor cylindrical/spherical grids the same 3-row extraction applies to every wavenumber block, but only the wavenumber-0 block is implemented here (sufficient for 1-D halo transfer).\n\nArguments\n\ngrid: SpringsteelGrid tile (or patch for the no-halo single-tile case)\n\nReturns\n\nSparseMatrixCSC{Float64, Int64} of size (spectral_rows, nvars) with 3 non-zero rows.\n\nSee also: sumSharedSpectral, calcHaloMap\n\n\n\n\n\n","title":"Springsteel.getBorderSpectral"},{"category":"function","location":"springsteel_grid/#Springsteel.sumSharedSpectral","page":"SpringsteelGrid","text":"sumSharedSpectral(sharedSpectral, tile, patchMap, haloMap) -> Nothing\n\nWrite a tile's interior B-vectors into sharedSpectral (exclusive region) and accumulate halo contributions (overlap region).\n\nUses the pre-computed patchMap to identify which rows are exclusive to this tile, and haloMap to identify the 3 rows that overlap with the adjacent tile.\n\nArguments\n\nsharedSpectral::SharedArray{Float64}: Shared memory array for the full-patch B-vector\ntile::SpringsteelGrid: Source tile providing B-coefficients and spectral index offsets\npatchMap: SparseMatrixCSC from calcPatchMap — exclusive interior region\nhaloMap: SparseMatrixCSC from calcHaloMap — halo overlap region\n\nConcurrency warning (RACE-1)\n\nThe halo accumulation (sharedSpectral[halo_rows, :] .+= ...) is non-atomic. If two adjacent tiles share halo rows, concurrent writes will produce incorrect results. Callers must serialise halo-zone writes across workers.\n\nSee also: splineTransform!, getBorderSpectral\n\n\n\n\n\n","title":"Springsteel.sumSharedSpectral"},{"category":"function","location":"springsteel_grid/#Springsteel.splineTransform!","page":"SpringsteelGrid","text":"splineTransform!(sharedSpectral, tile) -> Nothing\n\nApply the SA (B-vector → A-coefficient) transform for all splines in tile using data from the shared spectral array.\n\nAfter this call, tile.spectral[:, v] contains the A-coefficients for variable v (ready for tileTransform! to evaluate physical values).\n\nConcurrency warning (RACE-3)\n\nReads from sharedSpectral (SharedArray).  The caller must ensure that sharedSpectral is fully populated (all tiles have finished their sumSharedSpectral calls) before invoking this function in parallel.\n\nSee also: tileTransform!, sumSharedSpectral\n\n\n\n\n\nsplineTransform!(sharedSpectral, tile::_RLGrid) -> Nothing\n\nApply the SA (B-vector → A-coefficient) transform for all spline blocks in tile using B-coefficients in sharedSpectral.\n\nAfter this call, tile.spectral contains A-coefficients for every wavenumber block (k=0, and k=1..kDim real/imaginary pairs), ready for tileTransform!.\n\nThe spectral layout (RL convention, p = k*2):\n\nk=0 block: rows 1:b_iDim\nk real: rows (p-1)*b_iDim+1 : p*b_iDim\nk imag: rows p*b_iDim+1 : (p+1)*b_iDim\n\nUsage note: designed for single-tile or same-sized patch/tile scenarios. For multi-tile workflows with different tile and patch sizes, use the legacy 5-argument form with explicit patch splines.\n\nConcurrency warning (RACE-3)\n\nReads from sharedSpectral (SharedArray).  The caller must ensure that sharedSpectral is fully populated before invoking this function.\n\nSee also: tileTransform!, sumSharedSpectral\n\n\n\n\n\nsplineTransform!(sharedSpectral, tile::_SLGrid) -> Nothing\n\nApply the SA (B-vector → A-coefficient) transform for all spline blocks in the 2-D spherical (SL) tile using B-coefficients in sharedSpectral.\n\nStructurally identical to the RL variant; uses p = k*2 offset convention. After this call, tile.spectral contains A-coefficients ready for tileTransform!.\n\nSee also: tileTransform!, sumSharedSpectral\n\n\n\n\n\nsplineTransform!(sharedSpectral, tile::_RLZGrid) -> Nothing\n\nApply the SA (B-vector → A-coefficient) transform for all spline blocks in the 3-D cylindrical (RLZ) tile using B-coefficients in sharedSpectral.\n\nThe spectral layout is z-major with b_kDim Chebyshev-coefficient levels.  Within each z-level the layout is wavenumber-interleaved (RLZ convention: p = (k-1)*2):\n\nz-level z_b, k=0:     rows r1:r2  where r1 = (z_b-1)*b_iDim*(1+kDim_wn*2)+1\nz-level z_b, k real:  r2+1 + (k-1)*2*b_iDim ... + b_iDim\nz-level z_b, k imag:  r2+1 + (k-1)*2*b_iDim + b_iDim ... + b_iDim\n\nAfter this call, tile.spectral contains A-coefficients ready for tileTransform!.\n\nSee also: tileTransform!, sumSharedSpectral\n\n\n\n\n\nsplineTransform!(sharedSpectral, tile::_SLZGrid) -> Nothing\n\nApply the SA (B-vector → A-coefficient) transform for all spline blocks in the 3-D spherical (SLZ) tile.\n\nLayout identical to RLZ: z-major, with p = (k-1)*2 wavenumber offset convention. After this call, tile.spectral contains A-coefficients ready for tileTransform!.\n\nSee also: tileTransform!, sumSharedSpectral\n\n\n\n\n\nsplineTransform!(sharedSpectral, patch, tile) -> Nothing\n\nMulti-tile variant: apply the SA transform using patch's splines on the full sharedSpectral, then extract the tile's portion of A-coefficients into tile.spectral.\n\nUse this form in distributed workflows where tile is a subdomain of patch. The 2-argument form splineTransform!(sharedSpectral, tile) remains valid for single-tile (tile = patch) scenarios.\n\nArguments\n\nsharedSpectral::SharedArray{Float64}: patch-sized shared spectral (B-coefficients).\npatch: full-domain grid whose ibasis.data splines provide the SA matrix.\ntile: tile grid whose spectral array receives the extracted A-coefficients.\n\nSee also: tileTransform!\n\n\n\n\n\n","title":"Springsteel.splineTransform!"},{"category":"function","location":"springsteel_grid/#Springsteel.tileTransform!","page":"SpringsteelGrid","text":"tileTransform!(sharedSpectral, tile, physical, spectral) -> Array{Float64}\n\nEvaluate the field and its derivatives on tile's gridpoints using pre-computed A-coefficients stored in spectral.\n\nPopulates:\n\nphysical[:, v, 1] — field values\nphysical[:, v, 2] — first i-derivatives\nphysical[:, v, 3] — second i-derivatives\n\nThread safety (RACE-2)\n\nSafe to parallelise over variables v because ibasis.data[1, v] and physical[:, v, :] are fully independent per v.  Do not share basis objects across variables without reviewing this invariant. See RACE-2 in Developer Notes.\n\nArguments\n\nsharedSpectral::SharedArray{Float64}: Shared memory (not used directly; included for API uniformity)\ntile::SpringsteelGrid: Target tile whose physical array is written\nphysical::Array{Float64}: Destination physical array (shape (iDim, nvars, 3))\nspectral::Array{Float64}: Source A-coefficient array (pre-computed by splineTransform!)\n\nSee also: splineTransform!, gridTransform!\n\n\n\n\n\ntileTransform!(sharedSpectral, tile::_RLGrid, physical, spectral) -> Array{Float64}\n\nEvaluate the RL field and its radial/azimuthal derivatives on tile's gridpoints using pre-computed A-coefficients in spectral.\n\nspectral must have been populated by splineTransform! (A-coefficient form). The SA solve step is skipped — spline .a fields are set directly from spectral.\n\nPopulates all 5 physical derivative slots:\n\nphysical[:, v, 1] — field values\nphysical[:, v, 2] — ∂f/∂r  (first radial derivative)\nphysical[:, v, 3] — ∂²f/∂r²  (second radial derivative)\nphysical[:, v, 4] — ∂f/∂λ  (first azimuthal derivative)\nphysical[:, v, 5] — ∂²f/∂λ²  (second azimuthal derivative)\n\nSee also: splineTransform!, gridTransform!\n\n\n\n\n\ntileTransform!(sharedSpectral, tile::_SLGrid, physical, spectral) -> Array{Float64}\n\nEvaluate the SL field and its colatitudinal/azimuthal derivatives on tile's gridpoints using pre-computed A-coefficients in spectral.\n\nStructurally identical to the RL variant with two differences:\n\nRing size: lpoints = tile.jbasis.data[r, v].params.yDim (sin(θ)-based)\nPer-ring kmax: tile.jbasis.data[r, v].params.kmax (sin(θ)-based, per-ring cutoff)\n\nPopulates all 5 physical derivative slots (values, ∂/∂θ, ∂²/∂θ², ∂/∂λ, ∂²/∂λ²).\n\nSee also: splineTransform!, gridTransform!\n\n\n\n\n\ntileTransform!(sharedSpectral, tile::_RLZGrid, physical, spectral) -> Array{Float64}\n\nEvaluate the RLZ field and all derivatives on tile's gridpoints using pre-computed A-coefficients in spectral.\n\nThe SA solve step is skipped — spline .a fields are set directly.  The function mirrors gridTransform(::_RLZGrid, ...) but reads A-coefficients instead of B-coefficients.\n\nPopulates all 7 physical derivative slots:\n\nphysical[:, v, 1] — field values\nphysical[:, v, 2] — ∂f/∂r,  [:, v, 3] — ∂²f/∂r²\nphysical[:, v, 4] — ∂f/∂λ,  [:, v, 5] — ∂²f/∂λ²\nphysical[:, v, 6] — ∂f/∂z,  [:, v, 7] — ∂²f/∂z²\n\nSee also: splineTransform!, gridTransform!\n\n\n\n\n\ntileTransform!(sharedSpectral, tile::_SLZGrid, physical, spectral) -> Array{Float64}\n\nEvaluate the SLZ field and all derivatives on tile's gridpoints using pre-computed A-coefficients in spectral.\n\nStructurally identical to the RLZ variant with sin(θ)-based ring sizes:\n\nlpoints = tile.jbasis.data[r, z_b].params.yDim\nk <= tile.jbasis.data[r, z_b].params.kmax (per-ring kmax check)\n\nPopulates all 7 physical derivative slots.\n\nSee also: splineTransform!, gridTransform!\n\n\n\n\n\n","title":"Springsteel.tileTransform!"},{"category":"function","location":"springsteel_grid/#Springsteel.basis_cache_sizes","page":"SpringsteelGrid","text":"basis_cache_sizes() -> NamedTuple\n\nReturn the number of cached templates for each basis type.\n\n\n\n\n\n","title":"Springsteel.basis_cache_sizes"},{"category":"function","location":"springsteel_grid/#Springsteel.clear_basis_caches!","page":"SpringsteelGrid","text":"clear_basis_caches!()\n\nClear all cached basis templates (Spline1D, Fourier1D, Chebyshev1D).\n\nSubsequent basis construction will rebuild from scratch. Use this to reclaim memory in long-running processes that have accumulated many distinct parameter sets, or in tests that need cold-cache behavior.\n\n\n\n\n\n","title":"Springsteel.clear_basis_caches!"},{"category":"function","location":"springsteel_grid/#Springsteel.getGridpoints","page":"SpringsteelGrid","text":"getGridpoints(grid::SpringsteelGrid{CartesianGeometry, SplineBasisArray, NoBasisArray, NoBasisArray}) -> Vector{Float64}\n\nReturn the physical mish-point locations for the 1-D Cartesian spline grid.\n\nAll variables share the same radial domain, so the mish-points from the first variable's Spline1D object are returned as the canonical gridpoint array.\n\nArguments\n\ngrid: A 1-D Cartesian SpringsteelGrid (type alias R_Grid).\n\nReturns\n\nA Vector{Float64} of length grid.params.iDim containing the Gaussian quadrature (mish) evaluation points, monotonically increasing from near iMin to near iMax.\n\nExample\n\ngp   = SpringsteelGridParameters(geometry=\"R\", num_cells=10, iMin=0.0, iMax=10.0,\n                                  vars=Dict(\"u\"=>1),\n                                  BCL=Dict(\"u\"=>CubicBSpline.R0),\n                                  BCR=Dict(\"u\"=>CubicBSpline.R0))\ngrid = createGrid(gp)\npts  = getGridpoints(grid)\nlength(pts) == grid.params.iDim   # true\n\nSee also: spectralTransform!, gridTransform!\n\n\n\n\n\ngetGridpoints(grid::SpringsteelGrid{CartesianGeometry, SplineBasisArray, SplineBasisArray, NoBasisArray}) -> Matrix{Float64}\ngetGridpoints(grid::SpringsteelGrid{CartesianGeometry, SplineBasisArray, NoBasisArray, ChebyshevBasisArray}) -> Matrix{Float64}\n\nReturn a (npoints, 2) matrix of physical grid coordinates for a 2-D Cartesian grid.\n\nColumn 1 contains the i-direction (spline) coordinates; column 2 contains the j-direction (spline, for RR) or k-direction (Chebyshev, for RZ) coordinates.  Points are ordered with the j/k index varying fastest:  index (r-1)*jDim + l corresponds to (r, l).\n\nSee also: spectralTransform!, gridTransform!\n\n\n\n\n\ngetGridpoints(grid::SpringsteelGrid{CartesianGeometry, SplineBasisArray, SplineBasisArray, SplineBasisArray}) -> Matrix{Float64}\n\nReturn a (npoints, 3) matrix of physical (x, y, z) coordinates for a 3-D Cartesian Spline×Spline×Spline grid.\n\nPoints are ordered with k varying fastest, then j, then i: flat index (r-1)*jDim*kDim + (l-1)*kDim + z → (r, l, z).\n\nSee also: spectralTransform!, gridTransform!\n\n\n\n\n\ngetGridpoints(grid::SpringsteelGrid{CylindricalGeometry, SplineBasisArray, FourierBasisArray, NoBasisArray}) -> Matrix{Float64}\n\nReturn the (r, λ) physical gridpoint pairs for the 2-D cylindrical grid.\n\nEach radius has a different number of azimuthal grid points (ring size 4 + 4rᵢ where rᵢ = r + patchOffsetL), so the result is a flat matrix of all (r, λ) pairs in physical-array order.\n\nReturns\n\nA Matrix{Float64} of size (jDim, 2) where column 1 is the radial coordinate and column 2 is the azimuthal (λ) coordinate.  Rows are ordered by radius first, then azimuthal index within each ring.\n\nExample\n\ngp   = SpringsteelGridParameters(geometry=\"RL\", num_cells=10,\n                                  iMin=0.0, iMax=100.0,\n                                  vars=Dict(\"u\"=>1),\n                                  BCL=Dict(\"u\"=>CubicBSpline.R0),\n                                  BCR=Dict(\"u\"=>CubicBSpline.R0))\ngrid = createGrid(gp)\npts  = getGridpoints(grid)\nsize(pts) == (grid.params.jDim, 2)   # true\n\nSee also: spectralTransform!, gridTransform!\n\n\n\n\n\ngetGridpoints(grid::SpringsteelGrid{CylindricalGeometry, SplineBasisArray, FourierBasisArray, ChebyshevBasisArray}) -> Matrix{Float64}\n\nReturn the (r, λ, z) physical gridpoint triples for the 3-D cylindrical grid.\n\nPoints are ordered with z varying fastest, then λ within a ring, then r: flat index zi + (l-1)*kDim + z - 1 for ring starting offset zi.\n\nReturns\n\nA Matrix{Float64} of size (kDim*jDim, 3) where columns are (r, λ, z).\n\nSee also: spectralTransform!, gridTransform!\n\n\n\n\n\ngetGridpoints(grid::SpringsteelGrid{SphericalGeometry, SplineBasisArray, FourierBasisArray, NoBasisArray}) -> Matrix{Float64}\n\nReturn the (θ, λ) physical gridpoint pairs for the 2-D spherical (SL) grid.\n\nEach colatitude ring has a different number of azimuthal grid points determined by lpoints = 4 * kmax_ring where kmax_ring ≈ sin(θ) * max_ri.\n\nReturns\n\nA Matrix{Float64} of size (jDim, 2) where column 1 is the colatitude θ and column 2 is the longitude λ.  Rows are ordered by colatitude first, then azimuthal index within each ring.\n\nSee also: spectralTransform!, gridTransform!\n\n\n\n\n\ngetGridpoints(grid::SpringsteelGrid{SphericalGeometry, SplineBasisArray, FourierBasisArray, ChebyshevBasisArray}) -> Matrix{Float64}\n\nReturn the (θ, λ, z) physical gridpoint triples for the 3-D spherical (SLZ) grid.\n\nPoints are ordered z-fastest: for each ring (θ, λ) the kDim vertical levels are contiguous, analogous to the RLZ physical layout.\n\nReturns\n\nA Matrix{Float64} of size (kDim*jDim, 3) where columns are (θ, λ, z).\n\nSee also: spectralTransform!, gridTransform!\n\n\n\n\n\n","title":"Springsteel.getGridpoints"},{"category":"function","location":"springsteel_grid/#Springsteel.getRegularGridpoints","page":"SpringsteelGrid","text":"getRegularGridpoints(grid::SpringsteelGrid{G, SplineBasisArray, NoBasisArray, NoBasisArray}) -> Vector{Float64}\n\nReturn i_regular_out uniformly-spaced output locations spanning [iMin, iMax].\n\nUnlike getGridpoints, which returns the (unevenly-spaced) Gaussian mish points, this function returns a regular grid suitable for visualisation or file I/O. The number of points is grid.params.i_regular_out (default: num_cells + 1).\n\nArguments\n\ngrid: A 1-D Cartesian SpringsteelGrid (R_Grid / Spline1D_Grid).\n\nReturns\n\nA Vector{Float64} of length grid.params.i_regular_out uniformly spaced from grid.params.iMin to grid.params.iMax.\n\nExample\n\ngp   = SpringsteelGridParameters(geometry=\"Spline1D\", num_cells=10,\n                                  iMin=0.0, iMax=10.0,\n                                  vars=Dict(\"u\"=>1),\n                                  BCL=Dict(\"u\"=>CubicBSpline.R0),\n                                  BCR=Dict(\"u\"=>CubicBSpline.R0))\ngrid = createGrid(gp)\npts  = getRegularGridpoints(grid)  # 11 evenly-spaced points in [0, 10]\n\nSee also: regularGridTransform, getGridpoints, write_grid\n\n\n\n\n\ngetRegularGridpoints(grid::_2DCartesianRR) -> Matrix{Float64}\n\nReturn an (n_i × n_j, 2) matrix of uniformly-spaced (x, y) coordinates for a 2-D Cartesian Spline×Spline (RR) grid.\n\nUnlike getGridpoints, which returns the unevenly-spaced Gaussian mish points, this function returns a regular tensor-product grid for visualisation and file I/O.\n\nThe output dimensions are controlled by SpringsteelGridParameters fields:\n\ni_regular_out — x-points in [iMin, iMax]   (default num_cells + 1)\nj_regular_out — y-points in [jMin, jMax]   (default iDim * 2 + 1)\n\nPoints are ordered x-outer, y-inner (y varies fastest), matching regularGridTransform.\n\nSee also: regularGridTransform, getGridpoints\n\n\n\n\n\ngetRegularGridpoints(grid::_2DCartesianRZ) -> Matrix{Float64}\n\nReturn an (n_i × n_k, 2) matrix of uniformly-spaced (x, z) coordinates for a 2-D Cartesian Spline×Chebyshev (RZ) grid.\n\nUnlike getGridpoints, which returns the unevenly-spaced Gaussian and Chebyshev mish points, this function returns a regular tensor-product grid for visualisation and file I/O.\n\nThe output dimensions are controlled by SpringsteelGridParameters fields:\n\ni_regular_out — x-points in [iMin, iMax]   (default num_cells + 1)\nk_regular_out — z-points in [kMin, kMax]   (default kDim + 1)\n\nPoints are ordered x-outer, z-inner (z varies fastest), matching regularGridTransform.\n\nSee also: regularGridTransform, getGridpoints\n\n\n\n\n\ngetRegularGridpoints(grid::_3DCartesianRRR) -> Matrix{Float64}\n\nReturn an (n_x × n_y × n_z, 3) matrix of uniformly-spaced (x, y, z) coordinates for a 3-D Cartesian Spline×Spline×Spline (RRR) grid.\n\nUnlike getGridpoints, which returns the unevenly-spaced Gaussian mish points, this function returns a regular tensor-product grid for visualisation and file I/O.\n\nThe output dimensions are controlled by SpringsteelGridParameters fields:\n\ni_regular_out — x-points in [iMin, iMax]   (default num_cells + 1)\nj_regular_out — y-points in [jMin, jMax]   (default iDim * 2 + 1)\nk_regular_out — z-points in [kMin, kMax]   (default kDim + 1)\n\nPoints are ordered x-outer, y-middle, z-inner (z varies fastest), matching regularGridTransform.\n\nSee also: regularGridTransform, getGridpoints\n\n\n\n\n\ngetRegularGridpoints(grid::_RLGrid) -> Matrix{Float64}\n\nReturn an (n_r × n_λ, 2) matrix of uniformly-spaced (r, λ) coordinates for a 2-D cylindrical (RL) grid.\n\nUnlike getGridpoints, which returns the unevenly-spaced Gaussian mish points (with more azimuthal gridpoints at larger radii), this function returns a regular tensor-product grid suitable for visualisation and file I/O.\n\nThe output dimensions are controlled by SpringsteelGridParameters fields:\n\ni_regular_out — radial points spanning [iMin, iMax]   (default num_cells + 1)\nj_regular_out — azimuthal points spanning [0, 2π)     (default iDim * 2 + 1)\n\nPoints are ordered r-outer, λ-inner (λ varies fastest), matching the layout produced by regularGridTransform.\n\nSee also: regularGridTransform, getGridpoints\n\n\n\n\n\ngetRegularGridpoints(grid::_RLZGrid) -> Matrix{Float64}\n\nReturn an (n_r × n_λ × n_z, 3) matrix of uniformly-spaced (r, λ, z) coordinates for a 3-D cylindrical (RLZ) grid.\n\nThe output dimensions are controlled by SpringsteelGridParameters fields:\n\ni_regular_out — radial points in [iMin, iMax]     (default num_cells + 1)\nj_regular_out — azimuthal points in [0, 2π)       (default iDim * 2 + 1)\nk_regular_out — vertical points in [kMin, kMax]   (default kDim + 1)\n\nPoints are ordered r-outer, λ-middle, z-inner (z varies fastest), matching the layout of regularGridTransform.\n\nSee also: regularGridTransform, getGridpoints\n\n\n\n\n\ngetRegularGridpoints(grid::_SLGrid) -> Matrix{Float64}\n\nReturn an (n_θ × n_λ, 2) matrix of uniformly-spaced (θ, λ) coordinates for a 2-D spherical (SL) grid.\n\nUnlike getGridpoints, which returns the unevenly-spaced spherical mish points (with sin(θ)-dependent ring sizes), this function returns a regular tensor-product grid suitable for visualisation and file I/O.\n\nThe output dimensions are controlled by SpringsteelGridParameters fields:\n\ni_regular_out — colatitude points spanning [iMin, iMax]   (default num_cells + 1)\nj_regular_out — longitude points spanning [0, 2π)          (default iDim * 2 + 1)\n\nPoints are ordered θ-outer, λ-inner (λ varies fastest), matching the layout produced by regularGridTransform.\n\nSee also: regularGridTransform, getGridpoints\n\n\n\n\n\ngetRegularGridpoints(grid::_SLZGrid) -> Matrix{Float64}\n\nReturn an (n_θ × n_λ × n_z, 3) matrix of uniformly-spaced (θ, λ, z) coordinates for a 3-D spherical (SLZ) grid.\n\nThe output dimensions are controlled by SpringsteelGridParameters fields:\n\ni_regular_out — colatitude points in [iMin, iMax]   (default num_cells + 1)\nj_regular_out — longitude points in [0, 2π)          (default iDim * 2 + 1)\nk_regular_out — vertical points in [kMin, kMax]      (default kDim + 1)\n\nPoints are ordered θ-outer, λ-middle, z-inner (z varies fastest), matching the layout of regularGridTransform.\n\nSee also: regularGridTransform, getGridpoints\n\n\n\n\n\n","title":"Springsteel.getRegularGridpoints"},{"category":"function","location":"springsteel_grid/#Springsteel.regularGridTransform","page":"SpringsteelGrid","text":"regularGridTransform(grid::SpringsteelGrid{G, SplineBasisArray, NoBasisArray, NoBasisArray}, gridpoints::AbstractVector{Float64}) -> Array{Float64}\n\nEvaluate the B-spline representations at arbitrary output locations gridpoints, returning field values and first/second derivatives.\n\nApplies SAtransform! to the current grid.spectral coefficients (so the grid's spectral array must be up-to-date), then evaluates the field and its first and second derivatives at every point in gridpoints.\n\nArguments\n\ngrid: A 1-D Cartesian SpringsteelGrid.\ngridpoints: Output evaluation locations; typically from getRegularGridpoints but any points within [iMin, iMax] are valid.\n\nReturns\n\nAn Array{Float64} of shape (length(gridpoints), nvars, 3) where the third axis is:\n\n[:, :, 1] — field values\n[:, :, 2] — first derivatives (∂f/∂r)\n[:, :, 3] — second derivatives (∂²f/∂r²)\n\nExample\n\nspectralTransform!(grid)\nreg_pts  = getRegularGridpoints(grid)\nreg_phys = regularGridTransform(grid, reg_pts)\n\nSee also: getRegularGridpoints, gridTransform!, write_grid\n\n\n\n\n\nregularGridTransform(grid::_2DCartesianRR, i_pts, j_pts) -> Array{Float64}\nregularGridTransform(grid::_2DCartesianRR, gridpoints)   -> Array{Float64}\n\nEvaluate the RR spectral representation on a regular tensor-product x × y grid, returning field values and all five derivatives.\n\ngrid.spectral must be populated (call spectralTransform! first).\n\nAlgorithm (two-level tensor-product):\n\nEvaluate i-splines at i_pts for all j-modes → buffer ibuf[n_i, b_jDim].\nFor each i_out, set j-spline b-coefficients from ibuf[i_out, :], then evaluate at j_pts.\n\nReturns\n\nArray{Float64} of shape (n_i × n_j, nvars, 5) — y varies fastest.  Derivative slots:\n\n[:,:,1] — f(x, y)\n[:,:,2] — ∂f/∂x\n[:,:,3] — ∂²f/∂x²\n[:,:,4] — ∂f/∂y\n[:,:,5] — ∂²f/∂y²\n\nExample\n\nspectralTransform!(grid_rr)\nreg_pts  = getRegularGridpoints(grid_rr)\nreg_phys = regularGridTransform(grid_rr, reg_pts)\n\nSee also: getRegularGridpoints, gridTransform!\n\n\n\n\n\nregularGridTransform(grid::_2DCartesianRZ, i_pts, k_pts) -> Array{Float64}\nregularGridTransform(grid::_2DCartesianRZ, gridpoints)   -> Array{Float64}\n\nEvaluate the RZ spectral representation on a regular tensor-product x × z grid, returning field values and all five derivatives.\n\ngrid.spectral must be populated (call spectralTransform! first).\n\nAlgorithm (two-level tensor-product):\n\nEvaluate i-splines at i_pts for all Chebyshev modes → buffer ibuf[n_i, b_kDim].\nFor each i_out, set Chebyshev b-coefficients from ibuf[i_out, :], apply CAtransform!, then evaluate at k_pts using direct polynomial evaluation.\n\nReturns\n\nArray{Float64} of shape (n_i × n_k, nvars, 5) — z varies fastest.  Derivative slots:\n\n[:,:,1] — f(x, z)\n[:,:,2] — ∂f/∂x\n[:,:,3] — ∂²f/∂x²\n[:,:,4] — ∂f/∂z\n[:,:,5] — ∂²f/∂z²\n\nExample\n\nspectralTransform!(grid_rz)\nreg_pts  = getRegularGridpoints(grid_rz)\nreg_phys = regularGridTransform(grid_rz, reg_pts)\n\nSee also: getRegularGridpoints, gridTransform!\n\n\n\n\n\nregularGridTransform(grid::_3DCartesianRRR, x_pts, y_pts, z_pts) -> Array{Float64}\nregularGridTransform(grid::_3DCartesianRRR, gridpoints)           -> Array{Float64}\n\nEvaluate the RRR spectral representation on a regular tensor-product x × y × z grid.\n\ngrid.spectral must be populated (call spectralTransform! first).\n\nAlgorithm (three-level tensor-product):\n\nEvaluate i-splines at x_pts for all (j_coeff, k_coeff) pairs → ibuf[x, j, k].\nFor each x_out, evaluate j-splines at y_pts using ibuf[x_out,:,k] as B-coefficients.\nFor each (x_out, y_out), evaluate k-splines at z_pts for the final field values.\n\nThe existing grid basis objects are reused as scratch buffers (their .b and .a fields are overwritten; grid.spectral provides the authoritative spectral state).\n\nReturns\n\nArray{Float64} of shape (n_x × n_y × n_z, nvars, 7) — z varies fastest.  Derivative slots follow the same convention as gridTransform! for RRR:\n\n[:,:,1] — f, [:,:,2] — ∂f/∂x, [:,:,3] — ∂²f/∂x²\n[:,:,4] — ∂f/∂y, [:,:,5] — ∂²f/∂y²\n[:,:,6] — ∂f/∂z, [:,:,7] — ∂²f/∂z²\n\nExample\n\nspectralTransform!(grid_rrr)\nreg_pts  = getRegularGridpoints(grid_rrr)\nreg_phys = regularGridTransform(grid_rrr, reg_pts)\n\nSee also: getRegularGridpoints, gridTransform!\n\n\n\n\n\nregularGridTransform(grid::_RLGrid, r_pts, λ_pts) -> Array{Float64}\nregularGridTransform(grid::_RLGrid, gridpoints)   -> Array{Float64}\n\nEvaluate the RL spectral representation on a regular tensor-product r × λ grid, returning field values and all five derivatives.\n\ngrid.spectral must be populated (call spectralTransform! first).\n\nAlgorithm: Evaluates each Fourier-wavenumber radial spline at r_pts once (the a_k(r) amplitudes), then reconstructs the field via the Fourier series at every (r, λ) pair.  The FFTW R2HC convention stores k≥1 cosine/sine amplitudes at half their physical-space value, so the reconstruction multiplies k≥1 terms by 2: f(rλ) = a_0(r) + 2sum_k a^c_k(r)cos(kλ) + a^s_k(r)sin(kλ). Cost: O(kDim × n_r) spline evaluations plus O(n_r × n_λ × kDim) floating-point operations.\n\nReturns\n\nArray{Float64} of shape (n_r × n_λ, nvars, 5) — λ varies fastest.  Derivative slots:\n\n[:,:,1] — f(r, λ)\n[:,:,2] — ∂f/∂r\n[:,:,3] — ∂²f/∂r²\n[:,:,4] — ∂f/∂λ\n[:,:,5] — ∂²f/∂λ²\n\nExample\n\nspectralTransform!(grid_rl)\nreg_pts  = getRegularGridpoints(grid_rl)\nreg_phys = regularGridTransform(grid_rl, reg_pts)\n\nSee also: getRegularGridpoints, gridTransform!\n\n\n\n\n\nregularGridTransform(grid::_RLZGrid, r_pts, λ_pts, z_pts) -> Array{Float64}\nregularGridTransform(grid::_RLZGrid, gridpoints)           -> Array{Float64}\n\nEvaluate the RLZ spectral representation on a regular tensor-product r × λ × z grid.\n\ngrid.spectral must be populated (call spectralTransform! first).\n\nAlgorithm:\n\nFor each Chebyshev level z_b and Fourier wavenumber k, evaluate the radial B-spline at r_pts.\nSum the Fourier series at each λ for every (r, z_b) pair, multiplying k≥1 terms by 2 to account for the FFTW R2HC half-amplitude convention.\nEvaluate the Chebyshev series at z_pts using arbitrary-point DCT-I summation.\n\nReturns\n\nArray{Float64} of shape (n_r × n_λ × n_z, nvars, 7) — z varies fastest.  Slots:\n\n[:,:,1] — f, [:,:,2] — ∂f/∂r, [:,:,3] — ∂²f/∂r²\n[:,:,4] — ∂f/∂λ, [:,:,5] — ∂²f/∂λ²\n[:,:,6] — ∂f/∂z, [:,:,7] — ∂²f/∂z²\n\nExample\n\nspectralTransform!(grid_rlz)\nreg_pts  = getRegularGridpoints(grid_rlz)\nreg_phys = regularGridTransform(grid_rlz, reg_pts)\n\nSee also: getRegularGridpoints, gridTransform!\n\n\n\n\n\nregularGridTransform(grid::_SLGrid, θ_pts, λ_pts) -> Array{Float64}\nregularGridTransform(grid::_SLGrid, gridpoints)   -> Array{Float64}\n\nEvaluate the SL spectral representation on a regular tensor-product θ × λ grid, returning field values and all five derivatives.\n\nStructurally identical to the cylindrical RL regular-grid transform with the SL/RL wavenumber convention p = k*2.\n\ngrid.spectral must be populated (call spectralTransform! first).\n\nReturns\n\nArray{Float64} of shape (n_θ × n_λ, nvars, 5) — λ varies fastest.  Derivative slots:\n\n[:,:,1] — f(θ, λ)\n[:,:,2] — ∂f/∂θ\n[:,:,3] — ∂²f/∂θ²\n[:,:,4] — ∂f/∂λ\n[:,:,5] — ∂²f/∂λ²\n\nExample\n\nspectralTransform!(grid_sl)\nreg_pts  = getRegularGridpoints(grid_sl)\nreg_phys = regularGridTransform(grid_sl, reg_pts)\n\nSee also: getRegularGridpoints, gridTransform!\n\n\n\n\n\nregularGridTransform(grid::_SLZGrid, θ_pts, λ_pts, z_pts) -> Array{Float64}\nregularGridTransform(grid::_SLZGrid, gridpoints)           -> Array{Float64}\n\nEvaluate the SLZ spectral representation on a regular tensor-product θ × λ × z grid.\n\nStructurally identical to the cylindrical RLZ regular-grid transform with the SLZ/RLZ wavenumber convention p = (k-1)*2.\n\ngrid.spectral must be populated (call spectralTransform! first).\n\nReturns\n\nArray{Float64} of shape (n_θ × n_λ × n_z, nvars, 7) — z varies fastest.  Slots:\n\n[:,:,1] — f, [:,:,2] — ∂f/∂θ, [:,:,3] — ∂²f/∂θ²\n[:,:,4] — ∂f/∂λ, [:,:,5] — ∂²f/∂λ²\n[:,:,6] — ∂f/∂z, [:,:,7] — ∂²f/∂z²\n\nExample\n\nspectralTransform!(grid_slz)\nreg_pts  = getRegularGridpoints(grid_slz)\nreg_phys = regularGridTransform(grid_slz, reg_pts)\n\nSee also: getRegularGridpoints, gridTransform!\n\n\n\n\n\n","title":"Springsteel.regularGridTransform"},{"category":"function","location":"springsteel_grid/#Springsteel.write_grid","page":"SpringsteelGrid","text":"write_grid(grid::SpringsteelGrid{G, I, NoBasisArray, NoBasisArray}, output_dir, tag)\n\nWrite spectral coefficients, physical-space values, and a regular-grid interpolant for a 1-D SpringsteelGrid to three CSV files in output_dir:\n\nFile Contents\n$(tag)_spectral.csv columns r, then variable names; b_iDim rows\n$(tag)_physical.csv columns r, then <var>, <var>_r, <var>_rr; iDim rows\n$(tag)_gridded.csv same column layout as physical but on regular grid\n\nArguments\n\ngrid: 1-D SpringsteelGrid with an up-to-date physical (for the regular-grid output, the spectral array must also be current).\noutput_dir::String: Directory path for output files (must exist).\ntag::String: Filename prefix string.\n\nSee also: getGridpoints, getRegularGridpoints, regularGridTransform\n\n\n\n\n\nwrite_grid(grid::SpringsteelGrid{G, I, J, NoBasisArray}, output_dir, tag)\n\nWrite spectral coefficients and physical-space values for a 2-D SpringsteelGrid with an active j-dimension (e.g. RL_Grid, RR_Grid, SL_Grid) to two CSV files:\n\nFile Contents\n$(tag)_spectral.csv flat spectral indices with variable values\n$(tag)_physical.csv columns r, l, then 5 derivative slots per variable\n\nSee also: write_grid\n\n\n\n\n\nwrite_grid(grid::SpringsteelGrid{G, I, NoBasisArray, K}, output_dir, tag)\n\nWrite spectral coefficients and physical-space values for a 2-D SpringsteelGrid with an active k-dimension (e.g. RZ_Grid) to two CSV files:\n\nFile Contents\n$(tag)_spectral.csv flat spectral indices with variable values\n$(tag)_physical.csv columns r, z, then 5 derivative slots per variable\n\nSee also: write_grid\n\n\n\n\n\nwrite_grid(grid::SpringsteelGrid{G, I, J, K}, output_dir, tag)\n\nWrite spectral coefficients and physical-space values for a 3-D SpringsteelGrid (e.g. RLZ_Grid, RRR_Grid, SLZ_Grid) to two CSV files:\n\nFile Contents\n$(tag)_spectral.csv flat spectral indices with variable values\n$(tag)_physical.csv columns r, l, z, then 7 derivative slots per variable\n\nSee also: write_grid\n\n\n\n\n\n","title":"Springsteel.write_grid"},{"category":"function","location":"springsteel_grid/#Springsteel.read_physical_grid","page":"SpringsteelGrid","text":"read_physical_grid(file::String, grid::AbstractGrid)\n\nRead physical-space values from file (CSV format) into grid.\n\nThe CSV must have one column per variable (named with the variable key) and one row per physical gridpoint.  The function calls check_grid_dims to verify row counts before populating grid.physical[:, v, 1] for each variable v.\n\nArguments\n\nfile: Path to the CSV file.\ngrid: Any grid implementing the AbstractGrid interface.\n\nThrows\n\nDomainError if the CSV row count does not match the grid's physical array.\nDomainError if a required variable column is absent from the CSV.\n\nSee also: write_grid, check_grid_dims\n\n\n\n\n\n","title":"Springsteel.read_physical_grid"},{"category":"function","location":"springsteel_grid/#Springsteel.check_grid_dims","page":"SpringsteelGrid","text":"check_grid_dims(physical_data::DataFrame, grid::SpringsteelGrid)\n\nVerify that the number of rows in physical_data matches the physical-array row count of grid (size(grid.physical, 1)).\n\nThrows a DomainError when sizes do not match, with the actual row count as the offending value.  Returns nothing on success.\n\nArguments\n\nphysical_data: A DataFrame whose rows correspond to physical gridpoints.\ngrid: Any SpringsteelGrid instance.\n\nThrows\n\nDomainError(actual, msg) if nrow(physical_data) ≠ size(grid.physical, 1).\n\nSee also: read_physical_grid, write_grid\n\n\n\n\n\n","title":"Springsteel.check_grid_dims"},{"category":"function","location":"springsteel_grid/#Springsteel.save_grid","page":"SpringsteelGrid","text":"save_grid(filename::String, grid::SpringsteelGrid; compress::Bool=true)\nload_grid(filename::String) -> SpringsteelGrid\n\nSave a SpringsteelGrid to a JLD2 binary file (save_grid) or reload one previously written by save_grid (load_grid).\n\nsave_grid stores only the three serializable components—params, spectral, and physical—together with a format-version tag.  FFTW plans and other non-serializable objects are intentionally omitted.\n\nload_grid reads those components, calls createGrid to reconstruct all basis objects and FFTW plans from params, then copies the archived spectral and physical arrays into the fresh grid.\n\nArguments — save_grid\n\nfilename: Destination path (conventionally *.jld2).\ngrid: Any SpringsteelGrid instance.\ncompress: When true (default) data are compressed with Zstd via JLD2.\n\nArguments — load_grid\n\nfilename: Path to a JLD2 file previously written by save_grid.\n\nReturns — load_grid\n\nA fully functional SpringsteelGrid with the same geometry, parameters, spectral coefficients, and physical values as the saved grid. FFTW plans are freshly initialised and ready for use.\n\nExample\n\nsave_grid(\"output.jld2\", grid)\ngrid2 = load_grid(\"output.jld2\")\n@assert grid2.spectral ≈ grid.spectral\n\nSee also: createGrid, write_grid, gridTransform!\n\n\n\n\n\n","title":"Springsteel.save_grid"},{"category":"function","location":"springsteel_grid/#Springsteel.load_grid","page":"SpringsteelGrid","text":"load_grid(filename::String) -> SpringsteelGrid\n\nReload a grid previously written by save_grid. Reads the serialised params, spectral, and physical arrays from filename, calls createGrid to reconstruct basis objects and FFTW plans, then copies the archived arrays into the fresh grid.\n\nThe returned grid is fully functional — transforms, interpolation, and the solver can all be applied to it immediately.\n\n\n\n\n\n","title":"Springsteel.load_grid"},{"category":"function","location":"springsteel_grid/#Springsteel.read_netcdf","page":"SpringsteelGrid","text":"read_netcdf(filename::String) -> Dict{String, Any}\n\nRead a CF-compliant NetCDF file (typically produced by write_netcdf) and return its contents as a plain [Dict].\n\nThe returned dictionary has four string-keyed entries:\n\nKey Type Contents\n\"dimensions\" Dict{String, Int} dimension name → length\n\"coordinates\" Dict{String, Vector{Float64}} coordinate name → values\n\"variables\" Dict{String, Array{Float64}} data variable name → array\n\"attributes\" Dict{String, Any} global attribute name → value\n\nCoordinate variables are identified by sharing their name with a NetCDF dimension.  All other variables are returned under \"variables\".  This format-independent structure can be passed directly to plotting libraries or used for further analysis without requiring a SpringsteelGrid.\n\nArguments\n\nfilename: Path to an existing NetCDF file.\n\nReturns\n\nDict{String, Any} as described above.\n\nExample\n\nspectralTransform!(grid)\nwrite_netcdf(\"output.nc\", grid)\ndata = read_netcdf(\"output.nc\")\nprintln(data[\"attributes\"][\"Conventions\"])  # \"CF-1.12\"\nx    = data[\"coordinates\"][\"x\"]\nu    = data[\"variables\"][\"u\"]\n\nSee also: write_netcdf, read_physical_grid\n\n\n\n\n\n","title":"Springsteel.read_netcdf"},{"category":"function","location":"springsteel_grid/#Springsteel.write_netcdf","page":"SpringsteelGrid","text":"write_netcdf(filename::String, grid::SpringsteelGrid{G, I, NoBasisArray, NoBasisArray};\n             include_derivatives::Bool=false,\n             global_attributes::Dict{String,Any}=Dict{String,Any}())\n\nWrite a CF-1.12-compliant NetCDF file for a 1-D SpringsteelGrid on its regular output grid.\n\nThe file contains one dimension (\"x\" for spline/Fourier-based grids, \"z\" for Chebyshev-based grids), one coordinate variable with the same name, and one data variable per entry in grid.params.vars.  If include_derivatives is true, two additional variables per field are added: first derivative (\"{var}_x\" / \"{var}_z\") and second derivative (\"{var}_xx\" / \"{var}_zz\").\n\nArguments\n\nfilename: Destination path (conventionally *.nc).\ngrid: 1-D SpringsteelGrid whose spectral array is current (i.e. spectralTransform! has been called).\ninclude_derivatives: When true, write first- and second-derivative variables alongside the field values.\nglobal_attributes: Extra key-value pairs merged into the NetCDF global attributes (e.g. Dict{String,Any}(\"institution\" => \"My Lab\")).\n\nExample\n\nspectralTransform!(grid)\nwrite_netcdf(\"output.nc\", grid; include_derivatives=true,\n             global_attributes=Dict{String,Any}(\"institution\" => \"NCAR\"))\n\nReturns\n\nnothing\n\nSee also: getRegularGridpoints, regularGridTransform, write_grid\n\n\n\n\n\nwrite_netcdf(filename::String,\n             grid::SpringsteelGrid{G, I, J, NoBasisArray};\n             include_derivatives::Bool=false,\n             global_attributes::Dict{String,Any}=Dict{String,Any}())\n\nWrite a CF-1.12-compliant NetCDF file for a 2-D SpringsteelGrid with an active j-dimension (e.g. RL_Grid, RR_Grid, SL_Grid).\n\nThe file contains two dimensions and coordinate variables. Coordinate names, units, and attributes follow the geometry:\n\nGeometry i-dim j-dim\nCartesian x (units \"1\") y (units \"1\")\nCylindrical radius (units \"1\") azimuth (units \"degrees\")\nSpherical latitude (units \"degrees_north\") longitude (units \"degrees_east\")\n\nFor spherical grids, colatitude values from getRegularGridpoints are converted to latitude (lat = 90 − θ·180/π) and the latitude axis is reversed to run south-to-north (CF convention).  Data arrays are flipped accordingly.\n\nArguments\n\nfilename: Destination path (conventionally *.nc).\ngrid: 2-D j-active SpringsteelGrid whose spectral array is current.\ninclude_derivatives: When true, write all five derivative slots alongside field values (slots 2–5: ∂/∂i, ∂²/∂i², ∂/∂j, ∂²/∂j²).\nglobal_attributes: Extra key-value pairs merged into the NetCDF global attributes.\n\nExample\n\nspectralTransform!(rl_grid)\nwrite_netcdf(\"output.nc\", rl_grid)\n\nReturns\n\nnothing\n\nSee also: getRegularGridpoints, regularGridTransform, write_netcdf\n\n\n\n\n\nwrite_netcdf(filename::String,\n             grid::SpringsteelGrid{G, I, NoBasisArray, K};\n             include_derivatives::Bool=false,\n             global_attributes::Dict{String,Any}=Dict{String,Any}())\n\nWrite a CF-1.12-compliant NetCDF file for a 2-D SpringsteelGrid with an active k-dimension (e.g. RZ_Grid).\n\nThe file contains two dimensions: the i-coordinate and the k-coordinate. Coordinate names follow the geometry (e.g. x and z for Cartesian grids).\n\nIf include_derivatives=true, five slots are written per variable: value, ∂/∂i, ∂²/∂i², ∂/∂k, ∂²/∂k² (suffixes \"_x\", \"_xx\", \"_z\", \"_zz\" for Cartesian grids).\n\nArguments\n\nfilename: Destination path (conventionally *.nc).\ngrid: 2-D k-active SpringsteelGrid whose spectral array is current (i.e. spectralTransform! has been called).\ninclude_derivatives: When true, write first- and second-derivative variables alongside the field values.\nglobal_attributes: Extra key-value pairs merged into the NetCDF global attributes (e.g. Dict{String,Any}(\"institution\" => \"My Lab\")).\n\nReturns\n\nnothing\n\nExample\n\nspectralTransform!(rz_grid)\nwrite_netcdf(\"output.nc\", rz_grid; include_derivatives=true)\n\nSee also: getRegularGridpoints, regularGridTransform, write_netcdf\n\n\n\n\n\nwrite_netcdf(filename::String,\n             grid::SpringsteelGrid{G, I, J, K};\n             include_derivatives::Bool=false,\n             global_attributes::Dict{String,Any}=Dict{String,Any}())\n\nWrite a CF-1.12-compliant NetCDF file for a 3-D SpringsteelGrid (e.g. RLZ_Grid, RRR_Grid, SLZ_Grid).\n\nThe file contains three dimensions and coordinate variables. Coordinate names, units, and attributes follow the geometry:\n\nGeometry i-dim j-dim k-dim\nCartesian x (\"1\") y (\"1\") z (\"1\")\nCylindrical radius (\"1\") azimuth (\"degrees\") height (\"1\")\nSpherical latitude (\"degrees_north\") longitude (\"degrees_east\") height (\"1\")\n\nFor spherical grids, colatitude → latitude conversion and south-to-north latitude axis reversal are applied (CF convention).\n\nIf include_derivatives=true, seven slots are written per variable: value plus the six first/second derivative pairs for each dimension.\n\nArguments\n\nfilename: Destination path (conventionally *.nc).\ngrid: 3-D SpringsteelGrid whose spectral array is current (i.e. spectralTransform! has been called).\ninclude_derivatives: When true, write first- and second-derivative variables alongside the field values.\nglobal_attributes: Extra key-value pairs merged into the NetCDF global attributes (e.g. Dict{String,Any}(\"institution\" => \"My Lab\")).\n\nReturns\n\nnothing\n\nExample\n\nspectralTransform!(rlz_grid)\nwrite_netcdf(\"output.nc\", rlz_grid; include_derivatives=true)\n\nSee also: getRegularGridpoints, regularGridTransform, write_netcdf\n\n\n\n\n\n","title":"Springsteel.write_netcdf"},{"category":"type","location":"springsteel_grid/#Springsteel.R_Grid","page":"SpringsteelGrid","text":"R_Grid\n\nType alias for a 1-D Cartesian grid (Spline i-basis only).\n\nconst R_Grid = SpringsteelGrid{CartesianGeometry, SplineBasisArray{2}, NoBasisArray, NoBasisArray}\n\nSee also: Spline1D_Grid, SpringsteelGrid\n\n\n\n\n\n","title":"Springsteel.R_Grid"},{"category":"type","location":"springsteel_grid/#Springsteel.RR_Grid","page":"SpringsteelGrid","text":"RR_Grid\n\nType alias for a 2-D Cartesian grid with Spline i-basis and Spline j-basis.\n\nconst RR_Grid = SpringsteelGrid{CartesianGeometry, SplineBasisArray{2}, SplineBasisArray{2}, NoBasisArray}\n\nSee also: Spline2D_Grid, SpringsteelGrid\n\n\n\n\n\n","title":"Springsteel.RR_Grid"},{"category":"type","location":"springsteel_grid/#Springsteel.RZ_Grid","page":"SpringsteelGrid","text":"RZ_Grid\n\nType alias for a 2-D Cartesian grid with Spline i-basis and Chebyshev k-basis.\n\nconst RZ_Grid = SpringsteelGrid{CartesianGeometry, SplineBasisArray{2}, NoBasisArray, ChebyshevBasisArray{1}}\n\nSee also: SpringsteelGrid\n\n\n\n\n\n","title":"Springsteel.RZ_Grid"},{"category":"type","location":"springsteel_grid/#Springsteel.RL_Grid","page":"SpringsteelGrid","text":"RL_Grid\n\nType alias for a 2-D cylindrical grid (Spline i-basis, Fourier j-basis).\n\nconst RL_Grid = SpringsteelGrid{CylindricalGeometry, SplineBasisArray{2}, FourierBasisArray{2}, NoBasisArray}\n\nSee also: RLZ_Grid, SpringsteelGrid\n\n\n\n\n\n","title":"Springsteel.RL_Grid"},{"category":"type","location":"springsteel_grid/#Springsteel.RLZ_Grid","page":"SpringsteelGrid","text":"RLZ_Grid\n\nType alias for a 3-D cylindrical grid (Spline × Fourier × Chebyshev).\n\nconst RLZ_Grid = SpringsteelGrid{CylindricalGeometry, SplineBasisArray{2}, FourierBasisArray{2}, ChebyshevBasisArray{1}}\n\nSee also: RL_Grid, SpringsteelGrid\n\n\n\n\n\n","title":"Springsteel.RLZ_Grid"},{"category":"type","location":"springsteel_grid/#Springsteel.RRR_Grid","page":"SpringsteelGrid","text":"RRR_Grid\n\nType alias for a 3-D Cartesian grid (Spline × Spline × Spline).\n\nconst RRR_Grid = SpringsteelGrid{CartesianGeometry, SplineBasisArray{3}, SplineBasisArray{3}, SplineBasisArray{3}}\n\nSee also: SpringsteelGrid\n\n\n\n\n\n","title":"Springsteel.RRR_Grid"},{"category":"type","location":"springsteel_grid/#Springsteel.SL_Grid","page":"SpringsteelGrid","text":"SL_Grid\n\nType alias for a 2-D spherical grid (Spline × Fourier, latitudinally varying ring sizes).\n\nconst SL_Grid = SpringsteelGrid{SphericalGeometry, SplineBasisArray, FourierBasisArray, NoBasisArray}\n\nSee also: SLZ_Grid, RL_Grid, SpringsteelGrid\n\n\n\n\n\n","title":"Springsteel.SL_Grid"},{"category":"type","location":"springsteel_grid/#Springsteel.SLZ_Grid","page":"SpringsteelGrid","text":"SLZ_Grid\n\nType alias for a 3-D spherical grid (Spline × Fourier × Chebyshev).\n\nconst SLZ_Grid = SpringsteelGrid{SphericalGeometry, SplineBasisArray{2}, FourierBasisArray{2}, ChebyshevBasisArray{1}}\n\nSee also: SL_Grid, RLZ_Grid, SpringsteelGrid\n\n\n\n\n\n","title":"Springsteel.SLZ_Grid"},{"category":"type","location":"springsteel_grid/#Springsteel.Spline1D_Grid","page":"SpringsteelGrid","text":"Spline1D_Grid\n\nAlias for R_Grid.  Provided for backward compatibility.\n\nconst Spline1D_Grid = R_Grid\n\n\n\n\n\n","title":"Springsteel.Spline1D_Grid"},{"category":"type","location":"springsteel_grid/#Springsteel.Spline2D_Grid","page":"SpringsteelGrid","text":"Spline2D_Grid\n\nAlias for RR_Grid.  Provided for backward compatibility.\n\nconst Spline2D_Grid = RR_Grid\n\n\n\n\n\n","title":"Springsteel.Spline2D_Grid"},{"category":"type","location":"springsteel_grid/#Springsteel.Spline3D_Grid","page":"SpringsteelGrid","text":"Alias for RRR_Grid. geometry = \"Spline3D\" / \"Samurai\".\n\n\n\n\n\n","title":"Springsteel.Spline3D_Grid"},{"category":"type","location":"springsteel_grid/#Springsteel.Polar_Grid","page":"SpringsteelGrid","text":"Alias for RL_Grid. geometry = \"Polar\".\n\n\n\n\n\n","title":"Springsteel.Polar_Grid"},{"category":"type","location":"springsteel_grid/#Springsteel.Cylindrical_Grid","page":"SpringsteelGrid","text":"Alias for RLZ_Grid. geometry = \"Cylindrical\".\n\n\n\n\n\n","title":"Springsteel.Cylindrical_Grid"},{"category":"type","location":"springsteel_grid/#Springsteel.Samurai_Grid","page":"SpringsteelGrid","text":"Alias for RRR_Grid. geometry = \"Samurai\" / \"Spline3D\".\n\n\n\n\n\n","title":"Springsteel.Samurai_Grid"},{"category":"type","location":"springsteel_grid/#Springsteel.SphericalShell_Grid","page":"SpringsteelGrid","text":"Alias for SL_Grid. geometry = \"SphericalShell\".\n\n\n\n\n\n","title":"Springsteel.SphericalShell_Grid"},{"category":"type","location":"springsteel_grid/#Springsteel.Sphere_Grid","page":"SpringsteelGrid","text":"Alias for SLZ_Grid. geometry = \"Sphere\".\n\n\n\n\n\n","title":"Springsteel.Sphere_Grid"},{"category":"type","location":"springsteel_grid/#Springsteel.L_Grid","page":"SpringsteelGrid","text":"L_Grid\n\n1-D purely Fourier grid (geometry = \"L\").\n\nconst L_Grid = SpringsteelGrid{CartesianGeometry, FourierBasisArray, NoBasisArray, NoBasisArray}\n\n\n\n\n\n","title":"Springsteel.L_Grid"},{"category":"type","location":"springsteel_grid/#Springsteel.LL_Grid","page":"SpringsteelGrid","text":"LL_Grid\n\n2-D Fourier×Fourier grid (geometry = \"LL\").\n\nconst LL_Grid = SpringsteelGrid{CartesianGeometry, FourierBasisArray, FourierBasisArray, NoBasisArray}\n\n\n\n\n\n","title":"Springsteel.LL_Grid"},{"category":"type","location":"springsteel_grid/#Springsteel.LLZ_Grid","page":"SpringsteelGrid","text":"LLZ_Grid\n\n3-D Fourier×Fourier×Chebyshev grid (geometry = \"LLZ\").\n\nconst LLZ_Grid = SpringsteelGrid{CartesianGeometry, FourierBasisArray, FourierBasisArray, ChebyshevBasisArray}\n\n\n\n\n\n","title":"Springsteel.LLZ_Grid"},{"category":"type","location":"springsteel_grid/#Springsteel.Ring1D_Grid","page":"SpringsteelGrid","text":"Alias for L_Grid. geometry = \"Ring1D\".\n\n\n\n\n\n","title":"Springsteel.Ring1D_Grid"},{"category":"type","location":"springsteel_grid/#Springsteel.Ring2D_Grid","page":"SpringsteelGrid","text":"Alias for LL_Grid. geometry = \"Ring2D\".\n\n\n\n\n\n","title":"Springsteel.Ring2D_Grid"},{"category":"type","location":"springsteel_grid/#Springsteel.DoublyPeriodic_Grid","page":"SpringsteelGrid","text":"Alias for LLZ_Grid. geometry = \"DoublyPeriodic\".\n\n\n\n\n\n","title":"Springsteel.DoublyPeriodic_Grid"},{"category":"type","location":"springsteel_grid/#Springsteel.Z_Grid","page":"SpringsteelGrid","text":"Z_Grid\n\n1-D purely Chebyshev grid (geometry = \"Z\").\n\nconst Z_Grid = SpringsteelGrid{CartesianGeometry, ChebyshevBasisArray, NoBasisArray, NoBasisArray}\n\n\n\n\n\n","title":"Springsteel.Z_Grid"},{"category":"type","location":"springsteel_grid/#Springsteel.ZZ_Grid","page":"SpringsteelGrid","text":"ZZ_Grid\n\n2-D Chebyshev×Chebyshev grid (geometry = \"ZZ\").\n\nconst ZZ_Grid = SpringsteelGrid{CartesianGeometry, ChebyshevBasisArray, ChebyshevBasisArray, NoBasisArray}\n\n\n\n\n\n","title":"Springsteel.ZZ_Grid"},{"category":"type","location":"springsteel_grid/#Springsteel.ZZZ_Grid","page":"SpringsteelGrid","text":"ZZZ_Grid\n\n3-D Chebyshev×Chebyshev×Chebyshev grid (geometry = \"ZZZ\").\n\nconst ZZZ_Grid = SpringsteelGrid{CartesianGeometry, ChebyshevBasisArray, ChebyshevBasisArray, ChebyshevBasisArray}\n\n\n\n\n\n","title":"Springsteel.ZZZ_Grid"},{"category":"type","location":"springsteel_grid/#Springsteel.Column1D_Grid","page":"SpringsteelGrid","text":"Alias for Z_Grid. geometry = \"Column1D\".\n\n\n\n\n\n","title":"Springsteel.Column1D_Grid"},{"category":"type","location":"springsteel_grid/#Springsteel.Column2D_Grid","page":"SpringsteelGrid","text":"Alias for ZZ_Grid. geometry = \"Column2D\".\n\n\n\n\n\n","title":"Springsteel.Column2D_Grid"},{"category":"type","location":"springsteel_grid/#Springsteel.Column3D_Grid","page":"SpringsteelGrid","text":"Alias for ZZZ_Grid. geometry = \"Column3D\".\n\n\n\n\n\n","title":"Springsteel.Column3D_Grid"},{"category":"section","location":"multipatch/#Multi-Patch-Grids","page":"Multi-Patch Grids","text":"Springsteel supports grids composed of multiple B-spline patches linked by exact basis conversion coupling at their interfaces. A multi-patch grid lets you\n\nchain patches at different resolutions along a direction — useful for hurricanes, where a fine-resolution inner mesh handles the eyewall and a coarser outer mesh handles the environment;\nembed a fine patch inside a coarser outer patch — useful for nesting a limited-area high-resolution domain inside a larger global or environmental grid.\n\nAll interface data transfer happens through the existing spectral transforms — no interpolation, no re-projection, no loss.","title":"Multi-Patch Grids"},{"category":"section","location":"multipatch/#How-coupling-works","page":"Multi-Patch Grids","text":"Each patch is a normal SpringsteelGrid. What makes a set of patches into a multi-patch grid is a list of PatchInterface records, each describing one connection:\n\na primary patch with NaturalBC on the shared side — it runs freely, like any standalone grid;\na secondary patch with FixedBC (R3X) on the shared side — it receives spectral boundary values from the primary at each transform step;\na coupling matrix that converts three consecutive primary B-spline coefficients into three consecutive secondary border coefficients.\n\nThe coupling matrix depends only on the cell-width ratio between the two patches. Springsteel currently supports two ratios:\n\n1:1 — same cell width; the border coefficients are copied directly. See COUPLING_MATRIX_1X.\n2:1 — the fine patch has half the coarse cell width. The exact 3×3 matrix is derived from cubic B-spline basis evaluation at the staggered node positions. See COUPLING_MATRIX_2X.\n\nAny other ratio throws at construction time. The 2:1 case is exact for cubic B-splines — there is no interpolation error introduced at the interface, only the inherent basis truncation of each patch.","title":"How coupling works"},{"category":"section","location":"multipatch/#Transform-ordering","page":"Multi-Patch Grids","text":"For a forward transform on a multi-patch grid, the order is:\n\nspectralTransform! each patch independently.\nDone — spectral coefficients are now consistent across interfaces provided the input physical data was itself interface-consistent.\n\nFor an inverse transform, the order matters:\n\ngridTransform! the primary side of each interface first (the side with NaturalBC).\nCall update_interface! on each interface to copy the primary's border coefficients into the secondary's ahat vector.\ngridTransform! the secondary side — it now reconstructs physical values using the just-copied interface data through the R3X (inhomogeneous BC) code path.\n\nmultiGridTransform! handles this ordering automatically and is the entry point most user code should call.\n\nBoth update_interface! and multiGridTransform! on the parameterized MultiPatchGrid container are zero-allocation at steady state — the PatchInterface{P,S} parameters bind the concrete grid types so the dispatch path is fully specialised.","title":"Transform ordering"},{"category":"section","location":"multipatch/#Building-a-multi-patch-grid","page":"Multi-Patch Grids","text":"The high-level factory is createMultiGrid, which takes a configuration dict that describes topology, geometry, patch boundaries, and per-variable BCs. It auto-computes interface BCs (NaturalBC on primary sides, FixedBC on secondary sides), derives patchOffsetL for cylindrical/spherical geometries, and validates cell-width ratios.","title":"Building a multi-patch grid"},{"category":"section","location":"multipatch/#Chain-topology","page":"Multi-Patch Grids","text":"A chain links N patches along the radial / i direction. You pass N+1 boundary coordinates; adjacent patches share an interface at each interior boundary.\n\nusing Springsteel\n\nmg = createMultiGrid(Dict(\n    :topology   => :chain,\n    :geometry   => \"RL\",\n    :boundaries => [0.0, 50.0, 100.0],   # 2 patches: [0, 50] and [50, 100]\n    :cells      => [20, 10],              # 2:1 ratio (inner fine, outer coarse)\n    :vars       => Dict(\"u\" => 1),\n    :BCL        => Dict(\"u\" => NaturalBC()),\n    :BCR        => Dict(\"u\" => NaturalBC()),\n))\n\nThe inner patch (cells 20, width 2.5) is the secondary; the outer patch (cells 10, width 5.0) is the primary. Physical data flows outer → inner at the interface.\n\nFor equal-resolution chains, pass a scalar :cells:\n\nmg = createMultiGrid(Dict(\n    :topology   => :chain,\n    :geometry   => \"R\",\n    :boundaries => [0.0, 25.0, 50.0, 75.0],   # 3 patches\n    :cells      => 10,\n    :vars       => Dict(\"u\" => 1),\n    :BCL        => Dict(\"u\" => DirichletBC()),\n    :BCR        => Dict(\"u\" => DirichletBC()),\n))","title":"Chain topology"},{"category":"section","location":"multipatch/#Embedded-topology","page":"Multi-Patch Grids","text":"Embedded stacks fine patches inside a coarser outer patch. You pass domain bounds for each nested level, outermost first. The outer patch runs freely over the entire domain; the inner patches overwrite it inside their subdomains.\n\nmg = createMultiGrid(Dict(\n    :topology   => :embedded,\n    :geometry   => \"RL\",\n    :domains    => [(0.0, 100.0),     # outer\n                    (0.0,  50.0)],    # inner (2:1 refinement)\n    :cells      => [10, 20],\n    :vars       => Dict(\"u\" => 1),\n    :BCL        => Dict(\"u\" => NaturalBC()),\n    :BCR        => Dict(\"u\" => NaturalBC()),\n))\n\nThe outer patch provides background data everywhere; the inner patch receives R3X interface values at its outer edge from the outer patch and runs independently inside.","title":"Embedded topology"},{"category":"section","location":"multipatch/#Cell-width-/-refinement-constraint","page":"Multi-Patch Grids","text":"The fine patch must be at the half-gridpoint of the coarse patch for the 2:1 exact BC to work. createMultiGrid enforces this by computing patchOffsetL automatically for cylindrical and spherical geometries, but when you're hand-building patches via PatchChain / PatchEmbedded you are responsible for making sure adjacent patches are correctly aligned. The factory will throw if DX_primary / DX_secondary isn't 1.0 or 2.0 within tolerance.","title":"Cell-width / refinement constraint"},{"category":"section","location":"multipatch/#Low-level-factories","page":"Multi-Patch Grids","text":"If you need more control than createMultiGrid offers — for example mixing refinement ratios on different interfaces, or wiring up interfaces by hand on pre-built patches — use these direct factories:\n\nBoth take a vector of pre-constructed SpringsteelGrids and build the interface list, validating DX ratios and auto-computing coupling matrices.","title":"Low-level factories"},{"category":"section","location":"multipatch/#Relocating-a-multi-patch-grid","page":"Multi-Patch Grids","text":"The entire nest can be relocated as a unit with relocate_grid!:\n\nrelocate_grid!(mg, (Δx, Δy); boundary=:azimuthal_mean)\n\nFor embedded multigrids the outer patch provides OOB lookup data to the inner patches when they cross the old → new domain boundary. See Grid Relocation for the full relocation story.","title":"Relocating a multi-patch grid"},{"category":"section","location":"multipatch/#See-also","page":"Multi-Patch Grids","text":"Boundary Conditions — NaturalBC / FixedBC are what drive the primary / secondary roles\nSpringsteelGrid — single-patch grid type\nGrid Relocation — relocating a multi-patch nest\nDeveloper Notes / Contributing — TRAP-1..4 cover multi-patch spectral layout invariants","title":"See also"},{"category":"type","location":"multipatch/#Springsteel.PatchInterface","page":"Multi-Patch Grids","text":"PatchInterface\n\nDescribes a directional connection between a primary (coarse/free) patch and a secondary (fine/constrained) patch.  The primary patch has R0 (free) boundary conditions at the interface, and the secondary has R3X to receive data.\n\nAfter gridTransform! on the primary, call update_interface! to transfer spectral coefficients to the secondary's ahat vector before transforming the secondary.\n\nFields\n\nprimary::SpringsteelGrid — freely-evolving patch\nsecondary::SpringsteelGrid — patch receiving boundary data via R3X\nprimary_side::Symbol — :left or :right, which side of primary faces the interface\nsecondary_side::Symbol — :left or :right, which side of secondary receives data\ndimension::Symbol — connected dimension (:i only for now)\ncoupling_matrix::Matrix{Float64} — 3×3 basis conversion matrix\nis_stacked::Bool — true for interior (stacked) nest, false for hollow nest\nprimary_node_indices::Tuple{Int,Int,Int} — indices into primary .a for coefficient extraction\n\nSee also: MultiPatchGrid, update_interface!, PatchChain, PatchEmbedded\n\n\n\n\n\n","title":"Springsteel.PatchInterface"},{"category":"type","location":"multipatch/#Springsteel.MultiPatchGrid","page":"Multi-Patch Grids","text":"MultiPatchGrid\n\nContainer for a set of connected patches and their interfaces.  Stores a topologically sorted transform order so that primary patches are always transformed before their dependents.\n\nFields\n\npatches::Vector{<:SpringsteelGrid} — all patches in the multi-patch system\ninterfaces::Vector{PatchInterface} — all inter-patch connections\ntransform_order::Vector{Vector{Int}} — topological layers of patch indices\n\nSee also: PatchInterface, multiGridTransform!, PatchChain, PatchEmbedded\n\n\n\n\n\n","title":"Springsteel.MultiPatchGrid"},{"category":"constant","location":"multipatch/#Springsteel.COUPLING_MATRIX_1X","page":"Multi-Patch Grids","text":"COUPLING_MATRIX_1X\n\nIdentity 3×3 coupling matrix for 1:1 same-resolution B-spline basis conversion.\n\nUsed for domain decomposition where adjacent patches have the same cell width. The border coefficients are copied directly without transformation.\n\n\n\n\n\n","title":"Springsteel.COUPLING_MATRIX_1X"},{"category":"constant","location":"multipatch/#Springsteel.COUPLING_MATRIX_2X","page":"Multi-Patch Grids","text":"COUPLING_MATRIX_2X\n\nExact 3×3 coupling matrix for 2:1 coarse-to-fine B-spline basis conversion.\n\nMaps three primary (coarse) nodal amplitudes [A_{outer}, A_{interface}, A_{inner}] to three secondary (fine) border coefficients [a_{border-1}, a_{border}, a_{border+1}] at a patch interface where the fine grid cell width is exactly half the coarse.\n\nThe matrix arises from cubic B-spline basis evaluation at staggered node positions:\n\na_{border} and A_{interface} are collocated at the interface point\na_{border±1} sit at half-cell offsets relative to the coarse grid\n\nNo approximation — the values are exact for cubic B-splines with 2x refinement.\n\n\n\n\n\n","title":"Springsteel.COUPLING_MATRIX_2X"},{"category":"function","location":"multipatch/#Springsteel.update_interface!","page":"Multi-Patch Grids","text":"update_interface!(iface::PatchInterface)\n\nTransfer spectral coefficients from the primary patch to the secondary patch at the interface.\n\nMust be called after gridTransform! (or SAtransform!) on the primary and before gridTransform! on the secondary.  The primary's .a coefficients are extracted at the interface, multiplied by the 3×3 coupling matrix, and written to the secondary's .ahat vector.\n\nFor grids with dedicated splines per mode (R, RR, RRR), the transfer is performed independently for each j-spectral mode.\n\nFor grids with reused splines (RL, RLZ, SL, SLZ), per-wavenumber coupling is performed: B-coefficients are loaded from the spectral array, SAtransform! recovers A-coefficients, and the coupled ahat is stored in a per-wavenumber registry for use by gridTransform.\n\nExample\n\ngridTransform!(primary)\nupdate_interface!(iface)\ngridTransform!(secondary)\n\nSee also: PatchInterface, multiGridTransform!\n\n\n\n\n\n","title":"Springsteel.update_interface!"},{"category":"function","location":"multipatch/#Springsteel.multiGridTransform!","page":"Multi-Patch Grids","text":"multiGridTransform!(mpg::MultiPatchGrid)\n\nPerform a coupled inverse transform across all patches in topological order.\n\nFor each layer:\n\nCall gridTransform! on all patches in the layer (independent)\nTransfer interface coefficients for interfaces whose primary is in this layer\n\nThis ensures that primary patches are fully transformed before their coefficients are read by update_interface!.\n\nExample\n\n# Forward transform on all patches first\nspectralTransform!(mpg.patches[1])\nspectralTransform!(mpg.patches[2])\nspectralTransform!(mpg.patches[3])\n\n# Coupled inverse transform\nmultiGridTransform!(mpg)\n\nSee also: MultiPatchGrid, update_interface!\n\n\n\n\n\n","title":"Springsteel.multiGridTransform!"},{"category":"function","location":"multipatch/#Springsteel.createMultiGrid","page":"Multi-Patch Grids","text":"createMultiGrid(config::Dict{Symbol, Any}) -> SpringsteelMultiGrid\n\nConstruct a multi-patch grid from a configuration dictionary.\n\nAuto-computes interface BCs (NaturalBC for primary sides, FixedBC for secondary), patchOffsetL for cylindrical/spherical geometries, and validates DX ratios.\n\nRequired keys\n\n:topology — :chain or :embedded\n:geometry — \"R\", \"RL\", \"RLZ\", \"SL\", \"SLZ\", \"RR\", \"RRR\"\n:cells — Int (all equal) or Vector{Int} (per-patch)\n:vars — variable map\n:BCL, :BCR — outer BCs (per-variable Dict, required)\n\nChain: :boundaries => [x₁, x₂, ..., xₙ₊₁] (N+1 values → N patches)\n\nEmbedded: :domains => [(min₁,max₁), ..., (minₙ,maxₙ)] (outermost first)\n\nExample\n\nmg = createMultiGrid(Dict(\n    :topology   => :chain,\n    :geometry   => \"RL\",\n    :boundaries => [0.0, 50.0, 75.0],\n    :cells      => 10,\n    :vars       => Dict(\"u\" => 1),\n    :BCL        => Dict(\"u\" => NaturalBC()),\n    :BCR        => Dict(\"u\" => NaturalBC())))\nspectralTransform!(mg)\nmultiGridTransform!(mg)\n\nSee also: SpringsteelMultiGrid, PatchChain, PatchEmbedded\n\n\n\n\n\n","title":"Springsteel.createMultiGrid"},{"category":"type","location":"multipatch/#Springsteel.SpringsteelMultiGrid","page":"Multi-Patch Grids","text":"SpringsteelMultiGrid <: AbstractMultiGrid\n\nHigh-level multi-patch grid container, analogous to SpringsteelGrid for single grids.  Wraps a MultiPatchGrid (low-level typed internals) plus the construction config Dict for serialization and query.\n\nCreated by createMultiGrid.\n\nFields\n\nconfig::Dict{Symbol, Any} — construction configuration\nmpg::MultiPatchGrid — patches, interfaces, and topological transform order\n\n\n\n\n\n","title":"Springsteel.SpringsteelMultiGrid"},{"category":"function","location":"multipatch/#Springsteel.PatchChain","page":"Multi-Patch Grids","text":"PatchChain(grids; dimension=:i)\n\nCreate a MultiPatchGrid from a sequence of grids connected end-to-end.\n\nGrids are provided in spatial order (e.g., left to right).  At each interface between adjacent grids, the coarser grid is automatically selected as primary (R0 BC side) and the finer grid as secondary (R3X BC side).  For 1:1 ratio (same resolution), the left grid is primary by convention.\n\nSupports asymmetric refinement chains like 8-4-2-1-2-4-8 DX, where the primary/secondary direction flips at the finest grid.\n\nArguments\n\ngrids::Vector{<:SpringsteelGrid}: Two or more grids in spatial order.\ndimension::Symbol=:i: Connected dimension (:i only for now).\n\nBC requirements\n\nAt each interface: the primary side must have R0, the secondary side R3X.\nEnd grids may have any user-chosen BCs on their outer (non-interface) edges.\n\nExample\n\n# 3-grid chain: coarse — fine — coarse\nmpg = PatchChain([left_grid, center_grid, right_grid])\nmultiGridTransform!(mpg)\n\n# 7-grid chain: 8-4-2-1-2-4-8 DX\nmpg = PatchChain([g8a, g4a, g2a, g1, g2b, g4b, g8b])\n\nSee also: PatchEmbedded, PatchInterface, MultiPatchGrid\n\n\n\n\n\n","title":"Springsteel.PatchChain"},{"category":"function","location":"multipatch/#Springsteel.PatchEmbedded","page":"Multi-Patch Grids","text":"PatchEmbedded(grids; dimension=:i)\n\nCreate a MultiPatchGrid from a sequence of grids nested inside each other.\n\nGrids are provided from outermost to innermost.  Each inner grid is spatially contained within its predecessor and receives R3X boundary data from it on both sides.  The outermost grid keeps its user-specified BCs.\n\nAll inner grids must have strictly finer resolution than their parent (1:1 ratio is not allowed for embedded patches).\n\nArguments\n\ngrids::Vector{<:SpringsteelGrid}: Two or more grids, outermost first.\ndimension::Symbol=:i: Connected dimension (:i only for now).\n\nBC requirements\n\nInner grids must have R3X on both sides in the connected dimension.\nThe outermost grid may have any user-chosen BCs.\n\nExample\n\n# 2-level embedding\nmpg = PatchEmbedded([coarse_grid, fine_grid])\n\n# 3-level embedding\nmpg = PatchEmbedded([coarse_grid, medium_grid, fine_grid])\nmultiGridTransform!(mpg)\n\nSee also: PatchChain, PatchInterface, MultiPatchGrid\n\n\n\n\n\n","title":"Springsteel.PatchEmbedded"},{"category":"section","location":"#Springsteel.jl","page":"Home","text":"<p align=\"center\">\n  <img src=\"assets/logo.png\" alt=\"Springsteel.jl\" width=\"320\"/>\n</p>\n\nDocumentation for Springsteel.","title":"Springsteel.jl"},{"category":"section","location":"#Overview","page":"Home","text":"Springsteel is a semi-spectral grid engine for numerical modelling in Julia. It provides a unified SpringsteelGrid{G, I, J, K} parametric type that supports any combination of three basis function families in up to three dimensions:\n\nCubic B-splines (CubicBSpline) — compact support, ideal for the radial/Cartesian direction\nFourier series (Fourier) — periodic domains such as azimuthal coordinates\nChebyshev polynomials (Chebyshev) — non-periodic bounded domains such as vertical coordinates\n\nSpringsteel serves as the computational foundation for Scythe.jl (numerical model) and Daisho.jl (data analysis/assimilation).","title":"Overview"},{"category":"section","location":"#Supported-Grid-Types","page":"Home","text":"Geometry String Basis Functions Type Alias Coordinate System\n\"R\" / \"Spline1D\" Spline R_Grid Cartesian 1D\n\"RR\" / \"Spline2D\" Spline × Spline RR_Grid Cartesian 2D\n\"RZ\" Spline × Chebyshev RZ_Grid Cartesian 2D (axisymmetric)\n\"RL\" Spline × Fourier RL_Grid Cylindrical 2D (polar)\n\"RRR\" Spline × Spline × Spline RRR_Grid Cartesian 3D\n\"RLZ\" Spline × Fourier × Chebyshev RLZ_Grid Cylindrical 3D\n\"SL\" Spline × Fourier SL_Grid Spherical 2D\n\"SLZ\" Spline × Fourier × Chebyshev SLZ_Grid Spherical 3D","title":"Supported Grid Types"},{"category":"section","location":"#Installation","page":"Home","text":"Springsteel is a registered Julia package. To install, open the Julia REPL, enter Package mode by pressing ], and run:\n\npkg> add Springsteel\n\nOr equivalently from Julia code:\n\nusing Pkg\nPkg.add(\"Springsteel\")","title":"Installation"},{"category":"section","location":"#Development-installation","page":"Home","text":"If you are actively developing or modifying Springsteel, clone the repository and install in development mode:\n\ngit clone https://github.com/csu-tropical/Springsteel.jl.git\n\nThen in the Julia REPL Package mode:\n\npkg> dev /path/to/Springsteel.jl\n\nThis will track your local changes and recompile as needed.","title":"Development installation"},{"category":"section","location":"#Quick-Start","page":"Home","text":"using Springsteel\n\n# 1. Configure the grid\ngp = SpringsteelGridParameters(\n    geometry  = \"R\",\n    iMin      = 0.0,\n    iMax      = 100.0,\n    num_cells = 30,\n    BCL       = Dict(\"u\" => NaturalBC()),\n    BCR       = Dict(\"u\" => NaturalBC()),\n    vars      = Dict(\"u\" => 1)\n)\n\n# 2. Build the grid\ngrid = createGrid(gp)\n\n# 3. Fill the physical array\npts = getGridpoints(grid)\nfor i in eachindex(pts)\n    grid.physical[i, 1, 1] = exp(-(pts[i] / 20.0)^2)\nend\n\n# 4. Forward transform (physical → spectral)\nspectralTransform!(grid)\n\n# 5. Inverse transform (spectral → physical + derivatives)\ngridTransform!(grid)\n\n# grid.physical[:, 1, 1]  → reconstructed values\n# grid.physical[:, 1, 2]  → first derivative  ∂f/∂x\n# grid.physical[:, 1, 3]  → second derivative ∂²f/∂x²","title":"Quick Start"},{"category":"section","location":"#Derivative-Slot-Layout","page":"Home","text":"Dimensions Slots Contents\n1D 3 [f, ∂f/∂i, ∂²f/∂i²]\n2D 5 [f, ∂f/∂i, ∂²f/∂i², ∂f/∂j, ∂²f/∂j²]\n3D 7 [f, ∂f/∂i, ∂²f/∂i², ∂f/∂j, ∂²f/∂j², ∂f/∂k, ∂²f/∂k²]","title":"Derivative Slot Layout"},{"category":"section","location":"#Documentation-Contents","page":"Home","text":"","title":"Documentation Contents"},{"category":"section","location":"#User-guides","page":"Home","text":"Tutorial — step-by-step examples with 1D, 2D, and 3D grids\nSpringsteelGrid — core grid type, createGrid, transforms, tiling, and I/O\nBoundary Conditions — BoundaryConditions type system and convenience constructors\nSolver Framework — operator algebra DSL, solve / solve!, backends, block systems\nMulti-Patch Grids — chained and embedded B-spline patches with R3X coupling\nInterpolation — data import, same-geometry and cross-geometry interpolation\nGrid Relocation — RL/RLZ coordinate-shift for vortex-tracking workflows\nSpectral Filtering — wavenumber-domain filters for Fourier and Chebyshev bases","title":"User guides"},{"category":"section","location":"#Basis-modules","page":"Home","text":"CubicBSpline — cubic B-spline basis module\nFourier — Fourier basis module\nChebyshev — Chebyshev polynomial basis module","title":"Basis modules"},{"category":"section","location":"#For-contributors","page":"Home","text":"Contributing — development workflow, test conventions, architecture invariants, and v1.1+ roadmap","title":"For contributors"},{"category":"section","location":"tutorial/#Tutorial","page":"Tutorial","text":"This tutorial walks through the core Springsteel workflow — build a grid, transform between physical and spectral space, inspect derivatives — and then shows how the same grid plugs into the solver, interpolation, relocation, multi-patch, and I/O subsystems.\n\nThe goal is to get you from zero to solving a real problem in one reading. Each subsystem has its own reference page with deeper details:\n\nSpringsteelGrid — full grid-type catalog\nBoundary Conditions\nSolver Framework\nInterpolation\nGrid Relocation\nMulti-Patch Grids\nSpectral Filtering\n\n","title":"Tutorial"},{"category":"section","location":"tutorial/#1.-Your-first-grid","page":"Tutorial","text":"The simplest Springsteel grid uses cubic B-splines in a single dimension. SpringsteelGridParameters holds every configuration value; createGrid turns it into a live SpringsteelGrid:\n\nusing Springsteel\n\ngp = SpringsteelGridParameters(\n    geometry  = \"R\",\n    iMin      = -50.0,\n    iMax      =  50.0,\n    num_cells = 30,\n    vars      = Dict(\"gauss\" => 1),\n    BCL       = Dict(\"gauss\" => NaturalBC()),\n    BCR       = Dict(\"gauss\" => NaturalBC()),\n)\ngrid = createGrid(gp)\n\nThe grid's type is SpringsteelGrid{CartesianGeometry, SplineBasisArray, NoBasisArray, NoBasisArray}, aliased as R_Grid. Its storage layout:\n\nArray Shape Contents\ngrid.physical (iDim, n_vars, n_deriv) Function values and derivatives\ngrid.spectral (b_iDim, n_vars) B-spline coefficients\n\nFor 1D grids there are 3 derivative slots per variable: [f, ∂f/∂x, ∂²f/∂x²]. A 2D grid has 5 slots; 3D has 7.\n\nThe num_cells determines how many cells the domain is divided into. This is the spectral mesh and determines the grid spacing between Cubic B-spline nodes. The actual physical grid, or mish is finer than this, with a default split into 3 Gaussian quadrature points for each cell. With the default smoothing, the nominal resolution of the grid is therefore close to the nodal spacing but the actual grid spacing is 3 times finer.","title":"1. Your first grid"},{"category":"section","location":"tutorial/#2.-Fill,-transform,-inspect","page":"Tutorial","text":"Use getGridpoints to pull the gridpoint coordinates, fill grid.physical[:, var, 1] with your function values, then run spectralTransform! and gridTransform! for a full round trip:\n\npts = getGridpoints(grid)\nσ   = 10.0\ngrid.physical[:, 1, 1] .= exp.(-(pts ./ σ).^2)\n\nspectralTransform!(grid)    # physical → spectral\ngridTransform!(grid)         # spectral → physical + derivatives\n\n# Reconstructed values\noriginal   = exp.(-(pts ./ σ).^2)\nmax_err    = maximum(abs.(grid.physical[:, 1, 1] .- original))    # ≈ 1e-6\n\n# Analytic derivatives\ndgauss_dx  = @. -(2 * pts / σ^2) * exp(-(pts / σ)^2)\nd2gauss_dx = @. ((2 / σ^2) * (2 * pts^2 / σ^2 - 1)) * exp(-(pts / σ)^2)\n@assert maximum(abs.(grid.physical[:, 1, 2] .- dgauss_dx)) < 1e-5\n@assert maximum(abs.(grid.physical[:, 1, 3] .- d2gauss_dx)) < 1e-4\n\ngridTransform! always fills every derivative slot — you don't ask for a first or second derivative separately, they come as a group.","title":"2. Fill, transform, inspect"},{"category":"section","location":"tutorial/#3.-Boundary-conditions","page":"Tutorial","text":"Constraints at the domain edges are specified per-variable in direction dicts (BCL, BCR, BCD, BCU, BCB, BCT). The convenience constructors in Boundary Conditions cover every case:\n\n# Dirichlet u = 0 on both ends\nBCL = Dict(\"u\" => DirichletBC())\nBCR = Dict(\"u\" => DirichletBC())\n\n# Inhomogeneous Dirichlet u(x_max) = 1\nBCR = Dict(\"u\" => DirichletBC(1.0))\n\n# Mixed — Dirichlet on left, Neumann (zero flux) on right\nBCL = Dict(\"u\" => DirichletBC())\nBCR = Dict(\"u\" => NeumannBC())\n\n# Robin αu + βu' = γ\nBCR = Dict(\"T\" => RobinBC(1.0, 0.2, 293.15))\n\nAn older specification that maps to Ooyama (2002) nomenclature Dict(\"u\" => CubicBSpline.R0) form still works and can be mixed in the same spec, but new code should use the BoundaryConditions constructors — they validate and the physical meaning is obvious at the call site.","title":"3. Boundary conditions"},{"category":"section","location":"tutorial/#4.-2D-grid-with-Fourier-and-multiple-variables","page":"Tutorial","text":"A 2D polar grid uses splines in radius and Fourier in azimuth:\n\ngp = SpringsteelGridParameters(\n    geometry   = \"RL\",\n    iMin       = 0.0, iMax = 50.0,\n    num_cells  = 20,\n    vars       = Dict(\"u\" => 1, \"v\" => 2),\n    BCL        = Dict(\"default\" => NaturalBC()),\n    BCR        = Dict(\"default\" => DirichletBC()),\n)\ngrid = createGrid(gp)\n\nDict(\"default\" => ...) applies the same BC to every variable — use explicit keys to override per variable. Fourier dimensions don't take a user BC spec; they're implicitly periodic.\n\ngetGridpoints on a 2D grid returns an (N, 2) matrix:\n\npts   = getGridpoints(grid)       # N × 2: columns are [r, λ]\nrvals = pts[:, 1]\nλvals = pts[:, 2]\n\n# Fill a Gaussian vortex in u and an m=2 pattern in v\ngrid.physical[:, 1, 1] .= exp.(-(rvals ./ 15).^2)\ngrid.physical[:, 2, 1] .= exp.(-(rvals ./ 15).^2) .* cos.(2 .* λvals)\nspectralTransform!(grid)\ngridTransform!(grid)\n\nEach variable has its own slot in the trailing dimension of physical (integers from vars), and transforms process them independently — you never have to loop over variables yourself.\n\nOther frequently-used 2D / 3D geometries:\n\nGeometry Basis When to use\nRR Spline × Spline 2D Cartesian plane\nRZ Spline × Chebyshev 2D axisymmetric slab\nRLZ Spline × Fourier × Chebyshev 3D cylindrical (hurricane-like)\nRRR Spline × Spline × Spline 3D Cartesian volume\nSL / SLZ Spline × Fourier (× Chebyshev) Spherical shell / sphere\n\nSee SpringsteelGrid for the complete list of grid types and their type aliases.","title":"4. 2D grid with Fourier and multiple variables"},{"category":"section","location":"tutorial/#5.-Solving-a-boundary-value-problem","page":"Tutorial","text":"The v1.0 solver framework speaks operator-algebra notation. Build an unknown SpringsteelField, combine derivative atoms into an OperatorExpr, and pair it with an RHS.\n\nFor a 1D Poisson problem, configure the grid with two variables — one for the unknown u and one for the RHS f:\n\ngp = SpringsteelGridParameters(\n    geometry  = \"Z\",\n    iMin = 0.0, iMax = 1.0, iDim = 25, b_iDim = 25,\n    vars = Dict(\"u\" => 1, \"f\" => 2),\n    BCL  = Dict(\"u\" => DirichletBC(), \"f\" => NaturalBC()),\n    BCR  = Dict(\"u\" => DirichletBC(), \"f\" => NaturalBC()),\n)\ngrid = createGrid(gp)\n\n# 1D Poisson: u''(x) = f(x) with f(x) = -π² sin(πx)  →  u(x) = sin(πx)\npts = solver_gridpoints(grid, \"u\")\ngrid.physical[:, 2, 1] .= -π^2 .* sin.(π .* pts)    # write f (slot 2)\n\nu    = SpringsteelField(grid, \"u\")                   # unknown\nprob = SpringsteelProblem(grid, ∂ᵢ^2 * u => :f)      # pulls RHS from :f\n\nsolve!(prob)\n# Solution is now in grid.physical[:, 1, 1]; the original RHS is still\n# in grid.physical[:, 2, 1].\nmax_err = maximum(abs.(grid.physical[:, 1, 1] .- sin.(π .* pts)))\n@assert max_err < 1e-10\n\nWhy two variables? The RHS => :f tells the solver to read grid.physical[:, f_idx, 1] at every solve! call, and the writeback puts the solution into grid.physical[:, u_idx, 1]. Separating the two means the solve is non-destructive with respect to the RHS and you can solve repeatedly with updated RHS values — the canonical time-stepping pattern:\n\n# For a time-stepping loop:\n# while stepping\n#     grid.physical[:, 2, 1] .= new_rhs     # refresh f\n#     solve!(prob)                           # writes u into slot 1\n# end\n\nIt's technically legal to use a single grid variable as both the unknown and the RHS (vars = Dict(\"u\" => 1) with ∂ᵢ^2 * u => :u), and a one-shot solve will still return the correct answer. But the writeback then overwrites the RHS with the solution, so any second solve! on that problem would read the previous solution as the new RHS — rarely what you want. Use the two-variable pattern unless you have a specific reason not to.\n\nsolve is the non-mutating twin — it writes the result into an independent narrowed grid and returns a SpringsteelSolution whose physical and coefficients are views into that grid:\n\nsol = solve(prob)\nsol.physical         # view over the solution values\nsol.coefficients     # view over the spectral coefficients\nsol.grid             # independent SpringsteelGrid — pass to write_grid, etc.\n\nsol is a snapshot; mutating grid after the call doesn't change it.\n\nDerivative atoms: ∂ᵢ, ∂ⱼ, ∂ₖ (generic) and ∂_x, ∂_y, ∂_z, ∂_r, ∂_θ, ∂_λ (physical — resolved against the grid's geometry). Build 2D operators by summing:\n\nL = ∂_x^2 + ∂_y^2                    # Cartesian Laplacian\nL = ∂_r^2 + (1/r) * ∂_r                # cylindrical radial Laplacian\nprob = SpringsteelProblem(grid, L * u => :f)\n\nBackends: :auto (default, sparse LU for mixed-basis grids, dense for pure Chebyshev), :dense, :sparse, :krylov. See Solver Framework for preconditioners, block systems, and the full operator algebra reference.","title":"5. Solving a boundary value problem"},{"category":"section","location":"tutorial/#6.-Importing-data-from-a-regular-array","page":"Tutorial","text":"grid_from_regular_data lifts raw numpy-style array data onto a Springsteel grid and runs the forward transform in one call:\n\nx = collect(0.0:0.05:1.0)              # 21 points, uniform\ndata = @. exp(-(x - 0.5)^2 / 0.01)\n\ngrid = grid_from_regular_data(x, data;\n    mubar = 1,\n    BCL   = DirichletBC(),\n    BCR   = DirichletBC(),\n    vars  = Dict(\"u\" => 1),\n)\n\nCoordinate vectors must be uniformly spaced and (for mubar ≠ 1) have lengths compatible with the internal quadrature. 2D and 3D overloads take (x, y, data) or (x, y, z, data) respectively.\n\nFor netCDF inputs, grid_from_netcdf handles dimension permutation and per-variable import:\n\ngrid = grid_from_netcdf(\"rainfall.nc\";\n    dim_names = (\"lon\", \"lat\"),\n    var_names = [\"precip_mm\"],\n    BCL = NaturalBC(), BCR = NaturalBC(),\n    BCD = NaturalBC(), BCU = NaturalBC(),\n)","title":"6. Importing data from a regular array"},{"category":"section","location":"tutorial/#7.-Interpolating-between-grids","page":"Tutorial","text":"Once you have a spectral source grid, interpolate_to_grid! moves data to any target grid of the same geometry:\n\ncoarse = grid_from_regular_data(x, data; mubar=1, BCL=DirichletBC(), BCR=DirichletBC(), vars=Dict(\"u\"=>1))\n\nfine_gp = SpringsteelGridParameters(\n    geometry = \"R\", iMin = 0.0, iMax = 1.0, num_cells = 40,\n    vars = Dict(\"u\" => 1),\n    BCL  = Dict(\"u\" => DirichletBC()),\n    BCR  = Dict(\"u\" => DirichletBC()),\n)\nfine = createGrid(fine_gp)\n\ninterpolate_to_grid!(coarse, fine)       # fills fine.physical from coarse spectrum\n\nFor cross-geometry interpolation (e.g., a Cartesian RR analysis into a cylindrical RL hurricane grid) pass a coordinate_map callback; see Interpolation.\n\nTo evaluate a grid at arbitrary points without building a target grid:\n\npts = hcat(rand(100) .* 50.0,            # r\n           rand(100) .* 2π)               # λ\nvals = evaluate_unstructured(rl_grid, pts; vars=[\"u\"])","title":"7. Interpolating between grids"},{"category":"section","location":"tutorial/#8.-Relocating-a-cylindrical-grid","page":"Tutorial","text":"For vortex-tracking workflows where you want the grid origin to follow a moving center, relocate_grid! reprojects all spectral data to a new Cartesian center in place:\n\ngp = SpringsteelGridParameters(\n    geometry = \"RL\",\n    iMin = 0.0, iMax = 100.0, num_cells = 30,\n    vars = Dict(\"u\" => 1),\n    BCL = Dict(\"u\" => NaturalBC()), BCR = Dict(\"u\" => NaturalBC()),\n)\ngrid = createGrid(gp)\n# ... fill a vortex into grid.physical and spectralTransform!(grid) ...\n\nrelocate_grid!(grid, (5.0, 3.0); boundary = :azimuthal_mean)\ngrid_center(grid)     # → (5.0, 3.0); cumulative across calls\n\nAvailable boundary strategies for the OOB annulus: :nan, :nearest, :azimuthal_mean (default), :bc_respecting. taper_width=N smooths the transition over the last N rings. Repeated in-place calls track the cumulative center shift via grid_center.\n\nSee Grid Relocation for the full strategy table, per-radius fast path details, and multi-patch relocation.","title":"8. Relocating a cylindrical grid"},{"category":"section","location":"tutorial/#9.-Multi-patch-grids","page":"Tutorial","text":"Chain or embed B-spline patches to cover a large domain with non-uniform resolution — e.g., a fine inner mesh for a hurricane eyewall and a coarser outer mesh for the environment:\n\nmg = createMultiGrid(Dict(\n    :topology   => :chain,\n    :geometry   => \"RL\",\n    :boundaries => [0.0, 50.0, 100.0],   # 2 patches\n    :cells      => [20, 10],              # 2:1 ratio (inner fine)\n    :vars       => Dict(\"u\" => 1),\n    :BCL        => Dict(\"u\" => NaturalBC()),\n    :BCR        => Dict(\"u\" => NaturalBC()),\n))\n\n# Fill physical data on every patch, then run the whole thing as one\nfor patch in mg.mpg.patches\n    pts = getGridpoints(patch)\n    patch.physical[:, 1, 1] .= exp.(-(pts[:, 1] ./ 15).^2)\nend\nspectralTransform!(mg)\nmultiGridTransform!(mg)\n\nmultiGridTransform! handles interface synchronisation — the primary (outer) patch runs first, update_interface! pushes its border coefficients into the secondary (inner) patch's R3X ahat vector, and then the inner patch transforms. Grid and spectral transforms have no temporary memory allocation overhead, just CPU time.\n\nSee Multi-Patch Grids for embedded topology, the 2:1 half-gridpoint constraint, and the R3X coupling matrices.","title":"9. Multi-patch grids"},{"category":"section","location":"tutorial/#10.-Saving-and-loading-grids","page":"Tutorial","text":"Springsteel ships two persistence formats.","title":"10. Saving and loading grids"},{"category":"section","location":"tutorial/#JLD2-—-full-grid-state","page":"Tutorial","text":"save_grid / load_grid serialise the complete grid (physical, spectral, basis parameters) to a zstd-compressed JLD2 file. Reloaded grids are ready to transform immediately — FFTW plans are reconstructed on load from cached templates.\n\nsave_grid(\"vortex.jld2\", grid)\ngrid2 = load_grid(\"vortex.jld2\")\n\nspectralTransform!(grid2)    # works, no fresh createGrid needed\ngridTransform!(grid2)\n\nJLD2 is the format to use for checkpointing, restarting simulations, and passing spectral state between processes.","title":"JLD2 — full grid state"},{"category":"section","location":"tutorial/#NetCDF-—-CF-compliant-physical-output","page":"Tutorial","text":"write_grid writes grid.physical to a NetCDF file with CF coordinate metadata suitable for xarray, NCO, CDO, and most visualisation tools:\n\nwrite_grid(\"vortex.nc\", grid; vars = [\"u\"])\n\nThe companion read_netcdf / grid_from_netcdf bring external data in — read_netcdf into plain arrays, grid_from_netcdf straight onto a new SpringsteelGrid.\n\nBoth functions support variable subsets, attribute metadata, and per-direction regular output grids via regularGridTransform.","title":"NetCDF — CF-compliant physical output"},{"category":"section","location":"tutorial/#11.-Summary:-the-core-workflow","page":"Tutorial","text":"using Springsteel\n\n# 1. Configure\ngp = SpringsteelGridParameters(\n    geometry = \"R\",\n    iMin = 0.0, iMax = 1.0, num_cells = 30,\n    vars = Dict(\"u\" => 1),\n    BCL  = Dict(\"u\" => DirichletBC()),\n    BCR  = Dict(\"u\" => DirichletBC()),\n)\n\n# 2. Build\ngrid = createGrid(gp)\n\n# 3. Fill physical data\npts = getGridpoints(grid)\ngrid.physical[:, 1, 1] .= sin.(π .* pts)\n\n# 4. Forward transform\nspectralTransform!(grid)\n\n# 5. Inverse transform (fills values + derivatives)\ngridTransform!(grid)\n\n# 6. (Optional) solve, interpolate, relocate, or write out\n\nFrom here, the feature-specific pages walk through more details of each subsystem.","title":"11. Summary: the core workflow"}]
}
